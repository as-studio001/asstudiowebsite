# 專案說明（AS studio 首頁）

這是「原型結構 / AS. studio」的靜態首頁，部署在 GitHub Pages。

**⚠️ `index.html` 不是轉址頁，是一份獨立的完整首頁複本（2026-08-18 發現，這條說明本身以前是錯的）**：`index.html` 曾經只是一段 `<meta http-equiv="refresh">` 轉址到 `建築事務所首頁.dc.html`，但現在已經變成一份**跟首頁內容幾乎一樣、但獨立維護、不會自動同步**的完整檔案（2500+ 行，含自己的一整套 `nodeData`／`edgeData`／前導動畫程式碼），從 git log 看是帳號 `as-studio001`（不是這個 repo 平常在用的維護者帳號）透過另一條路徑（可能是原型工具本身的「發布」功能）直接覆蓋上去的。**只改 `建築事務所首頁.dc.html`不會反映到 `index.html`，兩份檔案的內容已經會有落差**（例如目前兩邊 `r109`「原型建築」節點的 `href` 就不一樣）。

**這代表兩件事，動手前要注意**：

1. **GitHub Pages 預設會把 `index.html`當成網站根目錄實際顯示的頁面**，不是 `建築事務所首頁.dc.html`——如果維護者說「網站上還是舊的」、「怎麼還沒改好」，先檢查 `index.html` 是否也需要同步套用同一份改動，不要只改 `建築事務所首頁.dc.html` 就以為完成了（2026-08-18 就發生過這個問題：字卡跳字動畫在 `建築事務所首頁.dc.html` 改掉了，但 `index.html` 裡是完全獨立、更舊的一份，沒人記得同步，使用者回報「還是有播」）。
2. **改動涉及首頁邏輯/版面/節點資料時，兩個檔案通常都要一起改**（除非維護者明確說只要改其中一個）。套用前可以用 `grep` 快速比對兩邊差異多大，改完也建議跑一次 diff 確認兩邊邏輯一致；如果差異很大或不確定該以哪份為準，先問維護者，不要自己猜。

## 節點資料的維護方式

首頁上的圓點（節點）與連線資料，內容來源是維護者自己的 Excel 活頁簿（工作表1：A 欄=網址、C 欄=節點名稱、D 欄起=連線對象、C 欄黃色底色=網址尚未定案）。網頁的 `建築事務所首頁.dc.html` 裡 `nodeData`／`edgeData` 是寫死的靜態陣列，跟 Excel **沒有即時連動**，需要人工（或請 Claude）比對後手動更新。**同一個檔案裡還有第三個容易被漏掉的 Excel 驅動資料**：跳轉子頁時的閃字過場動畫 `WORD_POOL`，對應活頁簿「前導動畫字串」工作表（單欄無標題列），同步節點資料時要一併檢查，別只顧著 nodeData/edgeData。

**如果有人拿一份 Excel 活頁簿來，要求比對或更新首頁節點資料，請先讀 [維護手冊_Excel資料同步.md](./維護手冊_Excel資料同步.md) 再動手** —— 裡面記錄了完整的欄位定義、比對規則（黃底節點不更新 href、「原型教學」固定排除、名稱正規化比對方式）與已知的資料品質問題，避免每次都要重新摸索一次。

**注意**：Excel 活頁簿通常不會放在這個資料夾裡，維護者的訊息裡如果沒有附上 Excel 檔案的完整路徑，不要用猜的或亂找，直接請對方提供路徑（`維護手冊_Excel資料同步.md` 開頭有起手式範本可以請對方照著填）。

## 修改時的限制

除非維護者明確要求調整版面／連線，否則同步 Excel 資料時只更動 `nodeData[].href`，不要動 `id`、`name`、陣列順序、`edgeData`、節點位置計算或顏色／大小邏輯——這些是手動調過的排版，Excel 沒有對應欄位可以驅動。如果 Excel 出現網頁完全沒有的新節點，這已經超出單純同步 href 的範圍，照 `維護手冊_Excel資料同步.md`裡「新增節點的做法」那一節處理，且動手前務必先給維護者確認要新增的內容（節點會加進圓環排版、連線數也會影響圓點大小，不是無痛操作）。搜尋功能是即時讀 `nodeData`，不用另外維護索引。

## 子頁面（原型教學等）的維護方式

`原型教學.dc.html` 這類子頁用另一份手冊：[維護手冊_子網頁資料同步.md](./維護手冊_子網頁資料同步.md)。子頁的橫向滑塊畫廊（`galleryItems`）是 Excel 驅動的，欄位是編號/大標/小標，工作表**第一列是標題列**（跟首頁的「工作表1」不同）——**但每個子頁要抓哪個工作表，一律在架設當下直接問架設人，不要用「頁面標題＝工作表名稱」自動猜**。時間軸模組是可選的，不是每個子頁都要放，架設新子頁時也要一併問清楚。活頁簿裡已經有「原型原點」「原型結構」「原型建築」「原型獲獎」等工作表準備好資料，但對應網頁還沒做，如果維護者要求新增子頁，照該手冊最後一節的步驟做。

**如果有人拿 Excel 來要求比對／更新子頁內容，請先讀那份手冊再動手**，跟首頁手冊一樣：路徑要維護者明確提供、套用前先列差異確認、不要動版型 CSS 與動畫邏輯。

子頁內文段落裡如果要放「案例資訊卡」（可展開收合的手風琴，含案例規格清單、開啟時的卡片光暈與游標光源陰影效果，範例見 `原型原點.dc.html`「海之博物館」卡），這是一個獨立的可重用元件，做法見 [維護手冊_案例資訊卡元件.md](./維護手冊_案例資訊卡元件.md)，不要重新設計一次。

## 部署方式

參考 `../../wix修改流程.txt`：網域 DNS 指向 GitHub Pages，`.nojekyll` 已存在於本資料夾，確保 GitHub Pages 不會用 Jekyll 處理檔案。
