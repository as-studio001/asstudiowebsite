# 專案說明（AS studio 首頁）

這是「原型結構 / AS. studio」的靜態首頁，部署在 GitHub Pages。

**⚠️ `index.html` 不是轉址頁，是一份獨立的完整首頁複本（2026-08-18 發現，這條說明本身以前是錯的）**：`index.html` 曾經只是一段 `<meta http-equiv="refresh">` 轉址到 `建築事務所首頁.dc.html`，但現在已經變成一份**跟首頁內容幾乎一樣、但獨立維護、不會自動同步**的完整檔案（2500+ 行，含自己的一整套 `nodeData`／`edgeData`／前導動畫程式碼），從 git log 看是帳號 `as-studio001`（不是這個 repo 平常在用的維護者帳號）透過另一條路徑（可能是原型工具本身的「發布」功能）直接覆蓋上去的。**只改 `建築事務所首頁.dc.html`不會反映到 `index.html`，兩份檔案的內容已經會有落差**（例如目前兩邊 `r109`「原型建築」節點的 `href` 就不一樣）。

**這代表兩件事，動手前要注意**：

1. **GitHub Pages 預設會把 `index.html`當成網站根目錄實際顯示的頁面**，不是 `建築事務所首頁.dc.html`——如果維護者說「網站上還是舊的」、「怎麼還沒改好」，先檢查 `index.html` 是否也需要同步套用同一份改動，不要只改 `建築事務所首頁.dc.html` 就以為完成了（2026-08-18 就發生過這個問題：字卡跳字動畫在 `建築事務所首頁.dc.html` 改掉了，但 `index.html` 裡是完全獨立、更舊的一份，沒人記得同步，使用者回報「還是有播」）。
2. **改動涉及首頁邏輯/版面/節點資料時，兩個檔案通常都要一起改**（除非維護者明確說只要改其中一個）。套用前可以用 `grep` 快速比對兩邊差異多大，改完也建議跑一次 diff 確認兩邊邏輯一致；如果差異很大或不確定該以哪份為準，先問維護者，不要自己猜。

## 節點資料的維護方式（2026-08-19 起：Excel 流程已停用）

**首頁不再用 Excel 活頁簿維護節點/連線資料**——舊的 Excel 驅動流程（活頁簿工作表對應 `nodeData`/`edgeData`/`galleryItems` 等）已經被一套獨立的「後台」管理工具取代，維護者透過那套工具直接視覺化編輯圓點位置/連線/內容，編輯完按下「同步到主網頁」會自動把結果推送到這個 repo（`git log` 裡看得到 `as-studio001` 帳號留下的 "Update homepage ... via 圓點位置編輯器"、"Fetch content from Internal-Pages admin backend for ..." 這類 commit，就是這套後台工具留下的紀錄）。對應的舊版說明文件（`維護手冊_Excel資料同步.md`、`維護手冊_子網頁資料同步.md`）跟 `reference_files/` 裡的 `.xlsx` 活頁簿都已經在 2026-08-19 從 repo 刪除，不要再照那兩份手冊裡描述的 Excel 欄位對應方式行事。

**這套後台工具本身的原始碼不在這個 repo 裡**，Claude 目前沒有存取權限，如果後台工具出問題（例如畫面卡住、報錯），沒辦法直接進去修，要跟維護者確認那套工具的程式碼位置或請維護者回報給對應的維護管道。

**目前實務上還在用的同步方式**是 `D:\2026as實習\網頁\站存.txt`——維護者用後台工具編輯完、部署上線後，會不定期把整份上線的 HTML 貼存到這個檔案，請 Claude 比對這份存檔跟 repo 裡 `建築事務所首頁.dc.html`（有時也要一併同步 `index.html`，見上面「index.html 不是轉址頁」那節）的 `nodeData`/`edgeData`/`specialIds` 差異、套用真正的內容差異。大量新增/刪除/重新排序節點時，務必用「bracket 深度掃描抓陣列邊界＋offset 精確 slice 取代＋`.NEWSYNC.tmp` 暫存驗證再覆蓋」這套安全做法，不要用簡單的正規表達式或 `String.replace(str, str)` 整段抓換——`站存.txt` 的匯出格式不保證跟 repo 裡的排版一致（常見是整段陣列擠成一行），naive 的字串比對容易抓錯邊界，這個教訓在 2026-08-18 真的把主網頁寫壞過一次（後來用 git 復原），細節可以查當時的對話紀錄或 git commit history。

`index.html`（跟偶爾也會被同一套後台工具直接覆蓋的 `建築事務所首頁.dc.html`）的 `nodeData`/`edgeData` 有時會跟 `站存.txt` 或本機 repo 版本有落差（新增節點漏帶、`href` 被覆寫成舊值等）——這是後台工具自己的匯出/發布流程造成的，不是本機這邊的同步腳本錯。發現落差時，比對後如實反映差異就好，不要自己猜測、自己「補回去」或「修正」對方的資料，除非維護者明確要求。

## 修改時的限制

除非維護者明確要求調整版面／連線，否則同步 `站存.txt` 資料時只套用真正的內容差異（`href`、節點名稱用字等），不要順手動 `id`、陣列順序、`edgeData`、節點位置計算或顏色／大小邏輯——這些通常是維護者在後台工具裡刻意調整過的排版。如果出現網頁完全沒有的新節點，動手前務必先跟維護者確認要新增的內容（節點會加進圓環排版、連線數也會影響圓點大小，不是無痛操作）。搜尋功能是即時讀 `nodeData`，不用另外維護索引。

## 子頁面（原型教學等）的維護方式

`原型教學.dc.html` 這類子頁的內容現在也是透過上面提到的後台管理工具維護（"Fetch content from Internal-Pages admin backend for ..." 這類 commit 對應的就是子頁內容更新），不再是 Excel 驅動的橫向滑塊畫廊資料。子頁本身的版型/CSS/動畫邏輯（Dark/Light 模式變數、時間軸模組是否要放等）維護時還是要謹慎，改之前先確認清楚要動的範圍，不要自己猜。

子頁內文段落裡如果要放「案例資訊卡」（可展開收合的手風琴，含案例規格清單、開啟時的卡片光暈與游標光源陰影效果，範例見 `原型原點.dc.html`「海之博物館」卡），這是一個獨立的可重用元件，做法見 [維護手冊_案例資訊卡元件.md](./維護手冊_案例資訊卡元件.md)，不要重新設計一次。

## 部署方式

參考 `../../wix修改流程.txt`：網域 DNS 指向 GitHub Pages，`.nojekyll` 已存在於本資料夾，確保 GitHub Pages 不會用 Jekyll 處理檔案。
