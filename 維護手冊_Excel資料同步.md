# 首頁節點資料維護手冊：從 Excel 同步到網頁

這份文件給「下一位維護者」看，也給接手的 Claude（不論哪個帳號、哪台電腦）看。目的是讓任何人拿到這個資料夾＋一份 Excel 活頁簿，就能重現同一套比對／更新流程，不需要重新問一遍「這個網站是怎麼運作的」。

## 背景：兩份檔案的關係

- **內容來源**：`活頁簿1.xlsx`（作者自己維護的 Excel，記錄每個節點的名稱、連結網址、彼此的連線關係）
- **實際顯示**：`建築事務所首頁.dc.html` 裡的 `nodeData` 陣列（節點名稱＋連結）與 `edgeData` 陣列（節點之間的連線）

**這兩者沒有即時連動**。網頁是純靜態的 GitHub Pages（見同資料夾 `index.html` 會自動轉址過去），瀏覽器沒辦法在使用者開網頁時去讀你電腦裡的 `.xlsx`。所以每次 Excel 更新後，都需要「有人（或 Claude）手動比對、手動改 `nodeData`／`edgeData` 陣列」，這份文件就是在講怎麼做這件事。

## Excel 檔案的資料結構（工作表1）

| 欄位 | 內容 |
| --- | --- |
| A 欄 | 該節點的連結網址（對應網頁 `nodeData[].href`） |
| B 欄 | 「記號-N」分類標籤，僅供人工分組參考，**不是**節點的唯一 ID，比對時忽略即可 |
| C 欄 | 節點名稱（對應網頁 `nodeData[].name`），**這是比對的主鍵** |
| D 欄起 | 該節點連到的其他節點名稱，一列可能橫向列出多個（D、E、F...都算），對應網頁 `edgeData` 裡以這個節點為一端的所有連線 |

### 重要規則：C 欄黃色底色 = 網址尚未定案

C 欄背景色為黃色（`solid`，RGB `FFFFFF00`）的列，代表該節點的 A 欄網址還沒確定（目前多半是佔位用的「副本-原型建築-1／2」頁面），**這些列的 href 一律不要更新到網頁**，保留網頁現有的連結。用 openpyxl 讀取時判斷方式：

```python
fill = cell.fill  # C 欄該列的 cell
is_yellow = (fill.patternType == "solid"
             and fill.fgColor.type == "rgb"
             and fill.fgColor.rgb == "FFFFFF00")
```

### 固定排除：連到站內子頁的節點（目前是「原型教學」「原型原點」「原型獲獎」「原型結構」）

不論黃底與否，凡是 href 已經改指向**站內子頁**（例如 `./原型教學.dc.html` 這種相對路徑，而不是外部網址）的節點，都不要被 Excel 覆蓋。目前「原型教學」（`./原型教學.dc.html`）、「原型原點」（`./原型原點.dc.html`）、「原型獲獎」（`./原型獲獎.dc.html`，節點 `r115`）與「原型結構」（`./原型結構.dc.html`，節點 `r23`）四個節點屬於這類，這是網站建置時刻意改的內部連結，不是 Excel 外部網址的替代品（它們本來就是黃底節點之一，這條規則算是雙重保險）。

**這條規則會隨著子頁越做越多而增加**：以後如果「原型建築」等節點也做出對應子頁（見 [維護手冊_子網頁資料同步.md](./維護手冊_子網頁資料同步.md) 最後一節），該節點的 href 就要同樣加進這份排除名單，改指向新做好的子頁檔案，不要再讓 Excel 的外部網址蓋回去。判斷方式很簡單：**網頁 href 已經是 `./某檔名.dc.html` 這種相對路徑的，一律不要被 Excel 覆蓋**，不用另外維護一份名單。

**「原型教學」子頁目前被多個不同名稱的節點共用連結**：`r3`（實習）、`r4`（美感教育）、`r5`（孩童）、`r6`（文化）、`r7`（設計方法）、`r8`（工作文化）、`r9`（大事記）、`r10`（同名的第二個「工作文化」）、`r11`（原型數位）、`r12`（ArchiCAD）、`r13`（midjourney）、`r14`（midas），這 12 個節點的 href 都刻意改成 `./原型教學.dc.html`，因為它們對應的內容都收在「原型教學」這個子頁裡。這不是資料錯誤或該用同一個節點名稱表示，同步 Excel 時看到這些節點名稱各自不同但 href 相同，屬於預期狀況，一樣照上面的相對路徑規則排除、不覆蓋。

**「原型原點」子頁同樣被多個節點共用連結**：`r15`（原型原點本身）、`r16`（SDG(Structural Design Group)）、`r17`（1992 海之博物館）、`r18`（1996 東京國際）、`r19`（2002橫濱大）、`r20`（原型三時期）、`r21`（一人）、`r22`（原型建築+原型結構），這 8 個節點的 href 都指向 `./原型原點.dc.html`，原理跟上面「原型教學」那批一樣，同步時一併排除、不覆蓋。

**「原型獲獎」子頁被 `r115` 本身之外，還有活頁簿「工作表1」第 116 列（D116:AE116，也就是連到 `r115` 的 28 個獎項節點）共用連結**：`r30`～`r58` 這個連續區間內，除了 `r39`（2022 X-SITE「藍屋」獲獎，不在該列連線清單裡，維持原網址）之外的 28 個節點（`r30` 2011媽祖宗教文化園區眺望台雜誌報導、`r31` 2012新營服務區公廁新建工程雜誌報導、`r32` 2013雲林縣微笑餐廳雜誌報導、`r33` 2021構竹臨帖新銳獎、`r34` 2024香港大學木構專案、`r35`～`r38`／`r40` 各屆 X-SITE 獲獎、`r41`～`r58` 各項競圖獲勝／建築獎／IF設計獎／ADA新銳建築獎等），href 都改成 `./原型獲獎.dc.html`。**這批節點名稱跟 Excel D 欄以後的連線用字有多筆年份或少字筆誤（例如 `r50` 網頁上是「2016 雲林農博」但 Excel 該列寫「2015雲林農博」、`r42` 網頁缺「設計」二字、`r35` 網頁是「2025 X-SITE」但其他工作表寫「2015X-SITE」)，比對時是依上面「Excel 資料品質提醒」那條已知問題，用語意判斷是同一個獎項後才歸進這個排除區間，不是逐字完全相符——同步 Excel 時這 28 個節點一樣排除、不覆蓋。

### A 欄空白的列

保留網頁現有的 href（通常網頁上會是 `href="#"` 佔位），不要清空或亂填。

## Excel 資料結構：前導動畫字串（WORD_POOL）

`建築事務所首頁.dc.html` 裡除了 `nodeData`／`edgeData`，還有第三個從 Excel 來的資料，**很容易被忽略，同步時要記得一起檢查**：`WORD_POOL` 陣列（[建築事務所首頁.dc.html:873](./建築事務所首頁.dc.html) 附近），對應活頁簿的**「前導動畫字串」工作表**。

**⚠️ 2026-08-18 起，這個「快速閃字」過場動畫（黑底白字、字詞快速輪播切換）已經整個拿掉，`WORD_POOL` 陣列本身也已經從程式碼裡刪除了，全站都不再使用**——一般本機子頁（`.dc.html`）點下去直接原生導頁、不播任何動畫，改成子頁自己到達時播放開場影片；「原型建築」（`r109`，外部連結、子頁檔案較大）這個唯一的特例，原本也用這套閃字＋iframe 預載，同一天也改成先在首頁播一次開場影片才跳轉（見 [維護手冊_子網頁資料同步.md](./維護手冊_子網頁資料同步.md)「前導動畫」一節）。**下面這整節「Excel 資料結構：前導動畫字串（WORD_POOL）」的同步規則已經不適用**——如果活頁簿的「前導動畫字串」工作表以後還在，它已經沒有對應的網頁陣列可以同步了，除非之後重新啟用類似機制。

| 項目 | 內容 |
| --- | --- |
| 工作表 | 「前導動畫字串」，單欄（A 欄），**沒有標題列**，每一列一個詞 |
| 對應網頁 | `var WORD_POOL = [...]` 陣列 |
| 順序是否重要 | **重要**——陣列順序＝動畫播放時逐字切換的順序，Excel 列的順序異動要照樣反映到陣列順序，不能當成無序集合隨便排 |
| 動畫最後停留的字 | 不在 `WORD_POOL` 裡，是程式另外用 `destinationWord`（被點擊節點自己的名稱，見下方連結）動態接在播放序列最後——跟 `WORD_POOL` 本身無關，不要跑去 `WORD_POOL` 裡找目的地文字，也不要把目的地文字加進 `WORD_POOL`。 |

比對／更新節點資料時，**把「前導動畫字串」工作表的內容跟 `WORD_POOL` 逐項比對**（比對方式：依序列出兩邊清單，看有沒有缺字、多字、順序跑掉），這個陣列是單純的字串清單、不影響版面位置或圓環排版，可以比照 href 用同樣的「列出差異、確認後套用」流程處理，不需要額外的風險評估。

**已修正紀錄（2026-07-15）**：當天發現活頁簿「前導動畫字串」工作表有 13 個詞（章architecturedesign、原型教學、原型原點、原型結構、木構造、竹構造、RC構造、鋼構造、成construction、法structuredata、構technics、原型建築、原型獲獎），但網頁 `WORD_POOL` 當時只有 12 個，漏了「原型教學」（在 Excel 裡排第 2 個）。已經補上，兩邊現在 13 項逐字對齊。這個案例留著當範例：`WORD_POOL` 這種不顯眼的清單也會跟 Excel 產生落差，同步時別漏掉它。

## 開始之前：一定要先給 Claude 兩個檔案的「完整路徑」

**這份手冊、這個資料夾，在每個人電腦上的位置都不一樣**。Claude 沒辦法自己搜遍整台電腦找檔案，只有在被明確告知路徑之後才讀得到。所以維護者開新對話請 Claude 做這件事時，訊息裡必須包含：

1. Excel 活頁簿目前存在電腦的完整路徑，例如 `C:\Users\某某\Desktop\活頁簿1.xlsx`
2. `建築事務所首頁.dc.html` 目前存在電腦的完整路徑（如果是剛從 GitHub 下載的整個資料夾，把該資料夾路徑也一併給，方便 Claude 順便看到本手冊跟 `CLAUDE.md`）

**另外要注意**：`CLAUDE.md` 只有在 Claude Code 的工作目錄「就是」或「在」`design_handoff_architecture_site` 這個資料夾底下時才會自動載入。如果維護者是在別的資料夾開 Claude Code、或用的是網頁版 claude.ai（不會自動讀取專案裡的檔案），這份自動載入不會生效——這種情況下，起手式訊息裡除了兩個檔案路徑，**還要把這份手冊本身的路徑也附上**（或直接把手冊內容貼給 Claude），Claude 才知道要照這些規則做。

### 建議的起手式範本

> 「請幫我比對並更新首頁節點資料。
> Excel 活頁簿路徑：`（貼上完整路徑）`
> 網頁檔案路徑：`（貼上完整路徑）`
> 維護手冊路徑：`（貼上完整路徑，如果 Claude 沒有自動讀到的話）`
> 請照手冊規則比對，先列出差異給我看，不要直接改網頁。」

把三個路徑都換成實際位置，貼給 Claude 即可開始。

## 比對／更新的標準流程

請未來的維護者（或直接請 Claude）照這個順序做：

1. **確認兩份檔案的路徑**（見上一節），必要時請 Claude 讀取檔案確認打得開、內容看起來正確。
2. **讀取 Excel**：工作表1（C 欄＝節點名稱、A 欄＝網址、D 欄起＝連線對象，並記錄每列 C 欄儲存格是否為黃色底色），**以及「前導動畫字串」工作表**（單欄清單，見上面「Excel 資料結構：前導動畫字串」）。
3. **讀取網頁**：解析 `建築事務所首頁.dc.html` 裡 `nodeData = [...]`（節點 id／name／href）、`edgeData = [...]`（`['rX','rY']` 連線配對）**與 `WORD_POOL = [...]`**（前導動畫字串陣列）這三個陣列的原始碼。
4. **名稱比對規則**：兩邊的節點名稱先各自去除所有全形／半形空白再比對（例如網頁的 `"2015 嘉義市立美術館 監造案 競圖獲勝"` 要能對到 Excel 的 `2015嘉義市立美術館監造案競圖獲勝`）。
5. **列出差異**，分四類回報給維護者：
   - 節點名稱：Excel 有但網頁沒有 / 網頁有但 Excel 沒有
   - href：兩邊都有這個節點，但網址不同（黃底與已連到站內子頁的節點除外，這兩種不比對、不更新）
   - 連線：Excel 宣告的連線在網頁 `edgeData` 裡找不到對應、或反過來網頁多畫了 Excel 沒有的線
   - **連線比對常見雜訊**：如果某筆連線「兩邊名稱都對不太起來」，先檢查是不是 Excel D 欄以後的用字跟該節點自己在 C 欄的名稱不一致（年份打錯、少字、簡繁誤植、儲存格黏字等）——這種情況通常是 Excel 自己的筆誤，不代表網頁真的漏畫，回報時要分開列「Excel 內部命名不一致」跟「兩邊名稱都對得上、網頁確實沒畫」這兩種，只有後者才是真的需要改網頁的項目。
   - **`WORD_POOL`**：跟「前導動畫字串」工作表逐項比對順序與內容，缺字/多字/順序跑掉都要列出來（別漏掉這項，它不像 nodeData/edgeData 那麼顯眼）。
6. **預設更新項目**：把 Excel 的 A 欄網址寫回網頁 `nodeData[].href`（套用「黃底不更新」「已連到站內子頁的節點不更新」「A 欄空白不更新」三條規則），並把 `WORD_POOL` 同步成跟「前導動畫字串」工作表一致（順序也要對齊）。**不要**改動 `id`、`name`、`mobileName`（2026-07-31 新增的手機版縮減名稱欄位，見下面「手機版節點文字縮減」一節——Excel 沒有對應欄位，純粹是網頁這邊手動策展的結果，同步時**只能新增/保留，絕對不要因為改寫整個 `nodeData` 陣列而不小心把既有節點的 `mobileName` 洗掉**）、陣列順序、`edgeData`、節點位置計算、顏色或大小相關的程式碼——這些是網站排版手動調過的，改動風險自負。如果 Excel 出現整列都是網頁沒有的**全新節點**，href 同步已經不夠用，要走下面「新增節點的做法」那一節，且務必先跟維護者確認才動手。
7. **更新前列清單給維護者確認**：哪些節點的 href 要被改、改成什麼、`WORD_POOL` 要怎麼調整，讓人看過一眼再套用，不要靜默覆蓋。
8. **套用變更後**，可以用「網頁 nodeData 條目數量前後一致」「edgeData 區塊逐字未變」這類檢查，確認除了 href／`WORD_POOL` 之外真的沒有動到別的東西。

看過差異清單、確認要更新哪些之後，再請 Claude 依手冊規則把 href 寫回網頁（起手式範本見前面「開始之前」那一節）。

## 新增節點的做法（Excel 出現網頁沒有的全新節點時）

這個流程已經在 2026-07-15 實際跑過一次（新增「2025好感空間展」），確認可行，步驟如下：

1. **找一個沒用過的 id**：看 `nodeData` 目前最大的 `rN` 編號，新節點用下一號（例如目前到 r115，新節點就用 `r116`）。id 只是給 `edgeData` 參照用的任意代號，跟排版順序無關，不用跟 Excel 的列號對齊。
2. **插入位置要跟著 Excel 的列順序，不是加在陣列最後面**：`nodeData` 陣列裡「排在第幾個」會決定這個節點在圓環版面上排在哪個角度（`ordered = this.nodeData` 之後依序繞圓環排列，見下面「已知的資料相依細節」）。所以新節點要插進 Excel 裡緊鄰它的前後節點名稱、在 `nodeData` 陣列裡對應的位置之間，讓視覺上的鄰居關係跟 Excel 邏輯順序一致，不要圖方便丟在陣列最後一筆。
3. **href 留空時先填 `"#"` 佔位**：Excel A 欄還沒有網址的話，就跟現有其他未定案的節點一樣填 `"#"`，之後 Excel 補上網址、下次同步時會自動被抓到並更新（前提是這個節點沒有被排除規則擋住）。
4. **把 Excel D 欄起的每個連線對象，加進 `edgeData`**：新增節點跟每個連線對象各補一筆 `['新id', '對象id']`，對象的 id 要去 `nodeData` 裡用名稱查出來。`edgeData` 陣列裡的順序不影響排版，加在陣列最後面即可。
5. **套用後提醒維護者**：節點總數變了，圓環排版的角度、每個節點的圓點大小（依連線數量算的）都會跟著重新分布，這是排版演算法本身的機制，並非誤動——這件事跟「只改 href 完全不影響版面」是不同等級的操作，套用前務必先列出要新增的 `nodeData`／`edgeData` 內容給維護者確認。
6. **搜尋功能不用另外處理**：首頁搜尋是即時用 `this.nodeData.filter(n => n.name...)` 比對（同檔案 `handleSearchInput` 函式），新節點只要進了 `nodeData`，搜尋自動就找得到，沒有另外的搜尋索引要維護。
7. **新節點預設不加 `mobileName`**：手機版縮減名稱（見下面「手機版節點文字縮減」一節）是逐一跟維護者確認過的手動結果，不是自動規則，新節點不加這個欄位的話手機版就會顯示完整的 `name`（安全預設值）。如果新節點的原名很長、想比照其他節點縮減，套用前一樣要先列出縮減前後對照給維護者確認，不要自己直接生一個。

## Excel 資料品質提醒

Excel D 欄以後有不少連線用字跟 C 欄自己的名稱對不起來（年份、簡繁、少字等筆誤），建議維護者之後整理 Excel 時，統一把連線欄位的用字跟 C 欄逐字校對一致，往後比對才不會一直冒出同樣的雜訊。

## 已知的資料相依細節（方便下次快速上手）

- `建築事務所首頁.dc.html` 裡的節點位置是由程式碼算出來的（一個 `positions` 物件，`nodeRadius`／排版邏輯在同檔案內的 `<script type="text/x-dc" data-dc-script">` 區塊，約在 700-750 行），**不是**直接抄自 Excel 的座標，所以改 Excel 不會影響版面位置——但 `nodeData` 陣列裡的排列順序會決定圓環上的角度順序，新增節點時插入位置很重要（見上面「新增節點的做法」）。
- `specialIds` 陣列（分類/大標節點的 id 清單）跟顏色邏輯也是寫死的，Excel 沒有對應欄位可以驅動它，如果要新增分類節點要手動處理這塊。
- 搜尋功能（`handleSearchInput`）直接讀 `this.nodeData`，不是另外維護的索引，`nodeData` 更新了搜尋就會跟著更新。
- **開場影片載入動畫**（`#sc-intro-overlay` / `#sc-intro-video`，[建築事務所首頁.dc.html:9-45](./建築事務所首頁.dc.html) 附近，播放 `uploads/網頁前導片.mp4`）：這跟 Excel 資料無關，純粹是首頁本身的行為。
  - **開場影片**只在真正「新進站」時播放（referrer 不是本站同網域，或是空白／外部來源，例如直接輸入網址、從 Google 或外部連結點進來）。
  - **2026-07-15 加了規則**：從站內任何子頁點連結（例如子頁麵包屑「首頁」）回到首頁時，**不會**重播這段開場影片——判斷方式是看 `document.referrer`：同網域、且不是首頁自己，就直接把 `#sc-intro-overlay` 移除，不播放影片。瀏覽器按「上一頁」回首頁的情況，多半會被瀏覽器的 bfcache 直接還原上次離開時的畫面（那時覆蓋層本來就已經被移除），所以通常也不會重播，這是瀏覽器行為本身如此，不是額外寫的程式碼——但如果瀏覽器沒有用 bfcache（例如快取被清掉），現在的程式碼沒有針對這種情況額外處理，理論上仍可能重播一次，這是已知的殘餘邊界案例，不算嚴重（真的少見，不建議為此額外增加複雜度）。
  - **2026-08-18 起，這支影片也是子頁「前導動畫」的內容**（見 [維護手冊_子網頁資料同步.md](./維護手冊_子網頁資料同步.md)「前導動畫」一節）：使用者要求「把所有進入子網頁的字卡輪播動畫都換成進入主網頁的logo動畫」，本機子頁到達時現在播的就是這支同一支 `網頁前導片.mp4`（子頁自己的 `#sc-arrival-overlay`／`#sc-arrival-video`，靠 `sessionStorage` 旗標 `scTransitionIn` 判斷要不要播，跟首頁 `#sc-intro-overlay` 是同一支影片檔但各自獨立的一份覆蓋層元素，播放邏輯也各自獨立，不是共用同一個 DOM）。
  - **前導動畫**（`WORD_POOL` 閃字）**2026-08-18 起全站都不再使用**，`playAndNavigate()`／`playAndPreloadThenNavigate()`／`WORD_POOL` 都已經整個從程式碼移除。一般本機子頁點下去不攔截、不播任何動畫，直接原生導頁，首頁這邊只在點擊當下設 `scTransitionIn` 旗標，讓子頁知道要播開場影片；`EXTERNAL_PRELOAD_NODE_IDS` 裡列出的節點（目前只有「原型建築」`r109`）改成 `playIntroVideoThenNavigate()`——先在首頁播一次跟首頁自己進站同一支開場影片（`uploads/網頁前導片.mp4`），播完/發生錯誤/15 秒逾時保險任一觸發才真正跳轉，**不再等外部頁面真的載入完成**（原本 iframe `load` 事件判斷載入完成的機制已經拿掉，這是使用者確認過可以接受的取捨），見下面「原型建築」節點改用循環版前導動畫」一節（歷史記錄，內容已過時，最新做法見再下一節的更新記錄）。
- **⚠️ `{{ }}` 樣板語法不支援三元運算子（`? :`）跟 `&&`／`||`**（2026-07-15 實測發現，見 `support.js` 的 `resolve()`/`resolvePath()` 函式）：只能寫單純的變數或路徑，例如 `{{ isMobile }}`、`{{ node.cx }}`、`{{ isMobile ? 'a' : 'b' }}`。如果直接在樣板裡寫 `{{ isMobile ? 'none' : 'flex' }}` 這種三元判斷，**不會報錯，但會靜默失效**——那個 CSS 屬性整個從畫面上消失，不是變成 `none` 或 `flex` 其中一個。正確做法：一律先在 `renderVals()` 裡把條件判斷算成一個單純命名變數（例如 `const searchBoxDisplay = isMobile ? 'none' : 'flex';`），回傳物件裡放這個變數，樣板只寫 `{{ searchBoxDisplay }}`。**這是既有程式碼裡到處都在用的正確寫法**（`bgColor`、`textColor` 等都是這樣），新增任何條件式樣式都要照這個模式，不要在 `{{ }}` 裡面直接寫三元或邏輯運算子。
  - 順帶一提：檔案裡原本就有幾處違反這個規則、直接寫 `{{ isDarkMode ? 'oklch(...)' : 'oklch(...)' }}`（漢堡選單下拉框、搜尋結果下拉框的背景色），這些其實從來沒有真的隨深色模式切換過，是既有的小 bug，影響很小（有毛玻璃模糊擋著不明顯），還沒有修。

## 手機／平板適配（2026-07-15 首次、2026-08-19 五十二次重大調整）

首頁在 768px 寬度以下會切換成手機版行為，768px（含）以上維持桌機/平板的畫面與程式邏輯，**但這個判斷從 2026-08-02（第十六次調整）起不再是單純比較 `viewportW`，而是統一透過 `_effectiveIsMobile()` 這個方法**（見下面第十六次調整那節）——畫面上新增了一個手動切換鈕，使用者可以無視目前畫面寬度、強制切換成手機版或桌機版。**以後任何要判斷「現在是不是手機版」的地方，一律呼叫 `this._effectiveIsMobile()`，不要再直接寫 `this.state.viewportW < 768` 或 `window.innerWidth < 768`**，否則會出現「畫面明明手動切成手機版，某個功能卻還是照桌機規則跑」這種樣板跟邏輯對不上的 bug（這正是第十六次調整實際踩過、修正的問題，見下面詳細說明）。

手機版的圓環視覺化這幾週改了三十四次方向，幅度都不小，維護時要認清「現在到底是哪一版」：

1. 2026-07-15：2D 圓環，但精簡過（只畫連線數 >3 的 19 個節點、點徑/字級放大）。
2. 2026-07-31（第一次）：改回 2D 圓環、但畫出跟桌機一樣完整的全部 116 個節點，單純把容器縮小等比例縮圖。
3. 2026-07-31（第二次）：整個圓環圖換成可拖曳旋轉的 3D 球體（使用者要求：「假設手機版的主視覺一樣以圓形為概念，只是將 2D 的平面圓形以及圓點改為 3D 圓效果，透過每個圓點之間的連線維持球體的結構，並可以依照操作者的滑動轉動球體」）。
4. 2026-07-31（第三次）：3D 球體本身不變，但加了雙指縮放、球體整體放大、中央標題改成跟著球體一起 3D 傾斜、手機版限制只有分類大標節點可以點擊（使用者回報：「整體主視覺太小」「原型×未來字樣...也會隨著球體轉動而轉動的立體效果」「手機版的小圓點只有大標題的圓點允許點擊」）。
5. 2026-07-31（第四次）：取消標題跟著球體傾斜的效果，改回固定正面顯示（使用者體驗過第三次的效果後要求取消）。
6. 2026-07-31（第五次）：手機版搜尋改成「飛到北極點」的地球儀效果——搜尋並點選結果後，黃色標註不再自動淡出（保持顯示），球體會動畫轉動把該節點帶到畫面正上方並固定住，其餘節點依原本速度繼續繞著這個新軸自轉，類似真實地球儀「鎖定某個地點、其餘依緯度自轉」的效果。分兩批做：先做「保持標註」跟「飛到北極點固定」，使用者確認沒問題後才繼續做地標 icon。
7. 2026-07-31（第六次）：標註節點正上方加一條會動畫延伸的虛線＋地標 icon，點地標 icon 或標註的圓點本身都能進入子頁（使用者要求的第三點，延續第五次）。連帶把「手機版只有分類大標節點可以點擊」的限制（第三次調整加的）開了一個例外：目前被搜尋標註的節點，不管本來是不是分類大標，都恢復可以點擊。
8. 2026-07-31（第七次）：虛線＋地標 icon 改成純線條、不填色（黑／金）；分類大標節點在球面上重新分布、不再擠成一團；球體的裁切框上下緣貼齊頁首下緣/頁尾上緣，不再留大片空白（使用者這次一次回報三點：文字顯示規則、大標分布、還有「好像有一個框架罩住主視覺」）。
9. 2026-07-31（第八次）：修正「文字只有一開始在正面的節點才會顯示，轉到背面之後永遠不會再出現文字」的 bug（第七次調整第一點原本被判定是「既有行為、沒有 bug」，使用者這次進一步描述症狀後才確認真的是 bug，見下面詳細說明）。
10. 2026-07-31（第九次）：地標 icon 位置上移、虛線變細、修正 icon 頂端貼近頁首的間距；搜尋標註成立後鎖定球體只能水平拖曳；點擊球體以外區域時標註消失並解除鎖定（使用者一次回報四點，前兩點是地標 icon 的位置/樣式微調，後兩點是新增的拖曳行為限制，見上面詳細說明）。
11. 2026-07-31（第十次）：手機版球體的節點文字改用縮減過的短名稱（`nodeData[].mobileName`），桌機圓環、搜尋結果仍顯示完整原名（使用者提供縮減前後對照清單、逐一確認每個節點的縮法，並處理了縮完會撞名的重複項目，見上面詳細說明）。
12. 2026-07-31（第十一次）：地標 icon／虛線改用比頁首更高的 z-index（不再靠夾位置閃避頁首）、線條與 icon 都再縮小/變細、虛線改成漸進式斷點、標註顯示後只要有任何操作（拖曳球體、點其他按鈕、捲動畫面）都立刻消失（使用者一次回報四點，第一點推翻了上一版「夾住安全高度」的做法，第三點取消了上一版才剛做的「水平拖曳鎖定」功能，兩者都要弄清楚為什麼被取代，見上面詳細說明）。
13. 2026-07-31（第十二次）：球體節點改成依景深（前半球／後半球）分兩層 DOM，中間夾著置中標題，讓「原型×未來」標題看起來真的嵌在球心——前半球的節點蓋在標題上面、後半球的節點被標題蓋住（使用者回報「圓點轉過標題時看不出前後關係」，選擇了工程量較大但效果最好的完整景深排序做法，見上面詳細說明）。
14. 2026-07-31（第十三次）：搜尋選定結果後自動收合手機搜尋列（不再跟地標 icon 疊在一起）；虛線改成三段式——貼地 1cm 固定最短虛線、中段漸進變長、貼機身 1cm 變實線，做出「飛碟降落」的視覺（使用者自己診斷出地標「被切一角」的真正原因是搜尋列還開著、跟地標疊在同一塊區域，見上面詳細說明）。
15. 2026-07-31（第十四次）：手機版頁尾全部資訊（電話/傳真/電郵/地址/社群連結）收進一個 Contact 按鈕，點了才展開（使用者要求減少頁尾佔用的畫面空間，見上面詳細說明）。
16. 2026-07-31（第十五次）：修正手機版球體實際上沒有真的垂直置中的 bug（`height:100%` 在這層巢狀 flex 結構裡靜默解析失敗）；地標 icon 重新加回「不能高於頁首下緣」的限制（使用者回報「主視覺位置偏高」，見上面詳細說明）。
17. 2026-08-02（第十六次）：頁首新增手機/電腦版手動切換鈕，讓使用者可以無視目前畫面寬度強制切換模式（使用者認為單靠畫面寬度判斷不夠精準，見上面詳細說明）。
18. 2026-08-02（第十七次）：修正「切換幾次手機/電腦版模式之後，球體轉動會卡住」的 bug（`_globeElCache` 快取沒有在模式切換時失效）（使用者回報「球轉動會卡住」，見上面詳細說明——這其實是比手動切換鈕更早就存在的既有 bug，只是切換鈕讓它變得非常容易重現）。
19. 2026-08-02（第十八次）：手機版標題字級再縮小一半；「×」改成獨立的 SVG 文字元素、精確釘在球體投影中心，不再依賴「原型」「未來」剛好等寬這種巧合（使用者要求「把X設定為球體的核心」，見下面詳細說明）。
20. 2026-08-02（第十九次）：地標 icon 位置再往下移 0.2cm；新增點擊地標 icon 的按壓縮放＋黃色漣漪動畫，示意點下去會進入該節點的子頁（使用者要求「地標icon往下移動0.2cm 並且增加一個鼠標點擊地標icon的動畫」，見下面詳細說明）。
21. 2026-08-02（第二十次）：修正兩組獨立 Fibonacci sphere 分佈公式必定讓「兩組各自最後一個節點」精確疊在同一個極點的 bug（這次實際疊在一起的是「台南咾咕石芳宅」跟「原型獲獎」）；地標 icon 再往下移 1cm；搜尋標註出現時整顆球體會往下平移，確保 icon 最上緣離頁首下緣至少 2cm（使用者一次回報三點：節點重疊、icon 要再下移、icon 要完整露出畫面，見下面詳細說明）。
22. 2026-08-02（第二十一次）：修正「連續搜尋兩次之後地標 icon 就不會再出現」的 bug（`display:none` 沒有在下一次搜尋時撥回來）；強化點擊漣漪動畫的觸發機制，加上 500ms 防重複觸發 + `animationend` 明確收尾，避免任何形式的重複觸發在畫面上疊成「看起來像自動不斷播放」（使用者一次回報兩點：icon 消失、漣漪疑似自動撥放，見下面詳細說明）。
23. 2026-08-02（第二十二次）：頁首新增「全螢幕播放」按鈕——進入後頁首/頁尾整個隱藏、所有節點不可點擊、搜尋/選單/深淺模式/裝置切換全部不可用，只能拖曳旋轉（含縮放）球體，按 Esc 或畫面右上角一個淡、細線繪製的退出鈕可以離開（使用者要求「無法使用任何功能（跳轉子網頁、搜尋等等）只能轉動球體」，見下面詳細說明）。
24. 2026-08-03（第二十三次）：主視覺（圓環/球體節點文字）新增多語系——頁首（手機、桌機都有，子網頁沒有）新增語言切換鈕；`nodeData` 節點可以有 `translations` 欄位，翻譯由「圓點位置編輯器.html」新增的語言管理／每節點翻譯欄位維護，用既有的複製貼上流程同步過來；順便修掉編輯器既有的一個 bug——有 `mobileName` 的節點匯入時會整個被靜默漏掉（使用者要求「主要針對主網頁的主視覺」新增翻譯功能，見下面詳細說明）。
25. 2026-08-03（第二十四次）：修正「圓點位置編輯器.html」頁首版面跑掉、高度變超高的 bug——新增「語言管理」按鈕時忘了把它加進控制頁首 icon 尺寸的 CSS 選擇器清單，SVG 沒吃到 `width:15px; height:15px`，瀏覽器退回用預設尺寸（約 300×150px）撐爆了整個頁首（使用者回報「頁首的高度超級高」，屬於上一次調整的隨附 bug，見下面詳細說明）。
26. 2026-08-03（第二十五次）：「圓點位置編輯器.html」的新增語言介面從兩個手打文字框改成下拉選單，內建英文／簡體中文／法文／日文／韓文／西班牙文六種語言可以直接選，選單最後一項「＋ 自訂語言…」才會出現原本的代碼/顯示名稱文字框，給內建六種以外的語言用（使用者要求新增語言的地方要改成下拉選單＋內建語言，見下面詳細說明）。
27. 2026-08-03（第二十六次）：修正「圓點位置編輯器.html」節點編輯面板欄位太多、捲不到最下面的 bug——原本只有「連線對象」清單自己會捲動，位置角度／大小／顏色／超連結／翻譯這些欄位固定高度、超出的部分被外層 `overflow:hidden` 直接裁掉；改成把名稱列以外的所有欄位包進同一個可捲動容器，整個面板一起捲，不再只有連線清單那一小段能動（使用者回報「東西太多需要可以捲動的功能，現在無法編輯到太下面的欄位」，見下面詳細說明）。
28. 2026-08-03（第二十七次）：把電腦版 116 個節點標題的六語系翻譯（先前整理成校對表給使用者確認過的版本）直接批次貼進「圓點位置編輯器.html」——`LANGUAGES` 填入英文／簡體中文／法文／日文／韓文／西班牙文六筆，`NODE_DATA` 每個節點都補上對應的 `translations` 物件，不用再一個個手動輸入；之後若要同步到主網頁，走既有的「同步至主網頁」複製貼上流程即可（使用者要求「可以直接幫我把這些翻譯貼進去編輯器嗎」，見下面詳細說明）。
29. 2026-08-03（第二十八次）：使用者在「圓點位置編輯器.html」實際操作後貼回一份新的 `languages`／`specialIds`／`nodeData`／`edgeData` 匯出文字，套用到主網頁跟編輯器的種子資料——刪除 13 個節點（美感教育／孩童／文化／midjourney／桃園市蘆竹國民運動中心…等，經使用者確認為刻意刪除）、新增 1 個節點（`r117`「2024 X-SITE 林木林」）、`specialIds` 新增 `r15`（原型原點）、大量節點的 `href` 换成真正的作品頁網址（不再是籠統的子頁連結）；同時修掉貼入資料裡兩筆網址被複製貼了兩次黏在一起的壞資料（`r56`／`r67`），並把主網頁既有的 `mobileName` 逐一保留，避免被這次沒有 `mobileName` 欄位的匯出資料整批洗掉。現有節點總數從 116 個變成 104 個（使用者要求「把以下資料更新到主網頁的程式碼」，並主動提醒「目前原點編輯器匯出的資料有點亂」，見下面詳細說明）。
30. 2026-08-04（第二十九次）：球心「原型 × 未來」標題也跟著語言切換鈕一起變成多語系——桌機版單一字串、手機版兩個獨立 `<text>` 元素（見第十八次調整），都改吃新的 `getCenterTitle()`。這不是 `nodeData` 節點，不走編輯器同步流程，六語文字直接寫死在主網頁的 `centerTitleTranslations` 裡（跟 `languages` 用同一組 code）（使用者要求「原型未來字樣也要可以多語言版本」，見下面詳細說明）。
31. 2026-08-04（第三十次）：使用者在做完第二十九次調整後要求「改回上一版」，完整撤銷球心標題多語系——刪掉 `centerTitleTranslations` 欄位、`getCenterTitle()` 方法、`renderVals()` 裡新增的 `centerTitle*` 變數，樣板三處字面文字改回寫死的「原型 × 未來」／「原型」／「未來」，回到跟第二十九次調整之前完全一樣的狀態。節點名稱的多語系（`getDisplayName()`／`nodeData[].translations`）不受影響，繼續正常運作（使用者要求「改回上一版」，見下面詳細說明）。
32. 2026-08-04（第三十一次）：重新做一次球心標題多語系，這次加上防溢出——使用者回報「原型X未來字樣的翻譯版 會因為字串的長度而產生超出圓圈或圓球」，要求「自行調整位置 並保持X符號要在中心」。做法不是手動幫每個語言調位置，是加一個 `estimateTitleWidth()` 粗估目前這句翻譯的顯示寬度，超出安全寬度（桌機是標題框寬度、手機是球體半徑的倍數）就把整個標題等比例縮小字級，× 的座標公式（`cx ± 字級×常數`）本來就會跟著字級等比例縮小間距，× 永遠精確落在圓心/球心，不需要另外處理（使用者要求「原型X未來字樣的翻譯版...請自行調整位置 並保持X符號要在中心」，見下面詳細說明）。
33. 2026-08-04（第三十二次）：修正手機版球心標題翻譯後完全不顯示的 bug——使用者回報「手機版中的原型未來消失了」。根本原因分兩層：(1) 手機版標題是 SVG `<text>`，`{{ centerTitleLeft/Right }}` 這種內插值一定會被 DC 模板引擎包一層 HTML `<span class="sc-interp">`（`support.js` `walkText()` 的固定行為，沒有選項可以關），SVG `<text>` 不接受這種非 SVG 命名空間的子元素，包了 span 之後這兩個翻譯字直接不會畫出來——改成模板留空、掛 `data-center-title-left/right`，由新的 `_syncCenterTitleText()` 在 `componentDidMount()`／新增的 `componentDidUpdate()` 直接寫 `textContent`，繞開這個限制；(2) 就算文字有畫出來，手機球體把標題夾在「背面節點層」跟「正面節點層」中間、讓標題看起來嵌在球心，但背面連線／節點透出來、加上偶爾轉到中央的正面節點，仍然常把標題整個蓋住——加一個用頁面背景色（跟深色模式一起變）往外羽化的放射狀漸層背光暈墊在標題後面，並把整組「背光暈＋標題」搬到全部節點畫完之後（球體 SVG 最上層），不管轉到哪個角度都保證看得到，代價只是球心附近一小圈範圍內的正面節點會被蓋住——這一小圈本來節點也擠在一起看不清楚，犧牲換可讀性划算（使用者回報「手機版中的原型未來消失了」，見下面詳細說明）。
34. 2026-08-04（第三十三次）：撤銷上一次「標題搬到最上層」的做法，改回夾在背面/正面節點層中間——使用者體驗後回報「現在看不出來原型未來是在球內 反而會擋住正面移動經過的圓點 希望能跟本來很明顯在球核效果」。標題搬到最上層雖然解決了「常被蓋住看不見」，但代價是正面節點永遠無法蓋過標題，等於整個推翻第十二次調整刻意做的「正面節點蓋過標題、標題蓋過背面節點」景深效果，使用者這次體驗後認為「嵌在球心」的立體感比「標題隨時 100% 可讀」更重要，兩者衝突時選前者。做法：只把標題（含背光暈、`data-center-title-left/right` 那份 imperative textContent 寫入邏輯）這一整塊 `sc-if` 從「全部節點畫完之後」搬回原本「背面節點層／正面節點層中間」的位置，SVG `<text>` 包 span 導致文字消失那個 bug 的修法（改成 JS 寫 `textContent`）維持不變、繼續有效；背光暈也維持不變，還是能擦掉背面連線/節點透出來的雜訊，只是正面節點現在會照常蓋在光暈跟標題上面（使用者要求「希望能跟本來恨明顯在球核效果」，見下面詳細說明）。
35. 2026-08-04（第三十四次）：手機版標題排版邏輯改成「中文/簡體中文維持 × 精確釘在球心，其他翻譯語言改成整段文字的視覺中點釘在球心」——使用者說明「原本的設定是X字樣固定在中心，但現在調整為除了中文之外(包括簡中)，整段文字(原型X未來)的翻譯版本的水平方向中點要固定在球的圓心或核心」。原因：`titleLeftX`/`titleRightX` 是用固定間距（字級 × 0.6）左右對稱展開，中文「原型」「未來」都剛好 2 個字、天生對稱，× 釘中心＝整段文字視覺中點也在中心；但翻譯後左右兩個字長度常常差很多（英文「Prototype」9 個字母 vs.「Future」6 個字母），× 還是死釘在正中央的話，整段文字的視覺中點會跟著往短的那邊偏，肉眼看起來整組字沒有置中。做法：非中文/簡中語言改成先用 `estimateTitleWidth()` 估出左右兩個字目前字級下的寬度差，把 ×（連同左右文字、背光暈）一起往寬度較短那側位移差值的一半，讓整段文字的視覺中點對齊球心，× 本身的座標不再固定等於球心，而是跟著一起被推算出來（使用者要求「整段文字(原型X未來)的翻譯版本的水平方向中點要固定在球的圓心或核心」，見下面詳細說明）。
36. 2026-08-17（第三十五次）：把使用者在「圓點位置編輯器.html」裡進一步調整、並已上線（`D:\2026as實習\網頁\站存.txt` 存檔）的節點內容/位置同步回主網頁與編輯器的種子資料——使用者要求「同步一下目前主網頁的內容」，實際意思是「把我在編輯器裡改完、部署上線的結果同步回這裡的程式碼」。跟主網頁原本（第二十八次調整後）的 104 節點版本比對，`站存.txt` 少了 26 個節點（`r10`／`r12`／`r14`／`r31`／`r34`／`r42`／`r45`～`r57` 一整批「○○競圖獲勝／台灣建築獎／ADA新銳建築獎／德國IF設計獎」／`r63`／`r65`／`r69`／`r77`／`r85`／`r90`／`r102`／`r103`），先用 `AskUserQuestion` 確認是使用者在編輯器裡刻意刪除、不是舊備份少存，確認後才套用；同時 `specialIds` 拿掉 `r11`／`r22`，`r1`（章 architecture design）／`r2`（原型教學）從陣列最前面搬到最後面（節點順序會直接決定球面/圓環上的排列位置，見上面「已知的資料相依細節」），`edgeData` 的連線方向大幅改成「分類大標 → 子節點」（例如 `r115→r30...r58`，原本是 `r30...r58→r115`）。套用方式是逐段 `Edit` 精確比對 `站存.txt` 與主網頁目前內容、只動有差異的節點/連線，刻意不整段覆蓋，這樣才能保留主網頁後來手動修過、但 `站存.txt`（較早存的備份）還沒跟上的欄位——目前發現的唯一一處是 `r109`（原型建築）的 `href`，主網頁已經是正式網域 `https://www.as-structure.com/...`，`站存.txt` 裡還是舊的 `github.io` 連結，這次同步刻意保留主網頁較新的網址、沒有跟著 `站存.txt` 退回舊連結。編輯器（`圓點位置編輯器.html`）的 `NODE_DATA`／`EDGE_DATA`／`SPECIAL_IDS` 改用程式腳本、以主網頁同步後的節點順序為準，逐一比對編輯器原有的每筆節點資料重新排序/篩選（保留編輯器自己既有的欄位差異，例如編輯器沒有 `mobileName`、`r23` 的 `href` 跟主網頁不同，這次都沒有被覆蓋掉），確保編輯器下次開啟不會又回退成舊的 104 節點版本（使用者要求「同步一下目前主網頁的內容」，見下面詳細說明）。
37. 2026-08-17（第三十六次）：使用者再次更新 `站存.txt`（同一份編輯器上線存檔又繼續改過），把差異同步過來——4 個分類大標節點（`r1` 章 architecture design、`r28` 成 construction、`r59` 法 structure data、`r100` 構technics）的 `href` 從舊的通用連結 `https://www.mashup.com.tw/as%20studio/?page=about` 改成新建立的本機子頁 `./原型方法.dc.html`（該檔案已存在於同目錄下，確認不是筆誤）。這次比對出的差異很小，只有這 4 筆 `href`（外加純格式差異：`站存.txt` 這次貼出來時 `languages`/`nodeData`/`edgeData` 開頭少了換行，跟上一版比是純空白差異、不影響內容，未套用）；`r109` 的 `href` 這次 `站存.txt` 裡仍是舊的 `github.io` 連結，比照上一次的判斷繼續保留主網頁較新的 `as-structure.com` 網址、不覆蓋。同步方式跟上一次一樣先跑一次完整檔案 diff 找出實際差異再逐筆套用，不整段覆蓋。編輯器（`圓點位置編輯器.html`）的這 4 筆節點也同步改成 `./原型方法.dc.html`，維持跟主網頁一致（編輯器本來就偏好本機子頁而非外部 `mashup.com.tw` 連結，例如 `r23` 已經是這樣，這次是同一個方向）（使用者要求「同步這個程式碼」，見下面詳細說明）。
38. 2026-08-17（第三十七次）：「原型建築」節點（`r109`）改用專屬的循環版前導動畫——使用者要求「在跳轉到原型建築子網頁時也加入進入主網頁的動畫 因為這個子網頁檔案比較大 這個動畫需要重複撥放直至子網頁檔案加載完成後再進入網頁」。`r109` 的 `href` 是外部網址（`https://www.as-structure.com/原型建築`，不是本站內的 `.dc.html`），原本既有的前導動畫（`WORD_POOL` 跳字，`playAndNavigate()`）的點擊判斷式會直接排除 `https://` 開頭的連結，所以這個節點點下去完全沒有動畫、也不等待目的頁載入，就是瀏覽器原生的立即換頁。新增 `playAndPreloadThenNavigate()`：跳字改成無限循環（不是播完固定字數就停），背景用一個隱藏 `<iframe>` 載入目的頁、以 `load` 事件當作「目的頁載入完成」的信號（跨網域一樣讀得到這個事件本身，只是讀不到內容，足夠判斷載入完成），完成後才停在目的地文字、停留一下、才真正 `window.location.href` 跳轉；另外比照開場影片遮罩既有的逾時保險模式（`setTimeout(reveal, 15000)`）加一個 20 秒的安全逾時，避免目的網站擋 `iframe` 嵌入（`X-Frame-Options`/CSP `frame-ancestors`）導致 `load` 事件永遠不觸發、使用者卡在跳字動畫出不去。點擊判斷式改成用節點 id（`EXTERNAL_PRELOAD_NODE_IDS = ['r109']`）決定要不要走這條新路徑，而不是寫死比對網址字串，這樣以後 `r109` 的 `href` 再被同步/修改也不用跟著改這裡的程式碼；其他所有本機 `.dc.html` 子頁的點擊行為完全不受影響，繼續用原本的 `playAndNavigate()`（使用者要求「在跳轉到原型建築子網頁時也加入進入主網頁的動畫...這個動畫需要重複撥放直至子網頁檔案加載完成後再進入網頁」，見下面詳細說明）。
39. 2026-08-18（第三十八次）：`站存.txt` 再次更新，新增 17 個節點（官網作品目錄裡本來缺漏的案子），並修正了整段同步流程本身的一個高風險 bug——使用者要求「同步程式碼」，過程中意外把主網頁檔案寫壞過一次，即時用 git 復原，詳見下面詳細說明。內容面：新增 `r118`～`r134` 共 17 個節點（嘉義城市博覽會生態牆：重生、台中馬禮遜美國學校、見晴竹韻、招霧亭、遠雄永續接待中心、漂浮會議室、【旭日東昇】顏水龍壁畫檢測及結構補強、石壁竹創森二期－杉嵐景觀台、鹽埔漁港公廁、2026舊屋力永續創新提案競賽、夢湖湧泉、新竹圖書館競圖、原型結構事務所、石壁竹創森二期－圓竹、石壁竹創森二期－竹虹、竹行動展覽館、專題），節點總數從 78 變成 95；`r108`／`r116`／`r109`／`r110`／`r111`／`r112`／`r113`／`r114`／`r1`／`r2` 這個區塊從陣列尾端搬回最前面（使用者在編輯器裡調整過位置，節點順序會直接決定球面/圓環排列，見上面「已知的資料相依細節」）；`r109` 的 `href` 這次 `站存.txt` 仍是舊的 `github.io` 連結，比照前兩次的判斷繼續保留主網頁較新的 `as-structure.com` 網址、不覆蓋。過程中 `站存.txt` 在同一個對話回合裡被使用者又更新了一次（使用者傳訊息「有更新了」），所以整套同步流程（含編輯器）實際跑了兩輪，第二輪抓到的才是最終版本（使用者要求「同步程式碼」，見下面詳細說明）。
40. 2026-08-18（第三十九次）：`站存.txt` 第三次更新，新增 1 個節點——使用者要求「更新程式碼」。新增 `r135`「信義街咾咕石‧芳宅」（台南案子，跟既有 `r114`「2025台南咾咕石芳宅」名稱相近但是獨立的新節點、不是覆蓋既有節點，照使用者在 `站存.txt` 裡給的資料原樣同步，不擅自判斷合併），節點總數從 95 變成 96；`edgeData` 新增 5 筆對應連線。同步流程沿用上一次修好的腳本（bracket 深度掃描抓陣列邊界＋暫存檔驗證再覆蓋），主網頁與編輯器都已更新，Playwright 確認桌機/手機皆正確渲染 96 個節點、console 無錯誤（使用者要求「更新程式碼」，見下面詳細說明）。
41. 2026-08-18（第四十次）：全站「字卡輪播」前導動畫拿掉，改用開場影片——使用者先要求「把所有進入子網頁的字卡輪播動畫都換成進入主網頁的logo動畫」，稍後追加「要拿掉那個字卡動畫」把僅存的「原型建築」節點也一併改掉。分兩階段：
    - **第一階段**（本機子頁）：首頁點本機 `.dc.html` 節點不再攔截、不播 `WORD_POOL` 閃字，直接原生導頁，只在點擊當下設 `sessionStorage` 旗標 `scTransitionIn`；5 個子頁（原型教學／原型原點／原型方法／原型結構／原型獲獎）的到達前導動畫從黑底白字的 `ARRIVAL_WORD` 文字滑開，改成播放跟首頁自己第一次進站同一支開場影片（`uploads/網頁前導片.mp4`，`#sc-arrival-overlay`／`#sc-arrival-video`），播完/錯誤/15 秒逾時任一觸發就淡出移除。5 個子頁這段程式碼現在完全一致，不再有 `ARRIVAL_WORD` 那種每頁要改的專屬變數。`playAndNavigate()` 函式整個移除。
    - **第二階段**（`r109`「原型建築」外部連結節點）：使用者確認過範圍後，把僅存的跳字動畫也拿掉。原本的 `playAndPreloadThenNavigate()`（跳字無限循環＋隱藏 iframe 預載＋20 秒逾時保險，靠 iframe `load` 事件判斷外部頁面是否真的載入完成）整個移除，改成 `playIntroVideoThenNavigate()`：在**首頁**（不是子頁）先播一次同一支開場影片，播完/錯誤/15 秒逾時任一觸發才 `window.location.href` 跳轉到外部網址。**跟本機子頁的關鍵差異**：本機子頁是「子頁自己到達後」播，`r109` 是「首頁還沒離開前」播——因為外部頁面不是我們能控制的內容，沒辦法讓它配合播我們的影片，也因此**這條路徑不再能真的等外部頁面載入完成才跳轉**，跳轉時機固定跟著影片播放狀態走，這是使用者確認過可以接受的取捨（見對話裡「改播同一支開場影片」的選擇）。
    - `WORD_POOL` 陣列本身已經從程式碼整個刪除（不只是沒人呼叫，是真的沒有這個變數了）。地標 icon（`ensureLandmark()`）裡藏著給轉場腳本讀取目的地文字用的 `<div>` 現在完全沒有作用（兩條路徑都不再顯示任何文字），留著不影響功能。
    - **驗證方式**：確認 `建築事務所首頁.dc.html` 大括號/小括號數量跟修改前的差值一致（沒有多出或少掉未閉合的區塊）、`grep` 確認 `WORD_POOL`／`playAndNavigate`／`playAndPreloadThenNavigate`／`ARRIVAL_WORD` 在所有 `.dc.html` 檔案裡都沒有殘留引用；5 個子頁的前導動畫區塊逐字比對確認完全一致。實際點擊驗收項目：從首頁點任一本機子頁節點，確認直接跳轉、子頁到達後播放開場影片；點「原型建築」節點，確認先在首頁播放開場影片、播完才跳到外部網址；兩種情況瀏覽器「上一頁」都要正常，不能卡在遮罩上。
42. 2026-08-19（第四十一次）：「原型建築」節點的開場影片也拿掉——使用者確認「現在是只要跳轉到子網頁都會撥放跳轉動畫嗎」的行為後，要求「原型建築不要播開場影片」。做法很單純：把 `EXTERNAL_PRELOAD_NODE_IDS` 陣列裡的 `'r109'` 拿掉、改成空陣列 `[]`，`playIntroVideoThenNavigate()` 這整套機制（第四十次調整新增）沒有刪除，只是目前沒有任何節點在用——點擊判斷式（`isExternalPreload` 一定是 `false`、`isLocalSubpage` 也是 `false`，因為 `r109` 的 `href` 是外部網址）會直接 `return`、完全不攔截，瀏覽器原生跳轉，效果跟其他所有外部案例連結（`mashup.com.tw` 個案頁、YouTube、wixsite、github.io 等）完全一致，不會有任何動畫。以後如果又想讓某個外部連結節點套用「先播開場影片再跳轉」的效果，把它的節點 id 加回 `EXTERNAL_PRELOAD_NODE_IDS` 即可，不用重寫邏輯（使用者要求「原型建築不要播開場影片」，見上面「PAGE TRANSITION PRELOADER」程式碼註解）。
43. 2026-08-19（第四十二次）：`站存.txt` 第四次更新，同步過來——這次沒有新增/刪除節點或連線（節點數維持 96、連線數維持 163），差異只有兩類：(1) 3 個節點的中文顯示名稱改了用字，`r105`「2025成大建築×信義街咾咕石黃宅展」→「2025成大建築×台南硓𥑮石．芳宅展」、`r107`「2025ADA新銳建築獎×信義街咾咕石黃宅」→「2025ADA新銳建築獎×台南硓𥑮石．芳宅」、`r114`「2025台南咾咕石芳宅」→「2025台南硓𥑮石．芳宅」——三筆都只改了 `name` 欄位本身，`mobileName`／`href`／`translations` 全部維持不變；(2) `specialIds` 陣列重新排序（成員完全相同，只是陣列字面順序不同，`圓點位置編輯器.html` 那邊 `SPECIAL_IDS` 是 `Set`、不受順序影響，不用跟著改）。因為這次差異範圍很小、不涉及節點增減或陣列結構變動，沒有動用整段陣列同步腳本（`extractArrayLiteral`／`splitTopLevelItems`／`.NEWSYNC.tmp` 那套，見第三十八次調整），改用針對性的 `Edit` 精確比對後逐筆套用到主網頁與編輯器；套用後重新跑一次逐節點內容比對腳本確認 `站存.txt` 與主網頁的 `nodeData`／`edgeData`／`specialIds` 完全一致（節點數、連線數、每個節點的 id 順序、每個節點的完整內容、每筆連線都逐一核對過，沒有殘留差異），Playwright 確認桌機版渲染出 96 個節點、`r114` 的顯示文字已更新成新名稱、console 無錯誤（使用者要求「更新程式碼」，見上面說明）。
44. 2026-08-19（第四十三次）：`站存.txt` 第五次更新，緊接著第四十二次調整、補上同一波「信義街咾咕石」→「台南硓𥑮石．芳宅」用字統一漏掉的最後一筆——`r135`「信義街咾咕石‧芳宅」→「台南硓𥑮石．芳宅」，同樣只改 `name` 欄位，`href`／`translations` 不變。節點數/連線數/`specialIds` 都沒有變動，是純粹的單一欄位補漏。做法沿用上一次的針對性 `Edit`＋逐節點內容比對腳本驗證，套用後 `站存.txt` 與主網頁的 `nodeData`／`edgeData`／`specialIds` 完全一致，Playwright 確認 `r135` 顯示文字已更新、桌機版 96 個節點、console 無錯誤（使用者要求「同步」，見上面說明）。
45. 2026-08-19（第四十四次）：`站存.txt` 第六次更新，同一波用字統一又補上最後一筆——`r108`「2025台南咾咕石×芳宅×原型未來展」→「2025台南硓𥑮石．芳宅×原型未來展」，同樣只改 `name` 欄位，`mobileName`／`href`／`translations` 都不變。至此 `r105`／`r107`／`r108`／`r114`／`r135` 這五個節點名稱裡的「信義街咾咕石」／「咾咕石」都已經統一改成「台南硓𥑮石．芳宅」這個新用字。節點數/連線數/`specialIds` 都沒有變動。做法沿用同一套針對性 `Edit`＋逐節點內容比對腳本驗證，套用後 `站存.txt` 與主網頁的 `nodeData`／`edgeData`／`specialIds` 完全一致，Playwright 確認 `r108` 顯示文字已更新、桌機版 96 個節點、console 無錯誤（使用者要求「同步」，見上面說明）。
46. 2026-08-19（第四十五次）：`站存.txt` 第七次更新，補上 2 個節點的網址——`r116`「2025好感空間展」的 `href` 從佔位用的 `#` 換成真正的網址 `https://www.tnhs.com.tw/`；`r101`「原型展覽」的 `href` 從 `https://e64872170.wixsite.com/mysite/...` 換成 `https://as-studio001.github.io/asarch/`（這個網域先前是 `r109`「原型建築」用過的舊網址，`r109` 換成正式網域 `as-structure.com` 之後這個舊網址被重新指派給 `r101` 用，`r109` 這次在 `站存.txt` 裡的 `href` 仍是正確的 `as-structure.com`、沒有跟著退回舊網址，不受影響）。這次同步有個要注意的地方：使用者這次貼存的 `站存.txt` 裡有兩份完整頁面內容前後黏在一起（`grep` 確認出現兩次 `<!DOCTYPE html>`／`nodeData = [`），研判是存檔時不小心貼了兩次；比對後確認檔案裡排在前面的第一份，其節點資料（含 `r109` 已經是正確的新網址）比排在後面的第二份更新，改用第一份當作這次同步的依據——同步腳本／比對邏輯本來就是用 `indexOf`／`match` 找「第一個」符合的 `nodeData = [`／`edgeData = [`，天生就會抓到前面這份，不需要額外處理，但這個「一份存檔裡混進兩份內容」的情況值得記錄，往後如果又出現同樣狀況，先確認排在前面的是不是真的比較新，不要預設一定是這樣。節點數/連線數/`specialIds` 都沒有變動，只有這 2 筆 `href`。套用後逐節點比對確認 `站存.txt`（第一份）與主網頁完全一致，Playwright 確認兩個節點的連結網址都已更新、桌機版 96 個節點、console 無錯誤（使用者要求「再同步一次」，見上面說明）。
47. 2026-08-19（第四十六次）：使用者直接指定「原型建築要插入這個連結 `https://as-studio001.github.io/asarch/`」（跟前一次同步時保留的 `as-structure.com` 相反，是明確的新指示，覆蓋掉之前「猜 `站存.txt` 的舊網址是過時資料」那個假設），把 `r109`「原型建築」的 `href` 從 `https://www.as-structure.com/%E5%8E%9F%E5%9E%8B%E5%BB%BA%E7%AF%89` 改成 `https://as-studio001.github.io/asarch/`；同時確認 `r116`「好感空間展」的 `https://www.tnhs.com.tw/` 已經是上一次同步時設好的，不用再改。三個檔案（`建築事務所首頁.dc.html`、`index.html`、`圓點位置編輯器.html`）都同步套用，git commit 後推送到 `origin/main`（使用者要求「原型建築要插入這個連結...插入後推送」，見「PAGE TRANSITION PRELOADER」程式碼註解，該區塊的說明文字目前還停留在舊網址時期，內容本身沒有需要改的邏輯，純粹是節點資料的 `href` 欄位更新）。
48. **2026-08-19（第四十七次，目前狀態）：發現並修正 `index.html`（GitHub Pages 實際 serve 的網站根目錄檔案，見前面「⚠️ `index.html` 不是轉址頁」那節說明）整份頁面內容被貼了兩次黏在一起、且第一份結尾的 `</script></body></html>` 遺失（被搬到整個檔案最後面）的嚴重 bug，造成瀏覽器把第二份文件的開頭當成還沒結束的 JavaScript 解析、丟出 `Unexpected token '<'` 語法錯誤，連帶讓首頁自己的開場影片遮罩（`#sc-intro-overlay`）「播完/逾時後淡出移除」的邏輯完全沒有執行到——使用者回報「播完動畫就卡住了」。**根本原因細節**：這個檔案透過 GitHub 帳號 `as-studio001` 的另一條發布路徑直接推上 `origin/main`（不是這個維護流程平常在用的推送方式），而且已經在同一天內至少發生過兩次「整份內容被貼兩次」的資料損毀（第一次是 `站存.txt` 存檔，這次是實際上線的 `index.html`，兩者手法一模一樣：完整文件內容 A + 完整文件內容 B 直接前後黏接、A 的結尾標籤消失），高度懷疑是使用者在用的那個發布/存檔工具本身有系統性問題，不是單一次意外。**排查過程**：用 `curl` 直接把 `https://www.as-structure.com/` 抓下來（確認 Bash 環境其實有對外網路，跟先前 session 認定「沙盒環境沒有網路」的假設不同，這點對之後任何需要抓外部真實網頁內容的排查都適用）、比對本機 git 版本的 `index.html`（乾淨、沒有重複）發現兩者完全不同，證實 `index.html` 是透過另一條管道發布、不是單純 `git push` 這個 repo 就會同步——直到用 `git fetch` 才發現 `origin/main` 其實已經超前本機好幾個 commit（`as-studio001` 帳號多次直接 "Update index.html"），重新抓下來後才發現「壞掉的重複內容」其實已經是 `origin/main` 上的最新狀態，也就是說我確實可以透過正常的 git commit + push 修好它，不需要找其他發布管道。用 Playwright 實際重現整套「症狀」（真的把抓下來的壞檔案跑起來，不是憑空推理）：DOM 裡真的有兩個 `#sc-intro-overlay`／兩個 `#sc-intro-video`，`document.getElementById` 只抓得到文件順序中第一個（也就是版面上真正看得見、有正確定位的那個），但因為語法錯誤，原本該幫這個元素綁 `ended`/`error`/15 秒逾時的那段程式碼完全沒跑到；反而是第二份（重複貼上、格式錯亂但語法本身沒錯）裡的那個 `<script>` 順利執行，抓到的卻是文件順序中第一個影片元素——結果變成：真正顯示的影片從頭到尾停在 `currentTime:0` 沒有播放、遮罩永遠不會淡出，另一份看不見的影片自己默默播完。**修法**：找到兩份內容的交界點（`})();<!DOCTYPE html>` 這個沒有分隔的接合處），只保留第一份、把正確的 `</script></body></html>` 補回去，整段刪除重複的第二份。**驗證方式**：Playwright 用真正的首次造訪流程（不繞過開場影片）分別測試修復前/修復後的檔案——修復前：`#sc-intro-overlay`/`#sc-intro-video` 各 2 個、console 有 `Unexpected token '<'`、遮罩在 20 秒後仍卡在 `opacity:1`；修復後：各 1 個、零錯誤、遮罩在影片實際長度（約 2 秒）後乾淨消失、後續 96 個節點正常渲染。修復後的 `index.html` commit 並推送到 `origin/main`，GitHub Pages 通常一兩分鐘內會部署完成。**這不是本次 `站存.txt` 同步或任何其他這個 session 裡對 `建築事務所首頁.dc.html` 的修改造成的**——確認過本次修復前 `index.html` 在本機完全沒有任何未提交的修改，問題完全來自 `as-studio001` 帳號那幾次 "Update index.html" commit，值得檢查該發布工具本身（使用者回報「為甚麼主網頁現在壞掉了 播完動畫就卡住了」，見上面「PAGE TRANSITION PRELOADER」程式碼註解）。
49. 2026-08-19（第四十八次）：`站存.txt` 第八次更新，同步過來——新增 1 個節點 `r136`「台東舊站櫥窗計畫」，節點總數從 96 變成 97；`edgeData` 新增 3 筆連線（`r23->r136`／`r24->r136`／`r24->r79`）。另外 3 筆既有節點的變動：(1) `r104`「2024第七屆ADA新銳建築獎展覽」從陣列最尾端搬到最前面（使用者在編輯器裡調整過順序，節點順序會直接決定球面/圓環排列位置，見上面「已知的資料相依細節」）；(2) `r112`「南寧老屋」的 `href` 從佔位用的 `#` 換成 `https://as-studio001.github.io/asarch/`（第三個指到這個網域的節點，跟 `r101`／`r109` 同一個網址）；(3) `r79` 整個內容被換掉——`name` 從「2020 柳營車站」變成「獅甲非營利幼兒園」、`href` 從 `#` 換成真正的 mashup.com.tw 連結、`translations` 也全部換成新內容，但 `mobileName` 欄位維持原樣還是「柳營車站」沒有跟著改（研判是使用者在編輯器裡把這個原本只有佔位資料的節點換成一個新案子、`mobileName` 欄位單純沒有一起更新，照 `站存.txt` 原樣同步、不擅自幫忙補上新的 `mobileName`）。因為這次牽動的節點順序變動幅度較大（`r104` 整個從尾端搬到最前面，等於後面所有節點索引都要跟著平移），用回第三十八次調整建立的整段陣列同步腳本（`extractArrayLiteral`／`splitTopLevelItems`／`.NEWSYNC.tmp` dry-run 驗證再覆蓋），而不是逐筆 `Edit`。**這次額外多同步了 `index.html`**（第四十七次調整才確認這是實際上線的檔案，這次起沿用同一套同步腳本、只是輸出目標換成 `index.html`，同時驗證過修復好的「單一份開場影片遮罩」沒有被這次同步重新弄壞），三個檔案（`建築事務所首頁.dc.html`、`index.html`、`圓點位置編輯器.html`）都已同步、逐節點比對確認跟 `站存.txt` 完全一致（97 節點、166 連線），Playwright 驗證兩個線上檔案渲染出的節點數、`r136`/`r112`/`r79` 的顯示內容都正確、console 無錯誤，commit 後推送到 `origin/main`（使用者要求「同步這個檔案的程式碼 同步之後推送」，見上面說明）。
50. 2026-08-19（第四十九次）：`站存.txt` 第九次更新，同步過來——這次差異只有 1 筆：`r93`「大阪世博TW館」的年份從「2023大阪世博TW館」改成「2025大阪世博TW館」，只改 `name` 欄位，`mobileName`／`href`／`translations` 都不變（`translations` 裡的年份文字目前仍是 2023，`站存.txt` 本身沒有一併更新，照原樣同步、不擅自幫忙改）。節點數/連線數/`specialIds` 都沒有變動，用針對性 `Edit` 直接套用到 `建築事務所首頁.dc.html`、`index.html`、`圓點位置編輯器.html` 三個檔案，逐節點比對確認跟 `站存.txt` 完全一致（97 節點、166 連線），Playwright 驗證兩個線上檔案渲染出的 `r93` 顯示文字已更新、console 無錯誤，commit 後推送到 `origin/main`（使用者要求「同步主網頁程式碼 並推送」，見上面說明）。
51. 2026-08-19（第五十次）：修正手機版地標 icon 點不進去子頁的 bug——使用者回報「手機版的搜尋系統好像壞掉了 無法點擊地標icon進入子網頁」，且明確是在正式網址 `www.as-structure.com` 上、真機測試「完全沒反應，點下去什麼都不發生」。**根本原因**：`_onDocMouseDown`（文件層級的 `mousedown` 監聽器）原本只排除「點在搜尋框 `[data-search-box]` 裡面」的情況，沒有排除「點在地標 icon（`_pinLink`）本身」——手指一碰到 icon，`mousedown` 就先觸發這裡的 `dismissFlash()`；`dismissFlash()` 把 `this._dismissing` 設成 `true`，緊接著下一個 `requestAnimationFrame`（`tick()` 裡的 `showPin` 分支）就會判定 `!this._dismissing` 為否，把 icon 的 `pointer-events` 設成 `none`。真機上 `click` 事件通常會比 `mousedown`晚一點點才觸發，這時候 icon 已經變成點不到，導航就直接被吃掉。**排查過程**：一開始用 Playwright 的 `page.click()`/`page.tap()` 完全重現不出來（見上面「排查過程」的說明）——後來才想到這兩個方法是同步連續派發完 `mousedown`→`click`，中間沒有真實觸控裝置那種讓 `requestAnimationFrame` 插進來的空檔；改用 `page.mouse.down()`＋等待一小段時間（模擬真機 mousedown 到 click 之間的真實延遲）＋`page.mouse.up()`，才第一次重現「點了完全沒反應」的症狀，順便量到 icon 的 `pointer-events` 在等待期間確實被設成了 `none`、`opacity` 變成 `0`——而點擊畫面上其他空白處，`dismissFlash()` 確實正常觸發（`opacity` 一樣會變 0），證實問題精確出在「點擊目標剛好是 icon 自己」這個特例沒被排除，不是整個 flash/dismiss 機制壞掉。**修法**：`_onDocMouseDown` 新增 `inPin` 判斷（`this._pinLink && e.target && this._pinLink.contains(e.target)`），跟既有的 `inBox` 一起排除在「觸發 dismissFlash()」的條件之外——點擊 icon 之後本來就會原生導頁離開這一頁，不需要在 `mousedown` 當下就急著淡出它；點擊其他任何地方（含空白處、globe 拖曳）仍然照舊會立刻 dismiss，行為不變。桌機完全不受影響（`_pinLink` 只在手機版的 `goToNode()` 分支才會建立，桌機該變數恆為 `null`，`inPin` 恆為 `false`）。三個檔案（`建築事務所首頁.dc.html`、`index.html`、`圓點位置編輯器.html`——編輯器沒有這套地標系統，不用改）都已同步套用。**驗證方式**：用同一支「`mouse.down()` + 等待 + `mouse.up()`」的腳本先對修復前的程式碼重現 bug（確認 icon `pointer-events` 變 `none`、URL 沒變），再對修復後的程式碼跑同一支腳本確認 icon `pointer-events` 全程維持 `auto`、`opacity` 維持 `1`、URL 成功變成子頁網址；額外確認點擊畫面空白處仍然會讓 icon `display:none`（既有的「點外面消失」行為沒有被連帶破壞）；桌機（1440px）分別開兩個檔案確認 97 個節點正常渲染、console 無錯誤（使用者回報「手機版的搜尋系統好像壞掉了 無法點擊地標icon進入子網頁」，見上面「地標 icon『被切一角』的真正原因」附近的說明）。
52. 2026-08-19（第五十一次）：淺色模式下「一般節點」跟「特殊節點（分類大標）」的顏色對調——使用者要求「主網頁的淺色模式的一般色跟特殊色目前是黑色/灰色，我想要顛倒過來，連同編輯器的設定也要一起改」。改之前：淺色模式下一般節點（圓點＋文字）是黑色（`dotColor`／`textColor` 都是 `oklch(0.16 0 0)`），特殊節點是灰色（`specialColor` 預設 `#9B9B9B`，只有滑鼠懸停在該節點本身時才會變黑 `#000000`）；改之後：一般節點變灰色、特殊節點變黑色，兩者對調。**做法**：`dotColor` 的淺色模式分支從 `'oklch(0.16 0 0)'` 改成 `'#9B9B9B'`；`specialColor` 的淺色模式「預設」分支從 `'#9B9B9B'` 改成 `'oklch(0.16 0 0)'`（懸停變黑 `#000000` 那個分支沒有動，只是因為新的預設值已經很接近純黑，懸停時視覺上幾乎看不出額外變化，這是對調後的自然結果、不是需要另外修的問題）；連帶把 `labelColor`（一般節點的文字顏色）從原本讀 `textColor`（跟頁面其他文字共用的全域顏色，例如頁首導覽、頁尾文字都在用，不能動）改成讀 `dotColor`——這樣一般節點的「圓點＋文字」才會是同一個顏色、跟特殊節點原本「圓點＋文字共用 `specialColor`」的作法一致，不是只改了圓點、文字顏色沒跟著換。桌機 2D 圓環、手機 3D 球體兩處重複的顏色計算（`renderVals()` 裡兩個各自獨立的 `nodes.map()` 區塊）都同步改了；深色模式的分支（`isDarkMode ? ...`）完全沒有動，維持原本白色一般節點／金色特殊節點的配色。編輯器（`圓點位置編輯器.html`）用的是 CSS 變數（`--dot`／`--special`），不是 JS 三元運算式，做法對應：`:root` 預設區塊跟 `:root[data-theme="light"]` 顯式淺色區塊（使用者手動切換主題用的那個，跟系統 `prefers-color-scheme` 那組分開宣告，見檔案開頭的說明）這兩處的 `--dot: #1a1a1a;`／`--special: #808080;` 對調成 `--dot: #808080;`／`--special: #1a1a1a;`；連帶把節點文字 `label.setAttribute('fill', isSpecial ? 'var(--special)' : 'var(--text)')` 改成讀 `var(--dot)`，跟主網頁同一個道理，兩個重複出現的位置（初次渲染 `renderNode()`、`setNodeColor()` 切換顏色時）都改了。深色模式的兩個區塊（`@media (prefers-color-scheme: dark)` 跟 `:root[data-theme="dark"]`）完全沒動。**另外一件事需要記錄**：這次使用者明確要求「網頁程式碼請從github asstudiowebsite index.html抓取」，所以 `index.html` 這次不是拿本機既有的版本改，是先用 `curl` 直接把 `https://raw.githubusercontent.com/as-studio001/asstudiowebsite/main/index.html` 抓下來當作這次編輯的底稿——抓下來後發現這份「真正上線」的內容，跟我方才推送過去的版本已經又不一樣了：`nodeData`／`edgeData` 陣列被 `as-studio001` 帳號的發布工具重新用「整段擠成一行」的格式覆蓋過一次（第三十八次調整記錄過的同一種存檔格式特徵），而且這次的版本**少了 `r93`「大阪世博TW館」這個節點跟它的 2 條連線**（比對後確認純粹是這次外部覆蓋漏帶的，不是使用者要求刪除）。因為使用者明確指定要以 GitHub 上的 `index.html` 當底稿，這次就照做、直接在這份（缺 `r93`）的內容上套用顏色改動，**沒有**額外把 `r93`補回去——如果之後又要跑一次 `站存.txt` 同步，這個缺口會在那時候自然被下一次同步補上；如果使用者希望現在就補，需要另外说一聲。`建築事務所首頁.dc.html`（本機這份完整含 `r93`）套用同一組顏色改動，`r93` 節點本身沒有受影響。**驗證方式**：Playwright 分別開 `建築事務所首頁.dc.html`／`index.html`，確認淺色模式下 `r23`（特殊節點）圓點填色是 `oklch(0.16 0 0)`、`r105`（一般節點）是 `#9B9B9B`；切到深色模式確認 `r23` 變回 `#BDA434`、`r105` 變回 `oklch(0.95 0 0)`，兩個檔案結果一致；編輯器直接讀 `getComputedStyle` 確認 `--dot`／`--special` 兩個 CSS 變數的淺色模式計算值也對調成功；全程 console 無錯誤（使用者要求「修改後推送」，見上面說明）。

**⚠️ push 前又發現一件更大的事，記在這裡供以後參考**：commit 完準備推送時 `git fetch` 發現 `origin/main` 一口氣多了 8 個 commit，其中 `0b3c580 "Update homepage 建築事務所首頁.dc.html via 圓點位置編輯器"` 這個 commit message 明確寫出——`建築事務所首頁.dc.html`**這次也**被外部「圓點位置編輯器」的「同步至主網頁」匯出功能覆蓋過一次（整段擠成一行的格式、同樣少了 `r93`），這是本 session 第一次發生在這個檔案上（先前一直只有 `index.html` 會被外部覆蓋，`建築事務所首頁.dc.html` 一直是只靠這裡的 git push 在維護，見「⚠️ `index.html` 不是轉址頁」那節）。`git rebase origin/main` 順利套用、沒有衝突（外部覆蓋動的是 `nodeData`／`edgeData`／`languages` 陣列格式，跟這次改的 `dotColor`／`specialColor`／`labelColor` 完全不同區塊），rebase 完重新用 Playwright 驗證過顏色改動、`inPin` 點擊修法、`index.html` 單一份開場影片遮罩全部都還在，才推送。**另外還發現 `圓點位置編輯器.html` 現在也是這個 git repo裡的追蹤檔案**（`git log --all -- 圓點位置編輯器.html` 可以看到完整歷史，包含這次的 `d753145 "Update NODE_DATA in 圓點位置編輯器.html"`），但它是 `D:\2026as實習\網頁\原型教學轉code\design_handoff_architecture_site\圓點位置編輯器.html`（repo 資料夾內、117 個節點、8/17 之後就沒再手動更新過）——**跟這整個 session 一直在編輯的 `D:\2026as實習\網頁\圓點位置編輯器.html`（repo 資料夾外、97 個節點、今天才改過）是兩份完全不同、已經分岔的檔案**，這次的顏色對調只套用到後者（外面那份），repo 內的那份完全沒動過。這兩份檔案的關係、哪一份才是使用者實際在用的「編輯器」需要跟使用者確認清楚，不要自己猜——已經在對話裡提出來問了，這裡先記下來，下次不管誰接手都要注意「圓點位置編輯器.html」這個檔名其實對應兩個不同路徑、內容不同步。
53. **2026-08-19（第五十二次，目前狀態）：把第五十一次的顏色對調同步到「後台」那份 `圓點位置編輯器.html`——使用者確認「這個顏色要同步到後台的編輯位置」，回答了上一次調整結尾提出的問題：使用者實際在用的編輯器是 repo 裡被外部工具追蹤的那份（`design_handoff_architecture_site\圓點位置編輯器.html`，117 節點），不是這整個 session 一直在改的 `D:\2026as實習\網頁\圓點位置編輯器.html`（外面那份，97 節點）。做法：先用 `curl` 從 GitHub 重新抓一次這個檔案的最新內容（比對後跟本機 repo 裡的版本逐位元組相同，沒有進一步落後），確認完全一致後直接在本機這份上套用跟前一次一模一樣的改法——`:root` 預設區塊跟 `:root[data-theme="light"]` 顯式淺色區塊裡的 `--dot: #1a1a1a;`／`--special: #808080;` 對調成 `--dot: #808080;`／`--special: #1a1a1a;`（深色模式的兩個區塊沒動），以及兩處節點文字 `label.setAttribute('fill', isSpecial ? 'var(--special)' : 'var(--text)')` 改成讀 `var(--dot)`。**驗證方式**：Playwright 開這份檔案，直接讀 `getComputedStyle(document.documentElement)` 確認 `--dot` 變成 `#808080`、`--special` 變成 `#1a1a1a`、`--text` 維持 `#1a1a1a` 沒被牽動，頁面正常載入、console 無錯誤；commit 後推送到 `origin/main`（使用者要求「這個顏色要同步到後台的編輯位置」，見上面「⚠️ push 前又發現一件更大的事」那段的說明）。至此外面那份（`D:\2026as實習\網頁\圓點位置編輯器.html`）**沒有**被回頭同步任何內容——它跟後台那份的分岔（97 vs. 117 節點）依然存在，這次只解決了顏色設定，節點資料本身的分岔沒有處理，使用者沒有要求也不要自己動手合併。**

**桌機/平板從第二次調整開始就完全不受影響，一直都是原本的 2D 圓環，程式碼路徑也完全分開**（`renderVals()` 裡 `if (isMobile) { ...3D球體... } else { ...2D圓環，數值邏輯從頭到尾沒變... }`），第三～七次調整同樣沒有動桌機/平板任何一行邏輯——這次的版面調整也一樣，`pageDisplay`/`worksDisplayMode`/`globeSurfaceHeight` 等新增的版面變數全部是 `isMobile ? ... : 原本的值`，桌機維持原本的 block 版面跟 90vw 圓環公式。

下面先講第 5 版（目前狀態）的完整技術細節，再列前兩版的精簡歷史紀錄。

### 目前狀態：手機版 3D 球體（2026-07-31 第二～四次調整）

**整體概念**：116 個節點不再排在一圈平面圓環上，而是用 Fibonacci sphere 演算法平均分布在一個虛擬球面上；畫面上看到的圓點/連線是這顆球「這一刻旋轉到哪個角度」的 2D 投影。使用者可以用手指拖曳旋轉球體，放開後有慣性減速，靜止時球體會自己緩慢自轉。桌機/平板完全沒有這些邏輯，維持原本靜態的 2D 圓環。

- **球面座標**（`Component.computeSphereUnits()`，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) `renderVals()` 之前）：依 `nodeData` 原始順序（跟桌機圓環一樣沿用試算表列順序），用黃金角螺旋公式把 116 個點平均鋪在單位球面上，算一次記住，不受旋轉影響——旋轉只改變「觀察角度」，節點在球面上的相對位置（資料結構本身）永遠不變。
- **旋轉與投影**（`rotateProject(u, yaw, pitch)`）：把單位球面上一點依目前的 `yaw`（繞垂直軸）、`pitch`（繞水平軸）旋轉，得到 `{x,y,z}`；`z` 代表「面向使用者的程度」（+1 正對螢幕、-1 在球體背面），螢幕座標 = 球心 + `{x,y} * 球半徑`（單純正交投影，不做真正的透視縮放）。
- **深度感**（`buildGlobeGeometry()`）：`depthT = (z+1)/2`（0=最遠、1=正對螢幕）驅動三件事——圓點大小（`scale = 0.5 + depthT*0.8`，近的節點大、遠的節點小，乘在原本依連線數算出的 `nodeRadius` 基準值上，跟桌機共用同一份 `computeNodeRadiusMap()`）、圓點/文字透明度（`0.15 + depthT*0.85`，背面節點仍隱約可見，維持「球體輪廓」的視覺，不是整個消失）、連線透明度同理用兩端點的平均深度算（`0.04 + avgDepth*0.5`）。
- **文字標籤只顯示正面**（`labelVisible: depthT > 0.72`）：116 個節點的文字如果同時攤開在球面上，前後文字疊在一起完全看不清楚；只顯示面向使用者這一側（實測約 30-35 個同時顯示），效果類似「轉到面前才看得到標籤」的地球儀讀法。這個門檻是唯一控制「畫面多擠」的旋鈕，調高會更乾淨但同時看到的節點變少，調低反之——之後如果要再調，改這一個數字就好（`buildGlobeGeometry()` 跟 `paintGlobe()` 共用同一個方法，只要改一處）。
- **容器大小與溢出保護（2026-07-31 第三次調整後的做法）**：`diagramContainerWidth` 手機版現在是 `min(92vw, 88vh)`——依畫面等比例放大到接近整個螢幕寬度（使用者回報「整體主視覺太小」）。這個數字**不是**像第二次調整那樣用「拖到大量角度、量文字框邊界、找最差情況下不超出螢幕的臨界值」那套方法試出來的（那次量出來的安全值是 `60vw`，比現在小很多）——第三次調整改了策略：與其把容器縮到「保證任何角度都不超出螢幕」，不如直接讓容器**自己裁切**：`data-globe-surface` 這個外層 `<div>` 加了 `overflow:{{ globeOverflow }}`，手機版是 `hidden`（桌機維持 `visible`，因為桌機 2D 圓環本來就需要文字延伸到 viewBox 外一點點才裝得下，見前面「容器旁邊留的水平邊距」那段說明）。容器本身寬度是螢幕的固定比例、置中，本來就不可能超出物理螢幕邊緣；只要球體/文字延伸到容器自己的框線外，直接被裁掉，不會真的溢出到頁面上——**這樣不管容器多大、縮放到多少倍，都不需要再逐角度量測，物理上就不可能讓內容跑出螢幕**，代價是轉到極端角度或雙指放大很多時，靠近容器邊緣的文字可能被裁到看不完整（比「量出一個保守到不會裁切的安全值」更簡單粗暴，但換來容器可以放得更大、視覺更好）。**以後如果還要再調容器大小，不用像第二次調整那樣做角度掃描，只要留意「裁切要在畫面邊緣附近才發生、不要整顆球都被裁到」這種明顯過大的狀況即可。**
- **雙指縮放**（`_globeZoom`，預設 `1`，範圍 `[0.55, 2.6]`）：`componentDidMount()` 裡的 `_onGlobePointerDown/Move/Up` 用 `_globeActivePointers`（`Map<pointerId, {x,y}>`）追蹤目前放在螢幕上的每根手指——**一根手指是拖曳旋轉（跟第二次調整的邏輯完全一樣），兩根手指同時按著才會進入縮放模式**，這兩種手勢互斥（`_globePinching` 為真時，`pointermove` 直接 return，不會同時把移動量套用到 yaw/pitch，避免縮放跟旋轉互相干擾、角度跟縮放同時亂跳）。縮放量是「目前兩指距離 ÷ 手勢開始時兩指距離」乘上手勢開始時的 `_globeZoom`，不是每影格累加，所以放大縮小都是相對「這次手勢一開始」算的，不會累積誤差。`_globeZoom` 乘進 `buildGlobeGeometry()`/`buildGlobeEdgeGeometry()` 的球半徑（連帶圓點大小、文字離圓點的偏移都跟著等比例縮放），而不是只放大螢幕座標——這樣縮放看起來是整顆球「變大/變小」，不是球維持原尺寸、只是點的間距被拉開。兩指放開時如果只剩一根手指，會把單指拖曳的基準點重設到剩下那根手指目前的位置（避免手指數量切換的瞬間角度跳一下）；兩指都放開後才真正結束整個手勢、拿掉 `document` 上的監聽器。兩指手勢期間 `_globeDragDist` 會被直接加大（`+= 999`），確保放開時緊接的 `click` 一定會被 `_onGlobeClickCapture` 攔截，不會被誤判成點擊節點。
- **中央標題固定正面顯示，不跟著球體 3D 傾斜（2026-07-31 第四次調整，取消第三次的效果）**：第三次調整曾經讓標題套用 CSS 3D transform（`buildTitleTransform()`，`perspective(900px) rotateX(pitchDeg) rotateY(yawDeg)`）跟著球體的 `yaw`/`pitch` 一起傾斜。使用者體驗過後要求取消，改回固定正面顯示——`buildTitleTransform()` 方法、`data-globe-title` 屬性、`titleTransform` 這個 renderVals() 回傳值、`paintGlobe()` 裡每影格重寫 `cache.title.style.transform` 的那行，全部整個移除，不是留著設成 `'none'`（避免留下之後容易被誤用/誤解的死程式碼）。標題文字現在跟桌機版一樣，永遠是靜態、正面朝向使用者，完全不受球體旋轉影響。`_globeYaw`/`_globePitch` 的預設值仍然維持 `0/0`（第三次調整時為了配合標題「預設正面」改的），這個預設值本身沒有理由改回去，跟標題現在固不固定無關，純粹是球體自己的初始觀察角度。
- **手機版限制只有分類大標節點可以點擊**（使用者回報：「手機版的小圓點只有大標題的圓點允許點擊，小點就不可點擊」）：`buildGlobeGeometry()` 給每個節點多算一個 `clickable`（`this.specialIds.includes(d.id)`，就是章/成/法/構那 11 個分類大標節點），`renderVals()` 手機分支據此算出 `pointerEvents`（`clickable ? 'auto' : 'none'`）跟 `cursorStyle`（`clickable ? 'pointer' : 'default'`），套在每個節點最外層的 `<a href="{{ node.href }}">` 上。設 `pointer-events:none` 在 `<a>` 本身，整個子樹（含 `<a>` 自己）都不會再吃到點擊/觸控事件，所以一般節點在球面上一樣看得到、一樣會跟著轉，只是點不進去——避免 116 個節點擠在一顆縮小的球上，使用者常常點到旁邊不相干、當下甚至沒顯示文字（背面/被裁切）的小節點，誤觸到不想去的子頁。桌機這兩個值固定是 `'auto'`／`'pointer'`，不受影響，116 個節點都能點。
- **拖曳旋轉的實作**（`componentDidMount()` 裡的 `_onGlobePointerDown/Move/Up`）：掛在圓環容器（`data-globe-surface="1"`）上，用 Pointer Events 處理滑鼠/觸控。
  - **⚠️ 重要陷阱，不要重蹈覆轍**：一開始在 `pointerdown` 時呼叫 `surface.setPointerCapture(e.pointerId)`（常見的「拖曳超出元素範圍也追蹤得到」寫法），結果導致**點擊節點完全無法導覽到子頁**——不只拖曳，連靜止點一下都壞掉。原因是 `setPointerCapture` 會把接下來這個手勢的 `pointerup` 甚至合成的 `click` 事件整個「重新指向」到呼叫 capture 的元素（`surface` 這個 `<div>`），不會送到手指底下真正的節點 `<circle>`/`<a>`，所以 `<a href>` 永遠收不到 click，`goToNode()`/子頁跳轉整個失效。這個 bug 用 `element.click()`（DOM API 直接觸發合成 click，不會經過真正的 `pointerdown→pointerup→click` 序列）測不出來，一定要用 Playwright 的 `page.mouse.down()/move()/up()`（會走完整事件序列）才會重現，之後任何跟 pointer 相關的改動都要用這種方式驗證，不要只用 `.click()`。**修法：完全不用 `setPointerCapture`**，改成 `pointerdown` 當下才把 `pointermove`/`pointerup` 監聽器掛到 `document` 上（拖曳期間手指滑出 surface 範圍一樣追蹤得到，效果一樣），`pointerup` 時移除——這樣 click 事件的目標元素完全不會被動過手腳。
  - 拖曳距離（`_globeDragDist`，pointermove 累積的位移絕對值總和）超過 6px 門檻，放開時的 `click` 會被 `_onGlobeClickCapture`（掛在 `surface` 上、`capture:true`）攔截（`preventDefault`/`stopPropagation`），避免「拖曳放開時手指剛好停在某個節點上」被誤判成點擊、意外跳轉到子頁；小於門檻（幾乎沒移動，視為單純點擊）則放行，click 正常送到 `<a>`，跟桌機版點擊行為一致。
  - `surface` 上加了 `touch-action:none`（透過 `globeTouchAction` 值，桌機是 `auto`），避免拖曳球體時瀏覽器把手勢誤判成頁面垂直捲動、搶走觸控事件。
- **旋轉動畫刻意不用 `setState()`，直接寫 DOM 屬性**（`paintGlobe()`、`globeTick()`）：跟既有的搜尋黃色亮點 `tick()`/`startFlash()` 是同一個理由（見下面「其他仍然有效的手機版細節」第 3 點）——旋轉是連續、高頻的動畫（拖曳中每次 `pointermove` 都要重畫，放開後還有慣性減速 + 閒置自轉，理論上每秒最多到螢幕更新率的頻率），如果每次都走 `setState()` 觸發一次完整 reconcile 太浪費、手感也容易變黏滯。做法：
  - `paintGlobe()` 用 `_ensureGlobeElCache()` 快取好「節點 id → DOM 元素」的對照表（因為 `sc-for` 用陣列索引當 React key，只要陣列長度/順序不變，這些元素在整個頁面生命週期裡不會被換掉，快取是安全的——見 `support.js` 的 `walkFor()`），每一影格直接 `setAttribute('cx'/'cy'/'r'/...)`，不經過 `setState()`。
  - 但 `renderVals()`（一般的 state 驅動渲染，例如切換深色模式時）用的是**同一個** `buildGlobeGeometry()`/`buildGlobeEdgeGeometry()` 方法、讀的是**同一個** `this._globeYaw`/`this._globePitch` instance field（不是 React state），所以就算中途因為別的原因觸發一次正常的 re-render，畫出來的位置也是「目前轉到哪」，不會跳回初始角度、也不會跟 `paintGlobe()` 的結果打架。
  - 節點/連線的 CSS `transition`（原本桌機版拿來做 hover 淡入淡出的 `opacity 0.25s ease`）在手機版直接關掉（`nodeTransition`/`edgeTransition` 算成 `isMobile ? 'none' : ...`）——如果沒關掉，`paintGlobe()` 每 16ms 寫一次 `opacity`，每次都會被拖著跑一段 0.25s 的緩動，拖曳手感會變得黏滯、有明顯延遲。
- **光暈裝飾（spotlight）手機版直接不畫**（`hasSpotlight: false`，跟 2026-07-15 第一版精簡手機版理由不同）：球體會自轉、使用者也能拖著轉，這個效果原本假設「固定朝中央」，套在會旋轉的 3D 球體上會很奇怪（原型結構轉到背面時，光暈仍然是攤平的一條線，看起來像穿透球體），所以手機版乾脆不畫，桌機不受影響。
- **搜尋/黃色亮點**：`goToNode()`/`startFlash()`/`tick()` 都是直接抓 `[data-dot="id"]` 的 `getBoundingClientRect()`，不管這個節點目前是被 2D 圓環的 `positions` 算出來的，還是被 3D 球體的 `buildGlobeGeometry()` 算出來的，邏輯完全通用。2026-07-31 第五次調整之後，手機版搜尋定位的完整行為（黃色標註不再自動淡出、球體飛轉把節點定住）見下面「手機版搜尋：飛到北極點的地球儀效果」。
- **效能取捨**：`paintGlobe()` 每影格對 116 個節點 + ~180 條連線各做幾次 `setAttribute`，用查表快取降低查詢成本，沒有做「依深度排序 DOM 順序」這種完整的畫家演算法（背面節點理論上可能疊在正面節點的 DOM 順序前面，因為透明度夠低，實測目視幾乎看不出來，先不處理；如果之後真的看得出穿幫，這裡是要補的地方）。

### 手機版搜尋：飛到北極點的地球儀效果 + 地標 icon（2026-07-31 第五、六次調整）

使用者回報三點搜尋系統修正，分兩批做：先做前兩點、確認沒問題後才做第三點（地標 icon）：

1. **黃色標註不再自動淡出**：`componentDidMount()` 裡的全域 `mousedown` 監聽器（`_onDocMouseDown`）原本「點擊發生在搜尋框外，就淡出黃色亮點」這條規則，改成只在桌機生效（`&& this.state.viewportW >= 768` 這個條件）。手機版原本這條規則的問題是：使用者搜尋定位後，只要一開始拖曳/縮放球體（點在 `[data-globe-surface]` 上，不在 `[data-search-box]` 裡），立刻被判定「點在框外」、黃色標註瞬間淡出消失——這跟「保持黃色標註」的需求直接衝突，而且新加的飛行動畫（見下一點）本來就需要標註在拖曳過程中持續可見。手機版現在標註只有在搜尋到新節點（`goToNode()` 換了 `flashId`）時才會移動位置，不會因為點擊其他地方而無故消失；桌機行為完全沒變，點擊搜尋框外一樣會淡出。
2. **搜尋到的節點飛轉到「北極點」固定住，其餘節點依原速度繼續自轉**：`goToNode()` 在手機版（`viewportW < 768`）額外呼叫 `startFlyTo(id)`，用一個約 700ms、`easeInOutCubic` 緩動的補間動畫把球體轉到「該節點正對畫面正上方」的姿勢，之後即使閒置自轉/使用者左右拖曳持續改變角度，這個節點仍然會固定在原地，其他節點則照常繞著這個新的軸畫圓——效果跟真實地球儀「轉到某個地點、鎖定緯度/經度」一樣。
   - **旋轉模型改成兩層合成，不是原本單純的一組 yaw/pitch**：這是實作過程中繞了一次彎才確定下來的做法，很值得記錄，以免以後改動這部分又踩到同一個坑。最初的想法是「算一組 (yaw0,pitch0) 讓目標節點『這一刻』轉到北極點，之後讓原本就會持續遞增的 `_globeYaw` 繼續動，心想反正『y 軸上的點不受 yaw 影響』」——**這個推論只對 Fibonacci sphere 天生就在兩極的兩個節點（`r1`、`r116`，也就是 `computeSphereUnits()` 排列時 x=z=0 的那兩個點）成立**，對任何其他節點都不成立：算出來的 (yaw0,pitch0) 只保證「套用當下」會落在北極點，yaw 一旦繼續改變，這個節點就會沿著一個小緯度圈慢慢飄走（實測：等 1.5 秒，飄移了近 10px，不是完全靜止；而且一開始還誤把飄移歸咎成測試腳本抓錯了搜尋結果——「原型結構」剛好是「原型建築+原型結構」的子字串，Playwright 用子字串比對的 `text=` 選到了不同節點，後來改用精準比對 `getByText(..., {exact:true})` 才發現飄移在正確節點上依然存在，確認是計算邏輯本身的問題，不是測試誤判）。
   - 正確做法是把旋轉拆成兩層：`_globeBaseYaw`/`_globeBasePitch`（**校準層**，搜尋定位後算一次、之後固定不變，把整顆球轉到「目標節點在校準後的座標系裡剛好是北極點」的姿勢）疊在下面，`_globeYaw`/`_globePitch`（**自轉層**，跟以前意義相同，拖曳/閒置自轉持續改變的就是這一層）疊在上面——`projectUnit(u)` 這個方法就是做「先套校準層、再套自轉層」的合成，`buildGlobeGeometry()`/`buildGlobeEdgeGeometry()` 都要呼叫這個方法，不能繞過它直接呼叫 `rotateProject()`。沒有搜尋定位過的話校準層是 `(0,0)` 單位矩陣，效果跟只有自轉層（也就是這次調整以前的行為）完全一樣，向下相容。北極點在「校準後的座標系」裡確實是 x=z=0，這時候自轉層對它做 yaw 旋轉才會真的不動——用 Playwright 直接呼叫 `projectUnit()`/`buildGlobeGeometry()` 驗證過，搜尋定位後的節點螢幕座標精確落在 `(450, 190)`（容器中心正上方、球心往上一個球半徑的距離），閒置自轉 1.5 秒後座標移動量是 `(0, 0)`，其他節點則確實跟著自轉層移動了幾個 px（隨自轉速度而定）。
   - **「北極點」是 `(0,-1,0)` 不是 `(0,1,0)`**：SVG 的 y 座標往下遞增，螢幕座標 `= cy + p.y·球半徑`，所以 `p.y=-1` 才對應畫面上緣（北極、視覺上的「頂端」），`p.y=+1` 反而是畫面下緣（南極）——一開始沒注意到這點會導致飛到「南極」（球體底部）而不是預期的頂部。
   - **拖曳的 pitch 夾限拿掉了**：原本手動拖曳的上下傾斜（`_globePitch`，現在是自轉層的 pitch）限制在 ±1.15 弧度內，避免使用者手動把球體整個翻過去。但飛到北極點常常需要把校準層的 pitch 轉到遠超過這個範圍（畢竟目標節點可能在原始球面座標裡任何位置，離「原生北極」多遠就要轉多少），如果拖曳的夾限還留著，飛行完成後只要使用者一伸手拖曳，pitch 會立刻被夾回 ±1.15 範圍內，畫面會明顯跳一下。乾脆把拖曳的 pitch 夾限整個拿掉，拖曳跟飛行共用同一個「pitch 沒有邊界」的規則，兩者才不會打架。
   - 飛行動畫進行中，如果使用者主動抓住球體拖曳/縮放，會立刻取消飛行（`_globeFlying = false`），改由手勢接管——不會讓動畫跟手動拖曳同時搶著改角度。
   - 飛行動畫同時會把自轉層的 pitch 補間回 0（跟校準層的動畫同步），確保飛到定位那一刻，目標節點精確落在畫面正上方置中，不會被飛行前使用者留下的上下拖曳角度帶偏；自轉層的 yaw（左右自轉）完全不受影響，飛行過程中維持原樣、繼續轉，銜接起來不會卡頓。
3. **標註節點正上方的虛線＋地標 icon，點 icon 或圓點本身都能進子頁**（2026-07-31 第六次調整，`ensureLandmark()`）：跟 `ensureFlash()`/`ensureTriangle()` 一樣，這組元素是原生 DOM、body-appended、不透過樣板，理由相同（見「旋轉動畫刻意不用 setState()」那條）——多一層原因是虛線本身要跟著 dot 的即時螢幕座標每影格更新，直接掛進既有的 `tick()` 迴圈（本來就在每影格重新量測 dot 位置）最省事，不用另外開一個迴圈。
   - **虛線**：一個蓋滿整個 viewport 的 `<svg>` 裝一條 `<line pathLength="1">`——用 `pathLength="1"` 把整條線的長度正規化成 1，`stroke-dasharray`/`stroke-dashoffset` 才能用固定的小數字（`"0.06 0.05"`、關鍵影格位移 `-0.11`）表示「虛線的一個週期」，不用管線實際畫面上有幾個 px 長，容器縮放/雙指縮放也不用重算。`@keyframes dashFlow` 讓虛線持續往同一方向流動，維持「有東西正在動」的感覺。
   - **「向上延伸」的長出動畫**：不是 CSS transition（SVG 的 `x1/y1/x2/y2` 用 CSS 過渡在部分瀏覽器支援度不一致），改成跟 `paintGlobe()` 同樣的做法——`tick()` 每影格用 `_pinGrowStart`（`goToNode()` 裡搜尋當下記錄的時間戳）算出經過時間的 easing 比例，直接把 `y2`（線的終點、也就是地標 icon 的位置）從 dot 邊緣往上補間到全長，380ms 內完成，之後維持全長。每次重新搜尋都會重設 `_pinGrowStart`，讓虛線重新「長」一次，就算上一次的還沒完全淡出也一樣。
   - **地標 icon 是真正的 `<a href>`**，不是裝飾用的 icon 加一個 onClick——這樣既有的頁面轉場腳本（`document` 上監聽點擊的那段，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 檔案最下面的 `<script>`）可以直接處理點擊導覽，不用另外重寫一套。連結裡藏了一個 `display:none` 但 `textContent` 還是讀得到的 `<div>`，放目標節點的名稱。**2026-08-18 起這個 div 已經完全沒有作用**——原本轉場腳本會用它決定跳字動畫要顯示的目的地文字，現在不管是本機子頁還是「原型建築」（`r109`）這個外部連結節點，前導動畫都已經不顯示任何文字（本機子頁直接原生導頁；`r109` 改播開場影片），這個 div 留著不影響功能，不用特地清掉，但也不用再假設它會被讀取。
   - **搜尋標註的節點恢復可點擊**：`buildGlobeGeometry()` 算出的 `clickable`（只有分類大標節點是 `true`，見第三次調整）加了一個例外——`renderVals()` 手機分支的 `pointerEvents`/`cursorStyle` 改成 `(g.clickable || d.id === this.state.flashId) ? 'auto' : 'none'`。原本「只有大標可點」是為了避免 116 個小節點擠在球面上很容易誤觸，但使用者搜尋定位到的節點就是使用者明確想去的地方，不該被這條規則擋住——地標 icon 提供了一個大很多的點擊目標，但「圓點本身也要能點」是使用者明確要求的，兩個入口都留著。
   - 桌機完全不受影響：`ensureLandmark()` 只在 `goToNode()` 的手機分支（`viewportW < 768`）才會被呼叫，桌機從頭到尾不會建立這組 DOM 元素。
   - **顏色改成純線條、不填色（2026-07-31 修正）**：一開始虛線跟 icon 都用跟黃色亮點同一種 `#FFD400` 填色（icon 是實心黃底teardrop），使用者要求改成單純線條——淺色模式黑線、深色模式金線（`#BDA434`，跟 `specialColor` 深色模式用的金色同一個色號），不要再填滿黃色。icon 的 SVG 改成 `fill="none" stroke="currentColor"`（跟頁首搜尋/漢堡選單那些線條 icon 同一種風格），顏色統一設在外層 `<a>` 的 CSS `color` 上，靠 `currentColor` 往下繼承到 `<path>`/`<circle>`；虛線的 `<line>` 顏色則直接設它自己的 `stroke` 屬性。兩者的顏色都在 `tick()` 每影格依 `this.state.isDarkMode` 現算現設（`pinColor = isDarkMode ? '#BDA434' : '#000000'`），不是只在建立時設一次——這樣使用者中途切換深色模式，正在顯示的地標線條會立刻跟著變色，不用等下一次重新搜尋。黃色亮點本身（`_flash`，dot 周圍的模糊光暈）不受影響，維持原本的黃色填色，使用者這次只針對虛線跟 icon 兩個元素。

### 手機版球體標題文字顯示規則、大標分布、裁切框大小（2026-07-31 第七次調整）

使用者一次回報三點，第一點是既有行為的確認，後兩點是實際修正：

1. **轉到正面的節點才顯示文字標籤，背面不顯示**——這條規則從第二次調整（3D 球體）就存在（`buildGlobeGeometry()` 的 `labelVisible: depthT > 0.72`）。第七次調整當下用 Playwright 統計過全部 116 個節點「當時那一刻」的 `labelDisplay` 狀態，看起來分類大標、一般節點都正確遵守同一條規則，判定沒有 bug——**這個判定後來證實是錯的**，只統計「某一刻的靜態快照」測不出這裡的 bug，因為 bug 的症狀是「隨時間旋轉之後才會出現」，不是任何單一時間點的狀態能看出來的。使用者接著回報「目前是只有一開始進入網頁時的正面會顯示字，轉到背面時變成什麼字都沒有」，才抓到真正的問題，詳見下面「文字顯示的 display 切錯元素」這條（2026-07-31 第八次調整）。
2. **分類大標節點在球面上重新平均分布**（`computeSphereUnits()`）：使用者回報「大標題」（章/成/法/構等 11 個節點）在球面上擠成一團，不是散開的。用 Playwright 直接讀 `computeSphereUnits()` 算出的原始球面座標驗證過，確實集中在緯度 `y` 一個很窄的區間——因為 Fibonacci sphere 的 `y` 是索引 `i` 的線性函數，這 11 個節點在 `nodeData`（試算表列順序）裡剛好落在索引比較接近的一段區間，跟黃金角決定的經度分不分散無關，緯度自然就跟著擠在一起。
   - **⚠️ 第一次修法是錯的，記錄下來避免以後重踩**：試過「全部 116 個節點共用同一組連續索引，但保留 11 個平均間隔（每 116/11≈10.5 個位置一個）的槽位給大標節點」——緯度確實變均勻了（線性函數、索引平均分布，`y` 自然也平均分布），但**經度完全沒有變均勻**：黃金角「相鄰索引在球面上均勻散開」這個性質，只在**索引間隔為 1** 的連續序列上成立；間隔約 10.5 的索引之間，經度（`theta = 黃金角 × 索引`）每次只增加「黃金角×10.5 mod 360°」這一個固定的小角度（實測約 3.8°），11 個點的經度全部擠在 0°~103° 這一小段裡，反而比原本更集中。一樣是用 Playwright 直接讀原始座標驗證出來的，不是肉眼猜的。
   - **正確做法**：大標節點（11 個）、一般節點（105 個）分成兩組，各自從索引 0 開始、各自代入自己那組的 `n` 獨立跑一次 Fibonacci sphere 公式——黃金角「連續索引均勻覆蓋全球面」的性質對任何 `n` 都成立，兩組各自單獨看都會均勻覆蓋整個球面（緯度、經度都均勻），疊在同一顆球上自然不會擠成一團。用 Playwright 驗證過：修正後大標節點的緯度剛好是 `1, 0.8, 0.6, ..., -1`（完美平均分布），經度也涵蓋了 -170°~158° 幾乎整個圓周（跟第一次修法的 0°~103° 天差地遠）。desktop 的 2D 圓環排版完全不受影響，桌機沒有呼叫 `computeSphereUnits()`。
3. **球體的裁切框（`data-globe-surface`）上下緣貼齊頁首下緣／頁尾上緣**：使用者形容「好像有一個框架罩住主視覺」，一開始不確定具體指什麼（頁面上沒有任何看得見的邊框/outline），先用 `AskUserQuestion` 確認，得到的答案是「球體容器本身的裁切邊界」——也就是 `data-globe-surface` 那個 `overflow:hidden` 的方形容器，原本被 `#works` 區塊上下各 450px 的固定留白（`padding:450px 12px`，桌機/手機共用同一組數字，不是手機版特別調的）擠成一個懸在畫面中間的小方塊，頁首下方跟頁尾上方各留了一大片空白。
   - **改法（僅手機版）**：最外層的頁面 `<div>` 改成 `display:flex; flex-direction:column`（`pageDisplay`/`pageFlexDirection`，桌機維持 `block`）；`#works` 改成 `flex-grow:1`（`worksFlexGrow`）去吃掉頁首（`position:fixed`，不佔 flex 版面空間）跟頁尾之間的剩餘高度，上邊距只留清開固定頁首要用的 `96px`（`worksPaddingTop`，用 Playwright 實測頁首真實高度後定案——第一版抓 80px 少算了 16px，球體裁切框上緣一度還被頁首蓋掉一截），下邊距歸零（`worksPaddingBottom`）。`data-globe-surface` 本身從「視覺球體」升格成「裁切框」：改成 `width:100%; height:100%`（`globeSurfaceHeight`）撐滿整個 `#works`，裡面另外包一層新的內層 `<div>`（沿用原本 `diagramContainerWidth` 算出來的正方形尺寸，視覺球體大小完全沒變），靠 flex 置中在裁切框正中央（`globeSurfaceDisplay:flex` + `align-items:center; justify-content:center`）。桌機這幾個新變數都是走原本的值（`block`／`450px`／`auto`），畫面完全沒變。用 Playwright 量測過：手機版裁切框上緣跟頁首下緣的間距、下緣跟頁尾上緣的間距，都精確是 0px。
   - **⚠️ 意外的連動 bug，這次一起修了**：裁切框貼齊頁首下緣之後，釘在北極點（畫面最上方）的搜尋標註，虛線往上長很容易長到頁首（`z-index:1000`，`overflow:hidden` 之前有 450px 留白擋著，兩者離得很遠不會相撞）底下——地標 icon 是 `z-index:951`，比頁首矮，圖示看得到但點下去打在頁首上，完全點不到。用 Playwright 的 `elementFromPoint()` 直接驗證出來，不是靠肉眼看漏。修法：`tick()` 算虛線終點（`endY`）時，夾住一個「安全上限」——`document.querySelector('header')` 目前的 `getBoundingClientRect().bottom`，再加上 12px 緩衝、以及地標 icon 自己的高度 38px（因為 icon 是靠 `transform:translate(-50%,-100%)` 錨定在虛線終點、往上長出 38px，只夾住錨點的話 icon 本體還是會露出一截在安全線之上，要把 icon 整個高度都算進安全邊界才夠）。夾住之後虛線在目標非常靠近頁首時會提早停止延伸（達不到原本設計的全長），但地標 icon 永遠留在頁首下方點得到的地方，這個取捨是值得的。

### 文字顯示的 display 切錯元素——只有初次載入時的正面節點會顯示文字（2026-07-31 第八次調整）

**症狀**：手機版球體剛載入時，正面那些節點的文字顯示正常；但只要開始旋轉（拖曳或閒置自轉），原本背面、後來轉到正面的節點永遠不會出現文字；反過來，原本正面、後來轉到背面的節點會正確把文字藏起來。旋轉久了，畫面上的文字會越來越少，最後全部消失。

**根本原因**：樣板實際控制「文字顯示/隱藏」的元素，是包住 `<foreignObject>` 的父層 `<g data-label-wrap style="display:{{ node.labelDisplay }}">`（[建築事務所首頁.dc.html:221](./建築事務所首頁.dc.html) 附近）——但 `paintGlobe()` 每影格更新時，錯把 `display` 切在 `<foreignObject>` 自己身上（`_ensureGlobeElCache()` 快取的 `fo` 欄位）。這兩個是**不同的 DOM 元素**：`foreignObject` 是 `labelWrap` 的**子元素**。

`labelWrap` 這層的 `display` 只有樣板初次渲染（`renderVals()` 觸發的 React 渲染，例如剛載入、或切換深色模式）才會設定一次，之後 `paintGlobe()` 的每影格更新完全沒有動過它——於是：

- 如果某節點剛載入時**在正面**（`labelWrap` 初次渲染就是 `display:block`），之後 `paintGlobe()` changes 的是它的**子元素** `foreignObject` 的 `display`——因為父層本來就是 `block`（沒擋住任何東西），子元素的 `display:none`/`''` 切換能正常生效，這個節點的文字顯示/隱藏看起來運作正常。
- 如果某節點剛載入時**在背面**（`labelWrap` 初次渲染就是 `display:none`），之後不管 `paintGlobe()` 怎麼切子元素 `foreignObject` 的 `display`，**父層的 `display:none` 從頭到尾沒被動過**，子元素永遠被父層蓋住，文字永遠不會出現——即使這個節點之後轉到正面也一樣。

這完全對得上使用者形容的症狀：只有「初次載入時剛好在正面」的那批節點，之後才能正常切換文字顯示；其餘節點（多數）不管轉到哪裡，文字永遠出不來；隨著旋轉，原本正面的那批陸續轉到背面、正確隱藏，但沒有新的文字補上來遞補，看起來就是「文字越轉越少，最後全部消失」。

第七次調整當時用 Playwright 統計過一次「當下那一刻」全部 116 個節點的 `labelDisplay` 狀態、覺得沒問題，是因為**單一時間點的快照本來就測不出這個 bug**——bug 只有「隨旋轉、跨越多個時間點」才會顯現，靜態快照不管抓哪一刻，當下顯示的節點集合看起來都是「正確的一部分」，要動態追蹤同一批節點跨時間的變化才抓得到。

**修法**：

1. 幫樣板裡的 `labelWrap` 那層 `<g>` 加上 `data-label-wrap="1"` 屬性，讓它可以被穩定選取到（原本沒有任何屬性可以跟 `foreignObject` 區分開）。
2. `_ensureGlobeElCache()` 新增 `labelWrap: group.querySelector('[data-label-wrap]')` 這個快取欄位，跟原本的 `fo`（`foreignObject`，只負責 `x`/`y`/`width`/`height` 這些跟文字顯示與否無關的屬性）分開。
3. `paintGlobe()` 改成 `els.labelWrap.style.display = g.labelVisible ? '' : 'none';`，切的是正確的那一層，跟樣板初次渲染用的是**同一個元素**，兩邊不會再對不上。

**驗證方式**：這個 bug 的本質是「跨時間的狀態」，靜態截圖或單一時間點的查詢都測不出來，一定要**動態追蹤同一批節點在多次旋轉步驟中的顯示狀態**才能驗證。用 Playwright 對球體做 24 次小角度拖曳（涵蓋 yaw 跟 pitch 兩個方向），每一步都記錄全部 116 個節點的 `labelWrap` 顯示狀態，統計每個節點在整個過程中「是否曾經顯示過」跟「是否曾經隱藏過」——修正前，多數節點會停留在初次載入時的狀態，一次都不會切換；修正後，116 個節點裡有 82~102 個（依旋轉樣本涵蓋的角度範圍而定）在測試過程中明確切換過至少一次顯示/隱藏，且畫面上同時顯示的文字總數不會隨旋轉單調遞減到 0（修正前會）。少數在特定測試樣本中沒切換到的節點，是因為該節點的球面座標剛好接近「目前這組 yaw/pitch 掃描范圍內怎麼轉都不會轉到正面」的位置（例如緯度非常接近某群組自己的極點，只有靠 pitch 而非 yaw 才能改變其正面/背面朝向），屬於取樣覆蓋率的限制，不是這個 bug 的殘留。

### 地標 icon 位置/樣式微調、搜尋標註成立後鎖定水平拖曳（2026-07-31 第九次調整，**第 3、4 點已被第十一次調整取代，見下面「標註顯示後任何操作都立刻消失」**）

**⚠️ 這節的第 3 點（`_pinLocked` 水平拖曳鎖定）、第 4 點（點球體外才消失）已經在第十一次調整被推翻**：使用者體驗過這版之後，明確要求「標註顯示後只要做任何操作（含拖曳球體本身）都要立刻消失」，`_pinLocked` 欄位、`_onGlobePointerMove` 裡鎖 `_globePitch` 的那段都已經整個移除，`_onDocMouseDown` 也改回「點擊搜尋框以外一律消失」（不再排除球體容器）。留著這節不刪，是因為第十一次調整的說明裡會提到「這個決定推翻了上一版剛做好的東西」，需要對照上一版原本在做什麼、為什麼一開始會這樣做——純粹是歷史脈絡，**照著抄的話，第 3、4 點的程式碼現況已經不是這樣了**。第 1、2 點（icon 位置/線粗）也各自被第十一次調整再修過一次，同樣別照抄數值，去看下面第十一次那節的最終版本。

使用者一次回報四點，前兩點是 `ensureLandmark()`/`tick()` 既有地標 icon 的位置與樣式微調，後兩點是新增的拖曳行為限制：

1. **地標 icon 位置再往上拉 2cm、虛線變細**（`tick()`，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）：`lineLen`（虛線長度，決定 icon 離 dot 多遠）原本是 `dotR + 60`，加上 `CM_PX * 2`（`CM_PX = 37.8`，CSS px/cm 標準換算 `96dpi ÷ 2.54`）。虛線的 `stroke-width` 從 `3` 改成 `1.5`（`ensureLandmark()` 建立 `<line>` 時的初始值）。
2. **修正 icon 頂端貼近頁首、確保至少留 1cm 間距**：`safeTop`（虛線終點/icon 錨點的最高位置，夾住不讓 icon 跑進頁首底下）原本只留 `12px` 緩衝，離頁首太近、視覺上像被頁首切掉一角；改成用同一個 `CM_PX` 常數，變成至少留 1cm（約 37.8px）。這個間距是 icon **頂端**（不是錨點）到頁首下緣的距離——`safeTop` 本身還要再加上 icon 自己的高度（`pinIconHeight = 38`）才是錨點的安全位置，這部分邏輯（第七次調整就有）沒有變。
3. **搜尋標註成立後，鎖定球體只能水平（赤道方向）拖曳**（`_pinLocked` 欄位）：`goToNode()` 手機分支在呼叫 `startFlyTo(id)` 之後，把 `this._pinLocked` 設成 `true`；`_onGlobePointerMove` 裡套用 `dy`（決定 `_globePitch`，也就是上下傾斜）的那段包進 `if (!this._pinLocked)`，鎖定時直接跳過，`dx`（決定 `_globeYaw`，左右自轉）完全不受影響。之所以只要擋 `_globePitch` 就夠、不用另外處理 `_globeBaseYaw`/`_globeBasePitch`（校準層）：校準層本來就只有 `startFlyTo()` 的補間動畫會改，手動拖曳從頭到尾只改自轉層，鎖定前後都一樣不會碰到校準層。閒置自轉（`globeTick()` 的 idle 分支）本來就只累加 `_globeYaw`、不動 `_globePitch`，不用額外處理。北極點在「校準後的座標系」裡本來就是 `x=z=0`，繞垂直軸（yaw）怎麼轉都不會偏離（見第五次調整「飛到北極點」那段的推導）——這正是「只鎖 pitch、不鎖 yaw」在幾何上站得住腳的原因，不是隨便選的限制。
4. **點擊球體以外的區域，標註消失並解除鎖定**：`_onDocMouseDown` 原本手機版完全不會因為點擊其他地方而讓標註淡出（第五次調整刻意改的，因為拖曳球體本身也會被判定成「點在外面」，會跟「保持標註」衝突）。這次改成用 `[data-globe-surface]` 而不是 `[data-search-box]` 當判斷邊界：點在球體容器**內**（拖曳/縮放）不算「點外面」，標註繼續保持；點在球體容器**以外**的任何地方（頁首、選單、頁尾……）才視為「使用者不再需要這個標註」，呼叫 `dismissFlash()` 淡出。`dismissFlash()` 同時把 `_pinLocked` 重設回 `false`，回到可以自由上下/水平拖曳的一般模式。桌機版判斷邊界維持原本的 `[data-search-box]`、行為完全沒變（點擊搜尋框外一律淡出）。
   - **驗證時的一個陷阱**：手機搜尋列展開時（`showMobileSearchBar`），它的外層 `<div data-search-box="1">` 是 `position:fixed; top:0; left:0; right:0` 的整條頁首寬度色塊——如果測試時忘記先關閉搜尋列就去點頁首其他地方，點擊其實會落在這個搜尋框的色塊裡，`inBox` 判斷為真，`dismissFlash()` 根本不會被呼叫，容易誤判成「點外面沒有解除鎖定」的 bug。用 Playwright 測試這個行為時要先點搜尋列的關閉鈕，再去點「球體以外」的地方。

**驗證方式**：用 Playwright 測了幾種情境——(1) 搜尋定位後量測 icon 頂端到頁首下緣的間距，精確等於 `37.8px`（1cm）；(2) 標註成立後做一個「大幅上下拖曳」的手勢，icon/虛線的螢幕座標完全沒變（鎖定生效），但同時做一個「純水平拖曳」，球面上其他節點的位置確實跟著轉動（yaw 沒被鎖住）；(3) 點擊頁首右側（球體容器以外）後，標註立刻淡出；(4) 淡出後再做一次「純垂直拖曳」，這次球面上的節點確實跟著上下傾斜（`_globePitch` 已解鎖，用 116 個節點的平均位移量驗證，鎖定時位移量趨近 0、解鎖後有明顯位移）。桌機版（1440px）重新截圖比對，搜尋行為（點外面淡出）、地標 icon 元素數量（桌機恆為 0）都跟改動前一致。

### 手機版節點文字縮減（`nodeData[].mobileName`，2026-07-31 第十次調整）

**動機**：手機版球體上的文字標籤空間很擠（見「手機版球體標題文字顯示規則」那節），`nodeData` 裡不少節點名稱是完整的「年份＋地點＋工程性質＋得獎狀態」長句（例如「2015 左鎮菜寮化石文化園區改建工程競圖獲勝」），縮短能讓球面上的文字更好讀。使用者要求先給縮減前後對照清單確認過，才套用到程式碼——不是自動規則，是逐一手動決定的結果。

**做法**：`nodeData` 每筆物件新增一個可選欄位 `mobileName`，只在需要縮減的節點上才加（沒有這個欄位的節點，手機版就退回顯示原本的 `name`）。使用位置只有兩處，都在手機分支：

- `renderVals()` 的 `isMobile` 分支（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）：`const displayName = d.mobileName || d.name;`，取代原本直接用 `d.name` 算 `nodes[].name`（樣板顯示用）跟 `boxW`（文字方塊寬度，依字數估算）。
- `paintGlobe()`：`boxW` 算法同步改成 `(d.mobileName || d.name).length`，跟 `renderVals()` 用同一份文字長度，避免兩邊算出的方塊寬度對不上（拖曳旋轉時文字框忽大忽小）。

**刻意不動的部分**（維持完整原名）：

- **桌機/平板的 2D 圓環**：`renderVals()` 的 `else`（非 `isMobile`）分支完全沒有讀 `mobileName`，`nodes[].name` 還是直接用 `d.name`——桌機畫面逐字不變。
- **搜尋功能**：`handleSearchInput()` 的比對（`n.name.toLowerCase().includes(...)`）跟搜尋結果清單顯示（`result.name`）都還是用 `nodeData` 的完整 `name`，手機/桌機都一樣。這是刻意的——縮減後的短名可能會讓使用者用原本記得的關鍵字（例如年份）搜不到東西，保留完整原名比較保險。
- **地標 icon 的隱藏文字**（`goToNode()` 裡 `this._pinLinkLabel.textContent = node.name`，供頁面轉場閃字動畫使用）：也是用完整原名，不是縮減版——這段文字轉場時只會短暫閃過，用完整資訊比較好認。

**縮減規則**（跟使用者來回確認出來的，不是寫死的演算法，之後如果又要縮減其他節點可以參考同一套邏輯，但每次都要先列清單給維護者確認，不要自動套用）：

1. 去掉開頭的西元年份。
2. 去掉結尾的「得獎狀態/行政流程」贅字（獲獎、競圖獲勝、雜誌報導、新建工程、興建工程、設計監造案、台灣建築獎首獎/佳作/特別獎、ADA新銳建築獎首獎、德國IF設計獎……），但保留地點/建物本身的描述性字詞（「眺望台」「公廁」「校舍」「景觀橋」等不刪），避免刪過頭讓人看不出是什麼案子。
3. **縮完會撞名（多個節點縮成一模一樣的文字）時，只留一個節點用縮減版，其餘節點的圓點保留、文字改回自己原本沒縮過的完整名稱**（不是刪除圓點，也不是憑空編一個新名字）——預設規則是「id 較小的留短名，id 較大的還原成原文」，最終逐組實際用哪個由使用者確認。已處理的撞名組：`r40`（留 `X-SITE「途中」`）/`r85`（還原 `2022X-SITE途中`）、`r48`（留 `嘉義實驗木場`）/`r57`（還原原文）、`r50`（留 `雲林農博`）/`r60`（還原原文）、`r51`（留 `宜蘭中山小巨蛋`）/`r62`（還原原文）、`r53`（留 `嘉義美術館`）/`r67`（還原原文）、`r54`（留 `德光教會`）/`r64`（還原原文）、`r102`（留 `構竹林鐵`）/`r103`（還原 `2023構竹林鐵`）。
4. **同一個專案跨多個節點、使用者覺得沒必要逐一區分細節時，可以整組直接收斂成同一個短名**（不是「撞名後留一個」的被動處理，是主動要求）——目前唯一一組：「構竹林鐵」家族的 `r33`、`r47`、`r84`、`r91`（原本分別是「構竹林鐵新銳展」「嘉義構竹林鐵新銳展」「構竹林鐵Ⅰ」「構竹林鐵Ⅱ」）全部統一改成 `構竹林鐵`，跟已經是這個短名的 `r102` 五個節點共用同一段文字——這五個圓點在球面上會顯示一樣的字，是刻意的（同一個專案不同年份/階段的紀錄），不是遺漏的撞名。**跟規則 3 的差異**：規則 3 是「縮完不小心撞名，挑一個保留」，規則 4 是「使用者主動要求整組共用同一個短名」，兩者判斷邏輯不同，之後遇到類似情況要分清楚問使用者是哪一種。
5. 有明確資料缺陷的節點，縮減時一併修正，不是機械式套用規則 1/2：`r19` 原文「2002橫濱大」去掉年份後剩「橫濱大」，看起來是原始 Excel/資料本身漏字，使用者確認後改成 `橫濱大棧橋`（不是單純去年份，是連內容一起補全）。
6. **這次特意維持不變、留給以後決定的節點**：分類大標 `r1`/`r22`/`r28`/`r59`/`r100`（中英雙語的「章 architecture design」等）目前沒有加 `mobileName`；「台南咾咕石」家族（`r105`/`r106`/`r107`/`r108`/`r114`）使用者明確要求「保持原本的」，只套用規則 1/2（年份/贅字去除），不像構竹林鐵家族那樣整組收斂成同一個短名。

**驗證方式**：Playwright 對照 13 個代表性節點（含撞名解決組、家族收斂組、資料修正組）的手機球體文字、桌機圓環文字、手機搜尋結果文字三者，確認手機球體顯示縮減版、桌機圓環跟兩種模式下的搜尋結果都仍是完整原文，逐一比對輸出結果跟預期一致。

### 地標 icon 改用高 z-index、線條更細、漸進式斷點、任何操作立刻消失（2026-07-31 第十一次調整，**第 1 點的「不夾頁首下緣」已被第十五次調整推翻**）

**⚠️ 這節第 1 點「拿掉 safeTop、只留螢幕上緣防呆」已經在第十五次調整被推翻**：使用者後來又要求「確保地標icon的高度低於頁首的下緣」，`tick()` 現在重新加回了夾頁首下緣的邏輯（細節見下面第十五次調整那節），跟這節寫的「不需要再夾頁首下緣」正好相反。z-index 拉到 `1010` 這件事本身沒有變（疊圖順序還是對的），但單靠 z-index 不再是唯一的防線，位置本身也要限制在頁首下方——留著這節不刪是要說明「為什麼一開始會覺得不需要夾」，不是目前的實際行為。

使用者體驗過第九次調整之後，一次回報四點——第 1、3 點分別推翻了第九次調整的兩個做法，維護時要清楚知道「為什麼上一版那樣做、這一版為什麼又改回來」，不是單純堆疊修正：

1. **地標 icon 還是被頁首切掉一角，改用 z-index 讓它疊在頁首之上，不再靠夾位置閃避頁首**：第七、九次調整都是用「夾住虛線終點的最高位置（`safeTop`），不讓它跑進頁首範圍」的做法，讓地標 icon 永遠留在頁首下方——但使用者這次回報「還是被切掉一小塊」，而且明確要求「請將頁首中段作成透明的或是用其他技術讓地標icon能完整呈現」。重新檢查 `<header>`（[建築事務所首頁.dc.html:125](./建築事務所首頁.dc.html)）才發現：**`<header>` 本身其實沒有背景色**，只有 padding/flex，真正有背景色的是裡面 logo、導覽 pill 那兩塊半透明毛玻璃小方塊，中段本來就是透明的——地標會被「切掉一角」的真正原因不是視覺上被什麼東西蓋住，是 **z-index 疊圖順序輸了**：地標原本是 `z-index:951`，頁首是 `z-index:1000`，地標的 icon 頂端只要幾何座標落進頁首的矩形範圍內，不管頁首那個位置有沒有畫東西，瀏覽器都還是照 z-index 決定「這個座標該畫誰」，頁首（就算透明）還是贏。**修法**：把地標的 `<svg>`（虛線）跟 `<a>`（icon）z-index 都改成 `1010`（比頁首 1000、手機搜尋列 1001 都高，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) `ensureLandmark()`），從根本上解決「疊圖順序輸給頁首」的問題，`tick()` 裡原本那段夾 `safeTop`（頁首下緣 + 1cm + icon 高度）的邏輯整個拿掉，改成只留一個「別跑出螢幕最上緣」的最小防呆（見下面第 5 點的教訓）。
2. **虛線、icon 線條都再變細，icon 位置再往上拉一點**：虛線 `stroke-width` 從 `1.5` 再減半到 `0.75`；icon 的 SVG 描邊（`path`/`circle` 的 `stroke-width`）從 `2` 減半到 `1`；`lineLen`（決定 icon 離 dot 多遠）在原本 `+2cm` 的基礎上再加 `0.5cm`（`CM_PX * 2.5`，`tick()` 附近）。
3. **標註（icon＋虛線＋黃色亮點）顯示後，只要有任何操作就要立刻全部消失，不再有「拖曳球體本身不算」的例外**：第九次調整加的 `_pinLocked`（鎖定球體只能水平拖曳，讓使用者可以邊看標註邊轉）被使用者明確推翻——這次要求「點擊被標註的圓點、滑動螢幕、手動轉動球體、點擊其他功能按鈕」全部都要立刻讓標註消失。**做法**：整個移除 `_pinLocked` 欄位跟 `_onGlobePointerMove` 裡鎖 `_globePitch` 的那段判斷（拖曳恢復成跟第九次調整以前一樣，dx/dy 都直接套用，沒有任何鎖定）；`_onGlobePointerDown`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html)）最開頭加一行——只要 `flashId` 還在，手指一碰到球體（不管接下來是要拖曳、雙指縮放，還是點被標註的圓點本身）就立刻呼叫 `dismissFlash()`，比等 `document` 上的 `mousedown` 事件冒泡上來更早觸發，感覺上才夠「馬上」；`_onDocMouseDown` 也把第九次調整加的「點在 `[data-globe-surface]` 內不算點外面」那段整個拿掉，改回跟桌機版一樣「只要不是點在搜尋框內，就讓標註消失」；另外加了一個 `window` 的 `scroll` 監聽器（`componentDidMount()`），涵蓋「滑動螢幕」這個情境（目前版面理論上不太會真的觸發頁面捲動，但保留這個監聽器成本很低、以防萬一）。**跟第九次調整的差異**：第九次調整是「拖曳球體 = 保持標註」，這次是「拖曳球體 = 標註消失」，兩個版本完全相反，不要把第九次調整那節的 `_pinLocked` 說明誤認成目前狀態（該節已經在最前面加註記）。
4. **虛線的斷點改成漸進式**：使用者確認「虛線流動的動畫效果是對的，要保留」，但希望虛線的「形狀」（`stroke-dasharray`）從原本等距的固定虛線，改成「越靠近圓點的線段越短（趨近小圓點）、越往上靠近 icon 線段越長，最後 0.5cm 直接變實線銜接到 icon 底部」。**做法**（`tick()`）：`pathLength="1"` 讓整條線的座標系永遠是 0（圓點端，`x1/y1`）~1（icon 端，`x2/y2`），每影格重新算一次 `stroke-dasharray`——先算出「最後 0.5cm 實線」在目前這條線實際長度下對應的比例（`solidCapFrac = min(0.4, (CM_PX*0.5) / 目前線長px)`，用 `min` 夾住是避免線很短時實線段吃掉大半條線），剩下 `1 - solidCapFrac` 的比例分成 6 段（`dashWeights = [1, 1.6, 2.4, 3.4, 4.6, 6]`，權重線性遞增，代表段與段之間的相對長度比），每段之間固定夾一個等長的間隔（`gapWeight = 2`），全部依權重比例縮放成分數、串成一個 `[dash1, gap, dash2, gap, ...,dash6, gap, solidCap]` 的 dasharray 字串。**這個字串每影格都不一樣長（形狀）**，但 CSS 的 `dashFlow` 關鍵影格（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 的 `<style>` 區塊）繼續只動 `stroke-dashoffset`（位置），兩個是完全獨立的 SVG 屬性——「虛線持續流動」的動畫效果不受影響，只是流動的「花紋」從等距虛線變成漸進式的疏密。因為北極點釘住的節點理論上位置不太會變（閒置自轉只動 `_globeYaw`，不影響北極點），這個 dasharray 字串在標註成立、生長動畫跑完之後通常會穩定不再變化，每影格重算的效能成本可以忽略。
5. **地標 icon 縮小成原本的一半**：`ensureLandmark()` 裡 icon 的 `<svg>` 從 `width="30" height="38"` 改成 `width="15" height="19"`（`viewBox` 維持 `0 0 24 32` 不變，等比縮小），跟第 2 點的描邊減半疊加後，icon 整體視覺比原本明顯小、線條也明顯細很多。
   - **⚠️ 實作時踩到的一個 bug，記錄下來避免以後重踩**：第 1 點拿掉 `safeTop`（夾頁首下緣）之後，第一次隨手把下限改成寫死的 `8`（`endY = Math.max(startY - lineLen*eased, 8)`），Playwright 一測發現 icon 整個跑到螢幕外面去了（`getBoundingClientRect().top` 是負值）——原因是 `endY` 夾住的是**虛線終點／icon 的錨點**（icon 靠 `transform:translate(-50%,-100%)` 從錨點往上長出自己的高度），不是 icon 的頂端；下限只留 `8` 的話，icon 頂端 = `8 - icon高度(19)` = `-11`，直接跑到 y<0 的螢幕外。**修法**：下限要把 icon 自己的高度也算進去（`8 + pinIconHeight`，`pinIconHeight = 19`），這樣夾住的才是「icon 頂端跟螢幕最上緣至少留 8px」，不是「錨點跟螢幕最上緣留 8px」。這跟第七次調整「安全間距要把 icon 高度算進去，不能只夾錨點」踩過的坑是同一種錯誤，這次是拿掉 `safeTop` 重寫時又不小心漏算了一次——以後任何調整這段涉及 icon 位置夾限的地方，都要記得 icon 的視覺範圍比它的錨點還要再往上多 19px。

**驗證方式**：Playwright 測了以下情境並全部通過——(1) 地標 icon 的 `width`/`height` 屬性確實是 `15`/`19`，`path` 的 `stroke-width` 是 `1`，虛線的 `stroke-width` 是 `0.75`；(2) `dasharray` 字串裡的 dash 段長度確實逐段遞增（例如 `0.028, 0.045, 0.067, 0.095, 0.129, 0.168` 依序變大），最後一段是固定的實線比例；(3) icon／虛線的 `z-index` 是 `1010`，比頁首的 `1000` 高，在 icon 頂端座標用 `document.elementFromPoint()` 取樣，回傳的是 icon 自己的 `<svg>`，不是 `<header>`（沒有被頁首擋住/蓋住）；(4) 修正前面提到的「跑出螢幕外」bug 之後，icon 的 `getBoundingClientRect().top` 精確等於 `8`（下限生效，且是正值、在螢幕內）；(5) 標註成立後開始拖曳球體，開始拖曳後不到 150ms（遠早於 480ms 的完整淡出時間）地標的 `opacity` 就已經從 `1` 掉到 `0.1` 左右，確認是拖曳「一開始」就觸發淡出，不是拖曳完才觸發；(6) 標註成立後點擊頁首的漢堡選單區域，地標立刻完全消失（`opacity:0`、`display:none`）。桌機版（1440px）重新截圖比對，搜尋行為、地標 icon 元素數量（桌機恆為 0）都跟改動前一致。

### 球體節點依景深分兩層 DOM，讓置中標題看起來嵌在球心（2026-07-31 第十二次調整）

**症狀**：球體旋轉時，節點（圓點＋文字）轉過畫面正中央的「原型×未來 / Architecture + Structure」標題那一帶，看不出前後關係——不管節點目前是在球體前半（該蓋住標題）還是後半（該被標題蓋住），視覺上的疊圖順序都一樣，看不出「標題其實是嵌在球心，節點在球面上」這個設計概念。

**根本原因**：手機版 3D 球體是「一個 SVG 裡的一層平面 DOM」，116 個節點跟置中標題全部是同一層裡的兄弟元素，疊圖順序（誰畫在誰上面）完全由 DOM 順序決定、不會隨旋轉即時改變。原本的 DOM 順序是「全部連線 → 全部節點 → 標題」，標題永遠排在最後（畫在最上面），所以不管節點實際在球體前半還是後半，一律都被標題蓋住——這跟「後半節點被標題蓋住」的預期一致，但「前半節點應該蓋住標題」完全沒有做到。

**⚠️ 這是本來就知道的效能取捨，這次因為標題本身很大很顯眼才變得需要處理**：「效能取捨」那段筆記本來就寫著「沒有做依深度排序 DOM 順序這種完整的畫家演算法……背面節點理論上可能疊在正面節點的 DOM 順序前面，實測目視幾乎看不出來，先不處理」——這個取捨對「節點蓋節點」的情況確實不明顯（都是小圓點+小字），但標題是畫面正中央一大塊固定內容，節點跟它的前後關係一旦錯了非常顯眼，所以這次要處理，範圍限定在「節點 vs. 標題」，不含「節點 vs. 節點」（後者維持原本的效能取捨，不動）。

**做法**：使用者在「完整景深排序」跟「輕量視覺提示」兩個方案裡選了前者（工程量較大、效果最好）。核心概念是**同一個節點在 DOM 裡放兩份完全一樣的元素，一份放在標題*之前*、一份放在標題*之後*，同一時間只顯示其中一份（另一份 `display:none`）**，讓瀏覽器自己處理「這份沒被隱藏的，疊圖順序天生就對」：

1. **樣板**（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 的 `<svg viewBox="0 0 900 900">` 區塊）：桌機的節點 `sc-for`（原本唯一的一份）包進 `<sc-if value="{{ !isMobile }}">`，逐字保留、完全不變。另外新增兩份手機專用的 `sc-for`（都用 `<sc-if value="{{ isMobile }}">` 包住，讀同一個 `nodes` 陣列）：一份放在置中標題的 `<foreignObject>` **之前**（「背面層」，`<g data-node-back="{{ node.id }}">`），一份放在**之後**（「正面層」，`<g data-node-front="{{ node.id }}">`）。連線（`edges`）刻意**沒有**跟著拆——連線本來就很細、`opacity` 已經依深度淡化，使用者這次回報的是節點跟標題的關係，不是連線，拆連線會讓改動範圍/風險多一倍卻沒解決回報的問題，先不做。
2. **`renderVals()` 手機分支**：每個節點物件新增 `backDisplay`/`frontDisplay` 兩個欄位，依 `depthT <= 0.5`（球體前後半的分界——`depthT` 的定義是 `(z+1)/2`，`0.5` 剛好對應 `z=0`，也就是球體正對觀察者的赤道切面，物理意義完全站得住腳）決定其中一個是 `''`、另一個是 `'none'`。這個門檻**特意跟** `labelVisible` 用的 `depthT > 0.72`（文字只顯示最靠近正面那一小段）**不同**——一個是分「整顆球的前後半」，一個是分「多正面才要顯示文字」，兩件事各自獨立，不要合併成同一個門檻。
3. **`_ensureGlobeElCache()`**：原本每個節點只快取一份 DOM 元素（`group`/`hit`/`dot`/`labelWrap`/`fo`/`div`），改成快取 `{ back: {...}, front: {...} }` 兩份，分別用 `[data-node-back="id"]`／`[data-node-front="id"]` 查詢（跟桌機版沿用的 `[data-node="id"]` 是不同的選取器，兩邊互不干擾，桌機從頭到尾不會有 `data-node-back`/`data-node-front` 這兩個屬性）。
4. **`paintGlobe()`**：每影格對「背面層」「正面層」兩份 DOM 寫**完全一樣**的幾何/樣式屬性（位置、大小、透明度、文字方塊……），確保不管當下顯示哪一份，數字都是最新的，切換顯示的那一刻不會有「剛切過去、位置還沒更新」的閃爍；另外用當下的 `depthT<=0.5` 判斷，把其中一份的 `<g>` 設成 `display:none`、另一份設成可見——這個門檻要跟 `renderVals()` 用的 `0.5` 完全一致，否則旋轉時會在某個角度突然跳一下。
5. **`_visibleDot(id)`**（新增的輔助方法）：`tick()`（黃色亮點/地標 icon 跟著搜尋節點即時定位）、`startFlash()`（量測黃色亮點該多大）原本都是直接 `document.querySelector('[data-dot="id"]')` 抓節點目前的螢幕座標——現在同一個 id 在 DOM 裡有兩份，`querySelector` 只會抓到 DOM 順序在前的那份（背面層），如果搜尋到的節點當下正好在正面（背面層那份是 `display:none`），量到的會是一個尺寸全部是 0 的隱藏元素，黃色亮點/地標 icon 會全部定位到螢幕左上角 `(0,0)`。`_visibleDot(id)` 改成分別查 `[data-node-back="id"]`／`[data-node-front="id"]`，用 `getComputedStyle(...).display !== 'none'` 判斷哪一份才是實際顯示中的，回傳那一份裡的 `[data-dot]`；桌機版（兩個屬性都查不到）自動退回原本的 `document.querySelector('[data-dot="id"]')`，桌機行為不受影響。

   **⚠️ 實作時踩到的一個 bug，記錄下來避免以後重踩**：第一版把 `backDisplay`/`frontDisplay` 綁在 `<a href>` 外層（樣板初次渲染），但 `paintGlobe()` 每影格切換的是**內層** `<g data-node-back/front>` 的 `display`——這是跟第八次調整（`labelWrap` vs. `foreignObject`）**一模一樣的錯誤**：父層（`<a>`）的 `display:none` 只在初次渲染設定一次，之後 `paintGlobe()` 只改子層（`<g>`）的 `display`，子層改成可見也沒用，還是被父層蓋住。**症狀**：Playwright 測出搜尋定位後黃色亮點的座標卡在 `(0px, 0px)`、`opacity:0`，跟第八次調整「文字只有初次在正面的節點才會顯示」的症狀是同一種模式（父子層 display 各自為政）。**修法**：把 `display:{{ node.backDisplay/frontDisplay }}` 從 `<a>` 的 `style` 移到內層 `<g data-node-back/front>` 的 `style`，跟 `paintGlobe()` 實際更新的元素對齊。**教訓**：以後任何「用 display 切換某個元素可見度」的地方，樣板初次渲染綁的屬性，跟後續每影格程式碼實際更新的屬性，一定要是**同一個 DOM 元素**，兩邊選取器對不上這種 bug 目前已經在這個檔案裡出現兩次，都是同一種模式。

**驗證方式**：Playwright 測了以下情境並全部通過——(1) DOM 結構檢查：116 個節點在 DOM 裡各有一份 `data-node-back`、一份 `data-node-front`，任何時刻兩者剛好一個可見、一個隱藏（不會兩個都顯示、也不會兩個都隱藏）；(2) DOM 順序檢查：`data-node-back` 那份在標題 `foreignObject` 之前、`data-node-front` 那份在標題之後；(3) 拖曳旋轉一段時間後重新檢查 (1)，確認不變式在旋轉過程中持續成立，不是只有初次渲染那一刻對；(4) 搜尋定位後黃色亮點的螢幕座標是正常數字（不是卡在 `0px,0px`）、`opacity:1`；(5) 用真實的 `page.mouse.click()`（會走完整 `pointerdown→pointerup→click` 事件序列，不是 `element.click()`）點擊一個目前顯示中是「正面層」的可點擊節點，確認頁面轉場閃字動畫正確顯示該節點的名稱、最終導覽到正確網址。桌機版（1440px）重新截圖比對，確認 `data-node-back`/`data-node-front` 這兩個屬性桌機完全不存在（只有原本的 116 個 `data-node`）、搜尋行為不受影響。

**已知限制**：連線（`edgeData`）沒有跟著做景深排序（見上面「做法」第 1 點），理論上連線經過標題正中央那一小段時，前後關係還是會不準——這是刻意的範圍縮減，不是遺漏，如果之後使用者也回報連線的前後關係有問題，才需要比照節點的做法把連線也拆成兩層。另外，「節點 vs. 節點」的前後關係依然沒有真正排序（維持原本的效能取捨，只是透明度隨深度淡化），只有「節點 vs. 標題」這一組關係被修正了。

### 搜尋列自動收合、虛線改成三段式「飛碟降落」效果（2026-07-31 第十三次調整）

**地標 icon「被切一角」的真正原因**：第十一次調整把地標的 z-index 拉到比頁首高，理論上不該再被任何東西擋住；但使用者這次自己抓出真正原因——**手機搜尋列**（`showMobileSearchBar` 區塊，`[data-search-box="1"]`，展開時是 `position:fixed; top:0; left:0; right:0` 貼齊頁面頂端的一整條，約 68px 高）選了搜尋結果之後，**原本會一直留在畫面上**（`goToNode()` 只清空了 `searchResults`，沒有把搜尋列本身收起來），釘在北極點（畫面最上方）的地標 icon 跟這條搜尋列剛好疊在同一塊區域。用 Playwright 驗證過：地標的 z-index（1010）確實比搜尋列（1001）高，`elementFromPoint()` 在地標的位置量到的也真的是地標自己，不是搜尋列——**技術上沒有真的被裁切/蓋住**，但兩個元素緊貼在同一塊窄窄的頂部區域，視覺上就是很擠、像被卡住/擋住的觀感。

**修法**：`goToNode()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）選定搜尋結果、手機版分支執行時，額外把 `showMobileSearch` 重設成 `false`（連同 `searchInput` 一併清空），讓搜尋列自動收合——選到結果代表這次搜尋已經結束，直接把整條搜尋列收掉，畫面最上方完全讓給地標，兩者不再有機會疊在一起。這個 state 只在手機版有意義（`showMobileSearchBar = isMobile && this.state.showMobileSearch`），桌機的搜尋框走的是另一組完全不相關的 `searchBoxDisplay`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) `renderVals()`），不受這次改動影響。**驗證時的陷阱**：桌機/手機的搜尋框外層都用同一個 `data-search-box="1"` 屬性，直接用 `document.querySelector('[data-search-box="1"]')` 判斷「搜尋列有沒有關閉」會誤判——桌機那個版本（`position:absolute`、置中在頁首正中央那個）不管手機版收合與否都一直存在於 DOM 裡（只是手機視窗下靠 `searchBoxDisplay` 設成 `none`），要用 `getComputedStyle(...).position === 'fixed'` 之類更精確的條件，才篩得出真正的「手機展開搜尋列」那一個。

**虛線改成三段式，做出「飛碟降落」的視覺**（`tick()`）：使用者要求虛線不要整條都用同一套漸進規則，改成三段——

1. **最下方 1cm（貼著圓點/地面那一段）**：固定用整條線裡最短的虛線單位，重複排列、不漸變——像降落點的一排小光點。
2. **中段（1cm 之後，到 icon 下方 1cm 之前）**：延續第九次調整的漸進式規則，虛線從跟第 1 段一樣短的單位開始，逐段變長——像光束從地面往上漸漸凝聚變粗，這段的最小單位刻意跟第 1 段用同一個尺寸，兩段交界處才不會有明顯的縫。
3. **最上方 1cm（icon 正下方）**：直接變實線銜接到 icon 底部——像光束在飛船（icon）這端已經完全成形（原本是 0.5cm，這次跟第 1 段對齊也改成 1cm）。

三段都是用 `CM_PX`（1cm 換算成 px）依目前這條線實際的像素長度重新分配成 `pathLength` 的 0~1 比例，不管線多長/多短都是同一種效果；`bottomFrac`/`topSolidFrac` 各自夾在 `0.3` 以內，避免線很短時三段互相打架吃光彼此的空間。CSS 的 `dashFlow` 關鍵影格仍然只動 `stroke-dashoffset`，跟這裡每影格重算的 `stroke-dasharray`（形狀）是兩個獨立屬性——「虛線持續流動」的動畫效果不受影響，使用者也明確說這部分是對的、要保留，只是流動的「花紋」從單純漸進式再細分成三段。

**驗證方式**：Playwright 測了——(1) 搜尋定位後，`showMobileSearchBar` 那個 `position:fixed` 的搜尋列元素確實不在 DOM 裡了（用位置/是否有 input 子元素判斷，不是單純查 `data-search-box`，避免誤判到桌機那份）；(2) 解析出來的 `stroke-dasharray` 數字序列：開頭連續 7 組 `dash/gap` 數值完全相同（貼地的固定短虛線段），接著逐漸變大（中段漸進），最後一個數字明顯大很多（貼機身的實線段）。桌機版（1440px）重新截圖比對，搜尋行為、地標 icon 元素數量（桌機恆為 0）都跟改動前一致。

### 手機版頁尾收進一個 Contact 按鈕（2026-07-31 第十四次調整）

**動機**：桌機版頁尾是三欄並排（電話/傳真/電郵、地址、社群連結），手機版原本直接沿用同一份內容改成單欄堆疊，會佔掉一大截垂直空間。使用者要求手機版把全部頁尾資訊收進一個 `Contact` 按鈕，點了才展開，減少頁尾佔用的畫面空間。

**做法**：`<footer>` 整個拆成兩份，比照第十二次調整（球體節點景深排序）用過的「桌機/手機各自一份 `sc-if`」模式：

- **桌機/平板**（`<sc-if value="{{ !isMobile }}">`）：逐字保留原本的三欄 `<footer>`，一個字都沒改。
- **手機版**（`<sc-if value="{{ isMobile }}">`）：新的 `<footer>` 只放一個 `Contact` 按鈕（信封 icon + 文字 + 展開箭頭），按鈕 `onClick="{{ toggleFooterContact }}"` 切換新增的 `showFooterContact` state；按鈕下面用 `<sc-if value="{{ showFooterContact }}">` 包住電話/傳真/電郵/地址四行文字＋三個社群圖示（內容跟桌機版三欄的資訊完全一樣，只是排版改成置中單欄），沒展開時這整塊不會出現在 DOM 裡，頁尾就只剩按鈕那一條。展開箭頭用 `transform:rotate({{ footerContactChevronDeg }}deg)` 在收合／展開時 180 度翻轉，`footerContactChevronDeg` 是 `renderVals()` 依 `state.showFooterContact` 算出的 `0`/`180`。

**⚠️ 實作時踩到的兩個 bug，記錄下來避免以後重踩**：

1. **按鈕點了完全沒反應**：第一版寫完 `onClick="{{ toggleFooterContact }}"`，Playwright 一測發現點擊後 `showFooterContact` 根本沒有切換、`sc-if` 包住的內容也沒有出現。原因是這個檔案的 DC 模板存取 class 方法的方式，不是「模板可以直接呼叫 `this.任何方法`」，而是要**在 `renderVals()` 的回傳物件裡明確列出每一個要給樣板用的方法**（現有的 `toggleDarkMode`/`toggleMaterialMenu`/`toggleMobileSearch` 等等全部都是這樣，各自在回傳物件裡包成 `toggleDarkMode: () => this.toggleDarkMode()` 這種形式）——新加的 `toggleFooterContact()` 方法本身寫對了，但忘記把它也加進 `renderVals()` 的回傳物件，樣板裡的 `{{ toggleFooterContact }}` 解析出來是 `undefined`，`onClick` 自然什麼都不會發生，而且**不會報錯**（跟三元運算子靜默失效是同一種「樣板寫錯但沒有任何錯誤訊息」的陷阱，只是原因不同）。**修法**：`renderVals()` 回傳物件裡補上 `showFooterContact: this.state.showFooterContact` 跟 `toggleFooterContact: () => this.toggleFooterContact()`。**教訓**：以後在樣板裡新增任何 `onClick`/`onInput` 等事件屬性指向的方法，都要記得回頭檢查 `renderVals()` 的回傳物件有沒有把這個方法也列進去，光是定義了 class 方法本身是不夠的。
2. **顏色三元運算子**：新增的手機版頁尾一開始圖省事直接照抄桌機版舊有的寫法 `{{ isDarkMode ? 'oklch(0.7 0 0)' : 'oklch(0.4 0 0)' }}`（桌機版那幾處本來就是「已知但還沒修的違規寫法」，見「已知的資料相依細節」那節的說明），因為 `{{ }}` 不支援三元運算子，這樣寫顏色會靜默失效。這次寫新程式碼就沒有照抄舊的違規寫法，改成在 `renderVals()` 裡新增 `footerContactBorderColor`／`footerMutedColor`／`footerSocialBg` 三個依 `isDarkMode` 算好的單純變數（`textColor`／`bgColor` 這兩個原本就有的變數也可以直接重複使用，不用再另外算一次），樣板裡全部改成 `{{ footerMutedColor }}` 這種單純變數寫法。

**驗證方式**：Playwright 測了——(1) 手機版頁尾初始只有按鈕、DOM 裡沒有任何 `<p>`；(2) 點按鈕後 DOM 裡出現 4 行文字（電話/傳真/電郵/地址）跟 3 個社群連結，`mailto:` 連結網址正確；(3) 再點一次按鈕，內容整個從 DOM 消失，回到只有按鈕的狀態；(4) 切到深色模式後展開，按鈕邊框色/文字色、內文顏色、頁尾背景色全部都是有效的 `oklch(...)` 值（不是空字串或瀏覽器預設黑色，確認顏色沒有因為誤用三元運算子而靜默失效）。桌機版（1440px）重新截圖比對，確認頁尾維持原本三欄樣式、沒有 `Contact` 按鈕、`<footer>` 元素數量是 1（不是桌機/手機各一個同時存在）。

### 手機版球體其實沒有真的垂直置中、地標 icon 重新限制在頁首下方（2026-07-31 第十五次調整）

**症狀**：使用者回報「目前主視覺位置偏高」——手機版球體看起來偏向畫面上半部，下面（頁尾上方）留了一大截空白。

**根本原因**：`data-globe-surface`（裁切框，見第七次調整）手機版原本是 `height:100%`，理論上應該撐滿 `#works` 的全部可用高度（頁首下緣到頁尾上緣）。但 `#works` 是 `display:flex`（預設 `flex-direction:row`），`data-globe-surface` 是它唯一的 flex item——**這個 `height:100%` 在這層巢狀 flex 結構裡會靜默解析失敗**：用 Playwright 直接量測 `getBoundingClientRect()` 確認過，`#works` 本身確實有明確的算出高度（一個 390×844 的手機視窗量到 732px），但 `data-globe-surface` 套用 `height:100%` 之後量到的高度只有 363px 左右——不是 732px 減掉上邊距後的 636px，而是幾乎剛好等於**裁切框內層那個正方形視覺球體自己的邊長**。換句話說，`height:100%` 沒有真的解析成百分比，瀏覽器退回用「內容自己撐開的高度」決定裁切框大小，裁切框沒有撐滿 `#works`，球體因此整個往上偏、下面留一大截空白。這不是視覺錯覺，是真的量得出來的版面 bug，只是原本因為裁切框本身透明（看不出邊界），加上球體大致還在合理範圍內，一直沒被注意到。

**驗證排查過程**（記錄下來，以後遇到類似「元素該撐滿卻沒撐滿」的情況可以參考同一套排查方法）：直接用 `element.style.setProperty('height', '636px', 'important')` 硬改成明確的 px 值，確認裁切框馬上正確撐滿到頁尾——證明問題就是出在百分比本身解析失敗，不是別的因素（例如 aspect-ratio、overflow 之類）搗亂。接著測試把 `height` 整個拿掉（`removeProperty('height')`）或改成 `height:auto`，兩種都一樣正確撐滿 636px——因為 `#works` 是 flex 容器、`align-items` 沒特別設定時預設是 `normal`（在 flex 情境下等同 `stretch`），`data-globe-surface` 只要不主動指定一個「解析失敗的」百分比高度，就會被 flex 的預設 stretch 行為自動撐滿，不需要百分比運算。

**修法**：`renderVals()` 的 `globeSurfaceHeight` 從 `isMobile ? '100%' : 'auto'` 改成固定 `'auto'`（桌機原本就是 `'auto'`，這次手機/桌機統一用同一個值，三元判斷直接拿掉）。改完之後裁切框重新精確撐滿頁首下緣到頁尾上緣（Playwright 量測 `gapBelowSurfaceToFooter` 精確等於 `0`），球體置中在這個裁切框正中央，視覺上也確實回到畫面中段。

**地標 icon 重新限制在頁首下方**：第十一次調整曾經把「夾頁首下緣」的邏輯整個拿掉，改成只留一個「螢幕最上緣」的下限（理由是 z-index 已經比頁首高，不會再被「擋住」）。這次使用者進一步要求「確保地標icon的高度低於頁首的下緣」——不是疊圖順序的問題，是**位置本身**就不應該跑到頁首那一段去，就算視覺上疊圖順序是對的也不行。`tick()` 重新加回夾頁首下緣的邏輯：`headerBottomY`（`document.querySelector('header').getBoundingClientRect().bottom`）加上 icon 自己的高度（19px）再加 8px 緩衝，跟原本就有的「螢幕最上緣」下限（`8 + pinIconHeight`）取兩者較嚴格的 `Math.max`——正常情況下頁首下緣這條線一定比較嚴格（頁首在螢幕上緣附近，會先被夾到），螢幕上緣那條理論上用不到，純粹當防呆保留。z-index 拉到 `1010` 這件事本身沒有改回去（不影響正確性、也還是有用——萬一之後又有其他 UI 元素疊在附近，至少不會被那個元素的疊圖順序蓋住），只是不再是唯一防線。

**驗證方式**：Playwright 測了——(1) 裁切框（`data-globe-surface`）高度精確等於 636px（`#works` 扣掉頁首留白後的可用高度），跟頁尾的間距精確是 `0`；(2) 裁切框內視覺球體的螢幕中心 Y 座標跟整個 viewport 的中心 Y 座標非常接近（誤差在個位數 px，因為頁首 96px 跟頁尾收合後的 112px 高度本來就不完全相等，裁切框是精確置中在「頁首下緣到頁尾上緣」這段可用空間的正中央，不是無視頁首/頁尾直接置中整個 viewport，這是預期內的微小差異）；(3) 搜尋定位後量測地標 icon 的頂端座標，確認 `pinTop >= headerBottom`（icon 確實留在頁首下方，沒有跑進頁首範圍）。桌機版（1440px）重新截圖比對，`globeSurfaceHeight` 桌機本來就是 `'auto'`、這次沒有變動，畫面逐字未變。

### 頁首新增手機/電腦版手動切換鈕（2026-08-02 第十六次調整）

**動機**：手機/桌機版的判斷從一開始就只看 `window.innerWidth`（視窗的 CSS px 寬度）——寬度 `< 768` 就是手機版，`>= 768` 就是桌機版，純粹是「視窗多寬」，跟「這是什麼裝置、用什麼方式操作」完全無關。使用者指出這樣不夠精準（例如手機橫放時寬度常常超過 768px 會被誤判成桌機版；桌機瀏覽器縮小視窗也會被誤判成手機版），要求加一個手動切換鈕，讓使用者可以直接指定要看哪個版本，不用被畫面寬度綁死。

**做法**：新增一個 `forcedMode` state（預設 `null`），跟一個統一的判斷方法：

```js
_effectiveIsMobile() {
  if (this.state.forcedMode != null) return this.state.forcedMode === 'mobile';
  return this.state.viewportW < 768;
}

toggleDeviceMode() {
  const next = this._effectiveIsMobile() ? 'desktop' : 'mobile';
  this.setState({ forcedMode: next });
}
```

`forcedMode` 是 `null` 時完全比照原本的行為（單純依畫面寬度判斷）；使用者按過切換鈕之後變成 `'mobile'` 或 `'desktop'`，之後不管視窗多寬都固定用這個手動選擇的模式，直到再按一次切換鈕。`forcedMode` 只存在 React state（instance 記憶體）裡，**不會寫進 localStorage，重新整理頁面就會重置回依畫面寬度判斷**——這是刻意的，沒有實作「記住使用者上次選擇」，如果之後有人想加這個功能，要另外評估要不要用 localStorage（要考慮多分頁同步、隱私模式等情境）。

**⚠️ 這是最容易漏改的地方，也是這次實作時真的踩到的問題**：`isMobile` 這個判斷散落在檔案裡至少 8 個地方——`componentDidMount()` 的 `scroll`/`pointerdown` 監聽器、`goToNode()`、`tick()`（注意這裡原本用的是 `window.innerWidth`，不是 `this.state.viewportW`，是另一種寫法）、`globeTick()`、`paintGlobe()`、`renderVals()`——**全部都要改成呼叫 `this._effectiveIsMobile()`，只改 `renderVals()` 那一處是不夠的**。只改樣板會出現「畫面看起來已經手動切成手機版了（3D 球體、收合頁尾都出現了），但拖曳球體沒反應、搜尋定位跑錯、閒置自轉不會動」這種樣板跟互動邏輯各自為政的狀況——因為這些互動邏輯自己另外判斷了一次 `viewportW < 768`，沒有跟著手動覆寫走。**以後新增任何跟手機/桌機分流有關的程式碼，一律呼叫 `_effectiveIsMobile()`，絕對不要再手動寫 `viewportW < 768` 這種比較。**

**UI／按鈕位置**：新按鈕放在頁首導覽 pill 裡，「Contact」（桌機版最後一個連結）／搜尋圖示（手機版）跟深淺模式切換鈕之間（[建築事務所首頁.dc.html:166-193](./建築事務所首頁.dc.html) 附近）——因為桌機/手機共用同一個 nav pill 容器（內部用 `sc-if` 切換 Team/Film/Contact 連結 vs. 搜尋圖示），新按鈕只需要寫一次，會自動出現在兩種版面的同一個相對位置，不用另外複製一份手機專屬的按鈕。**icon 顯示的是「按下去會切換成的模式」，不是目前所在的模式**——目前是桌機版就顯示手機圖示，目前是手機版就顯示電腦圖示（`sc-if value="{{ !isMobile }}"` 顯示手機 icon，`sc-if value="{{ isMobile }}"` 顯示電腦 icon），符合一般「這顆按鈕按下去會發生什麼事」的圖示慣例，不是「這是現在的狀態」。

**已知的取捨（不是 bug）**：手動切成手機版之後，球體容器的尺寸公式（`diagramContainerWidth: min(92vw, 88vh)`）還是依照**目前實際的視窗尺寸**算，不是模擬一個真正的手機螢幕大小——所以在很寬的桌機視窗上強制切成手機版，球體會照桌機視窗的實際寬高比例放大，不會變成「看起來像在一支手機上」那樣的等比例縮小畫面。這是刻意的（純粹套用手機版的*互動邏輯與版面規則*到目前的視窗尺寸，不是做一個完整的裝置模擬器），效果類似瀏覽器開發者工具的「行動裝置模擬」但沒有真的改變 viewport 尺寸。同樣地，強制切成桌機版塞進一個很窄的手機視窗，桌機版的搜尋框、Team/Film/Contact 連結會被擠出可視範圍、需要橫向捲動才看得到——這也是預期內的結果，桌機版面本來就不是為窄螢幕設計的，使用者手動選了「看桌機版」，理論上就是要接受這個代價。

**驗證方式**：Playwright 測了以下情境並全部通過——(1) 390px 寬視窗（原本自動判定手機版）點擊切換鈕後，畫面實際切成桌機版（3D 球體消失、Team/Film/Contact 連結出現、頁尾收合按鈕消失），圖示也正確變成手機圖示（下一步按下去會切回手機版）；(2) 再點一次切回手機版，畫面正確切回；(3) 1440px 寬視窗（原本自動判定桌機版）點擊切換鈕後，畫面實際切成手機版（3D 球體出現、頁尾收合成 Contact 按鈕），圖示正確變成電腦圖示。三種情境都是直接檢查 DOM 結構（有沒有 `data-node-back`／頁尾按鈕／Team 連結）確認畫面真的整套切換過去，不是只檢查一兩個屬性。

### 切換模式多次後球體卡住不會轉（`_globeElCache` 沒有在模式切換時失效，2026-08-02 第十七次調整）

**症狀**：使用者回報「在多次切換後手機版的球轉動會卡住」，並補充「準確說是球上的圓點會繼續轉，但是標題文字卻不會跟著轉」。

**根本原因**：手機版 3D 球體每個節點在 DOM 裡的 `<g data-node-back="id">`／`<g data-node-front="id">`（見第十二次調整的景深排序）**只在 `isMobile` 為真時才存在**（樣板用 `<sc-if value="{{ isMobile }}">` 包住）。用 Playwright 直接驗證過：切到桌機版時，這些節點元素會整個從 DOM 移除；切回手機版時，會產生**全新的 DOM 元素**（用一個自訂 marker 屬性測試過：切換前標記在某個節點上的 marker，切換回來之後量不到了，證明不是同一批元素被重複使用，是貨真價實的卸載又重新掛載）。

問題出在 `_ensureGlobeElCache()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）——這個方法把「節點 id → DOM 元素」的對照表快取在 `this._globeElCache`，**而且只在第一次呼叫時建立，之後永遠直接回傳快取、不會重新查詢**（`if (this._globeElCache) return this._globeElCache;`）。這個設計本身沒問題（見該方法上面的註解：`sc-for` 用陣列索引當 React key，只要陣列長度/順序不變，元素在整個頁面生命週期不會被換掉，快取起來是安全的）——**但這個前提在「手機/桌機模式可以切換」之後不成立了**：模式一切換，整批節點元素會被整個卸載重新掛載，快取裡存的還是切換前那批已經從 DOM 移除的舊元素。之後 `paintGlobe()`（每影格驅動旋轉動畫的方法）繼續埋頭更新這些已經不在畫面上的舊元素——這些更新完全不會反映到畫面上，因為畫面上真正顯示的是模式切換後新掛載的那批元素，從掛載那一刻起就再也沒被更新過，永遠停在 `renderVals()` 初次渲染算出來的位置。

**為什麼使用者形容成「圓點會轉、標題文字不會跟著轉」**：這只是使用者對同一個根本問題的觀察角度——實際上是「畫面上顯示的節點（含圓點跟文字）整批全部凍結在切換模式那一刻的位置，不會再更新」，不是「圓點在轉、文字沒跟上」這種局部/選擇性的問題。**測試這個 bug 時有一個很重要的陷阱，記錄下來避免以後誤判成「沒有 bug」**：如果用 Playwright 的 `page.mouse.move()`（滑鼠事件）模擬拖曳，途中滑鼠游標掃過其他節點的隱形觸控熱區時，會連帶觸發 `onMouseEnter`/`onMouseLeave`（`setHover()`/`clearHover()`），這兩個方法會呼叫 `setState()`——這會觸發一次完整的 React 重新渲染，`renderVals()` 用當下最新的 `_globeYaw`/`_globePitch` 重新算一次幾何、直接寫回 DOM，**意外地把凍結的畫面「修好」一次**，讓 bug 看起來沒有重現。**必須改用真正的觸控事件（`pointerType:'touch'` 的 PointerEvent，不會觸發滑鼠 hover 事件）才能穩定重現**，這也更貼近真實手機使用情境（觸控操作本來就不會觸發 `mouseenter`/`mouseleave`）。

**這其實是比手動切換鈕更早就存在的既有 bug**：只要視窗寬度跨過 768px 斷點（例如直接拖曳桌機瀏覽器視窗邊緣），`isMobile` 就會透過 `resize` 事件間接改變，觸發同一種卸載/重新掛載，一樣會讓 `_globeElCache` 失效——只是這個操作路徑本來就少有人會在日常測試時做，加上前面提到的「用滑鼠測試會意外自我修復」陷阱，一直沒被抓到。第十六次調整加的手動切換鈕讓「模式切換」變成點一下按鈕就能重複觸發的操作，這次才真正暴露出來。

**修法**：`_globeElCache` 只要在「有效 isMobile 判斷可能真的改變」的兩個地方主動清成 `null`，讓 `_ensureGlobeElCache()` 下次呼叫時重新查詢，抓到新掛載的元素：

1. `toggleDeviceMode()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）：呼叫 `setState()` 之前先 `this._globeElCache = null;`。
2. `componentDidMount()` 裡的 `_onResize`（debounce 後的 callback，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）：在真正呼叫 `setState({ viewportW: ... })` 之前，先用「切換前」跟「即將變成的」`_effectiveIsMobile()` 結果比較，兩者不一樣才清快取——避免每次 resize（就算沒跨過斷點）都白白清一次快取。

**驗證方式**：Playwright 測了以下情境並全部通過——(1) 連續切換模式 2 次、4 次、8 次（確保奇數/偶數次都測到，涵蓋「切回手機版」跟「切到桌機版又切回來」兩種情境），每次都改用純觸控型 `PointerEvent`（`pointerType:'touch'`）直接對 `[data-globe-surface]` `dispatchEvent`，避開滑鼠 hover 的意外自我修復；(2) 切換後量測全部 116 個節點的圓點座標跟文字方塊座標，拖曳前後兩者都正確改變（修正前是全部 116 個節點都完全不動）；(3) 切換後用真實的 `page.mouse.click()` 點擊一個節點，確認頁面轉場閃字動畫正確觸發、顯示正確的節點名稱。桌機版（1440px，全程沒有觸發手機/桌機切換）重新測過搜尋跟截圖比對，確認沒有受到這次改動影響。

### 「原型建築」節點改用循環版前導動畫、等外部子頁載入完成才跳轉（2026-08-17 第三十七次調整）

**背景**：首頁點節點連到子頁時，本來就有一個「前導動畫」機制（見下面「PAGE TRANSITION PRELOADER」相關段落／[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 檔案結尾的 `<script>` 區塊）：點擊會攔截原生連結行為，蓋一層黑底遮罩、跳字跑過 `WORD_POOL`（各節點大標題）最後停在目的頁標題，停留 `LANDING_HOLD_MS`（550ms）後才真正 `window.location.href` 跳轉。但這個點擊判斷式（`document.addEventListener('click', ...)`）只認 `a[href$=".dc.html"]` 且明確排除 `/^https?:\/\//i` 開頭的連結——`r109`「原型建築」的 `href` 是外部網址 `https://www.as-structure.com/原型建築`，完全沒有走這條動畫路徑，點下去就是瀏覽器原生立即換頁。使用者要求「在跳轉到原型建築子網頁時也加入進入主網頁的動畫，因為這個子網頁檔案比較大，這個動畫需要重複撥放直至子網頁檔案加載完成後再進入網頁」——不只是「也要有動畫」，還要求動畫時長要跟著目的頁實際載入時間走，而不是像現有機制那樣播完固定跳字序列就直接跳頁、完全不管目的頁到底載入完成了沒。

**做法**：

1. 新增 `EXTERNAL_PRELOAD_NODE_IDS = ['r109']`：用節點 id（不是寫死網址字串）決定要不要走新路徑，這樣以後 `r109` 的 `href` 再被同步/修改（例如前兩次調整那種 `站存.txt` 同步）也不用跟著改這段程式碼。
2. 點擊判斷式從 `a[href$=".dc.html"]` 放寬成 `a[href]`，攔截到之後才用 `a.querySelector('[data-node]')` 讀出節點 id，判斷是 `isExternalPreload`（id 在 `EXTERNAL_PRELOAD_NODE_IDS` 裡）還是 `isLocalSubpage`（`href` 是本機 `.dc.html`、不是 `http(s)://` 開頭），兩者都不是才放行原生行為（例如未來如果又出現其他外部連結、但不需要這個處理的節點）。
3. 新增 `playAndPreloadThenNavigate()`，跟既有 `playAndNavigate()`（本機子頁用，維持不變）並存：
   - 跳字改成 `flickerLoop()` 無限循環（`i % sequence.length`），不會像 `playAndNavigate()` 的 `flicker()` 那樣播完 `WORD_POOL.concat([destinationWord])` 就停。
   - 背景建立一個隱藏 `<iframe>`（`position:fixed; left:-9999px` 移出畫面外、`opacity:0`，掛在遮罩 `overlay` 底下而不是 `document.body`，這樣 bfcache 還原時 `overlay.remove()` 會一併把它清掉）指向目的網址，監聽它的 `load` 事件——**跨網域的 iframe 讀不到裡面的內容（CORS），但 `load` 事件本身讀得到，足夠拿來判斷「目的頁的文件跟它自己的資源都載入完了」**，不需要目的網站配合做任何事。
   - `load` 事件觸發（或 20 秒安全逾時 `EXTERNAL_PRELOAD_TIMEOUT_MS` 觸發，兩者都會呼叫同一個 `finish()`，用 `finished` 旗標防重複執行）才停止跳字循環、停在目的地文字、停留 `LANDING_HOLD_MS` 後才真正 `window.location.href` 跳轉——這時瀏覽器多半已經把目的頁的資源快取過一輪（iframe 剛載入過），實際換頁通常會比沒有這層預載入快，體感上「動畫播完＝進頁面幾乎是同一瞬間」。
   - 20 秒安全逾時是必要的：如果目的網站設定 `X-Frame-Options`/CSP `frame-ancestors` 擋掉被嵌入 iframe，`load` 事件可能永遠不觸發（或觸發但內容是被擋掉的錯誤頁，行為不保證），沒有逾時保險的話使用者會永遠卡在跳字動畫裡出不去——這個保險模式直接比照開場影片遮罩既有的做法（`sc-intro-overlay`／`setTimeout(reveal, 15000)`，見上面「開場影片載入動畫」）。
4. `pageshow`（bfcache 還原）的既有清理邏輯也一併擴充：原本只清 `overlay`／`navigating`，現在還要清 `activeLoopTimer`／`activeSafetyTimer`／`activePreloadFrame` 這三個新的進行中狀態——因為這條新路徑的跳字是無限循環、逾時保險長達 20 秒，中途被瀏覽器 bfcache 凍結（例如使用者在動畫播放中途按上一頁）再還原的機率，比原本 `playAndNavigate()` 那種一兩秒內就會呼叫 `window.location.href` 的短暫路徑高很多；沒清掉的話計時器/iframe `load` 事件還原後會繼續跑，可能在使用者已經按過上一頁、明明看到首頁了之後，卻又被強制導去外部網址。

**驗證方式**：Playwright 用 `page.route()` 攔截 `https://www.as-structure.com/**` 換成本機回應的假頁面（沙盒環境沒有對外網路，沒辦法連到真正的正式站），點擊 `r109` 節點後確認：(1) 沒有原生立即換頁，而是先出現跳字遮罩；(2) 最終確實導向到攔截到的目的網址（不是卡住不動）；(3) 整段耗時明顯短於「跑完整個 `WORD_POOL` 固定序列」所需的時間（因為假頁面回應幾乎是瞬間，`load` 事件很快觸發），證明真的是靠 `load` 事件驅動結束、不是誤觸發到舊的固定序列路徑；(4) 全程 console 沒有新增錯誤。另外點一個本機子頁節點（`r2` 原型教學）確認完全沒有受影響、還是走原本 `playAndNavigate()` 的固定序列，正常導到 `./原型教學.dc.html`。

### 分類大標節點的 href 換成本機子頁「原型方法.dc.html」（2026-08-17 第三十六次調整）

**背景**：上一次調整（第三十五次）比對並套用了 `站存.txt` 之後，使用者又直接對同一份 `站存.txt` 貼了一次「同步這個程式碼」，代表編輯器那邊又有新進度上線。這次直接重跑一次完整檔案 diff（`diff -u 站存.txt 建築事務所首頁.dc.html`），確認實際差異只剩：

- 4 個分類大標節點（`r1` 章 architecture design、`r28` 成 construction、`r59` 法 structure data、`r100` 構technics）的 `href`，`站存.txt` 已經改成 `./原型方法.dc.html`，主網頁還是舊的 `https://www.mashup.com.tw/as%20studio/?page=about`。
- 純格式差異：這次 `站存.txt` 貼出來的內容裡，`languages =`／`nodeData =`／`edgeData =` 後面少了一個換行（陣列第一個元素直接接在同一行），跟主網頁已有的排版風格不同，屬於貼上時的格式問題、不是內容差異，沒有套用。
- `r109`（原型建築）的 `href`：`站存.txt` 這次仍然是舊的 `https://as-studio001.github.io/asarch/`，主網頁維持第三十五次調整時保留的 `https://www.as-structure.com/...`，這次同樣沒有跟著退回。

套用前先確認 `原型方法.dc.html` 這個檔案真的存在於同一個目錄（`ls` 確認過），排除掉是筆誤/還沒建立的可能，才動手改。

**做法**：4 個節點的 `href` 各自用 `Edit` 精確替換（避免整段覆蓋動到其他沒變的欄位），主網頁跟編輯器（`圓點位置編輯器.html`）的對應節點都改了——編輯器原本就偏好本機子頁勝過外部 `mashup.com.tw` 連結（例如 `r23` 已經是 `./原型結構.dc.html`），這次 4 筆改成本機頁面跟編輯器既有的方向一致，不算引入新的不一致。

**驗證方式**：改完用腳本檢查主網頁/編輯器兩邊的 `nodeData`/`NODE_DATA`、`edgeData`/`EDGE_DATA` 都是 78 筆節點、無重複 id、無懸空連線；Playwright 對主網頁桌機寬度（1440px）截取這 4 個節點對應的 `<a>` 元素 `href` 屬性，確認全部都變成 `./原型方法.dc.html`，console 沒有新增錯誤。

### 新增節點「信義街咾咕石‧芳宅」（2026-08-18 第三十九次調整）

**背景**：`站存.txt` 第三次更新，新增 `r135`「信義街咾咕石‧芳宅」一個節點，使用者要求「更新程式碼」。這筆資料跟既有 `r114`「2025台南咾咕石芳宅」名稱相近，但 `id` 不同、`href`（`?page=product_shop&p_id=622507`）也不同，判斷是使用者刻意新增的獨立節點（同一個實際案子的不同呈現方式，或確實是兩個不同案子），不是要覆蓋/合併既有節點——照 `站存.txt` 給的資料原樣同步，沒有自作主張去重。

**做法**：沿用上一次調整修好的同步腳本（見上面「新增 17 個節點...」一節的詳細說明：bracket 深度掃描抓 `nodeData`／`edgeData` 陣列邊界，逐項用同樣方式切分，取代時用精確字元位置 `slice()` 而不是 `String.prototype.replace()`，避免特殊替換樣式的地雷；套用前先寫暫存檔驗證過再覆蓋正式檔案）。`r109` 的 `href` 依照前幾次的判斷繼續保留主網頁較新的 `as-structure.com` 網址。

**驗證方式**：暫存檔驗證（節點數、有無重複 id、有無懸空連線、id 順序跟 `站存.txt` 是否完全一致）通過後才覆蓋正式檔案；Playwright 確認桌機（1440px）與手機（390px）都正確渲染 96 個節點，console 無錯誤。

### 新增 17 個節點、以及一次「差點寫壞主網頁」的同步腳本 bug 與修法（2026-08-18 第三十八次調整）

**背景**：使用者這次直接在 `站存.txt` 裡新增了一批節點（案例出自 [as-structure.com/原型結構](https://www.as-structure.com/%E5%8E%9F%E5%9E%8B%E7%B5%90%E6%A7%8B) 官網作品目錄，在前一個回合已經先用 WebFetch 逐一核對過官網 8 個分類、找出跟當時主網頁比對後缺漏的案子），要求「同步程式碼」。跟前兩次（第三十五、三十六次）比，這次差異的量體明顯更大：新增 17 個節點、外加一個既有節點區塊的大搬移（`r108`／`r116`／`r109`／`r110`／`r111`／`r112`／`r113`／`r114`／`r1`／`r2` 從陣列尾端搬回最前面）。

**同步腳本 bug 與復原經過**：延續前兩次的做法，改用 Node.js 腳本（而非手動 `Edit`）處理，因為改動範圍太大不適合逐段手動比對。第一版腳本用 `mainTxt.match(/nodeData = \[[\s\S]*?\n  \];/)` 這種「非貪婪比對到 `\n  \];`」的正規表示式，去抓 `站存.txt` 裡 `nodeData`／`edgeData` 陣列的完整內容——這個假設在前兩次都成立，但 `站存.txt` 這次貼出來的陣列結尾是 `}];`（直接黏在最後一個項目後面），不是預期的「換行＋兩個空格＋`];`」，導致正規表示式找不到預期的結尾、非貪婪比對被迫一路往後找到檔案更後面某個湊巧符合 `\n  \];` 格式的地方才停下——抓出來的內容遠遠超出 `nodeData` 陣列本身，一路吃進後面的 `edgeData`、甚至更後面「PAGE TRANSITION PRELOADER」那段程式碼的註解與陣列。用這段被污染的內容去做字串替換，直接把 `建築事務所首頁.dc.html` 從 2596 行撐到 6059 行、裡面整段程式碼重複貼了好幾次——**這個問題在套用到真正的檔案之前就被發現**：改動後照慣例跑 `wc -l` 確認行數，2596→6059 這種暴增幅度明顯不對勁，馬上意識到不對，因為這個目錄有 git（`design_handoff_architecture_site` 是獨立的 git repo，見前幾次調整的「推送」紀錄），直接 `git checkout -- 建築事務所首頁.dc.html` 復原到最後一次 commit 的乾淨版本，沒有造成任何實際損害或需要额外补救。

**修法**：改寫抓取陣列內容的邏輯，不再依賴「陣列固定用某種空白格式結尾」這個假設，改成真正逐字元掃描、追蹤 `[`／`]` 深度（並跳過字串常值內的 `[`／`]`，避免文案裡剛好出現方括號字元誤判），深度歸零那個字元才是陣列真正的結尾——不管陣列本身是排得整整齊齊還是像 `站存.txt` 貼出來時常見的「擠成一行」，都能正確抓到邊界。陣列裡個別項目（每個 `{ id: ..., translations: {...} }`）的切分，同樣改成追蹤 `{`／`[` 深度找「回到最外層」的逗號，而不是直接假設項目之間用逗號換行分隔（因為 `translations: {...}` 本身也有逗號，樸素的字串分割會切錯）。取代文字時也改成用擷取階段記下來的精確字元位置（`slice(0, start) + 新內容 + slice(end)`）做替換，而不是 `String.prototype.replace(舊字串, 新字串)`——後者的「新字串」如果剛好包含 `$&`／`` $` ``／`$'`／`$$` 這幾種特殊樣式，會被解讀成特殊替換語法（`` $` ``／`$'` 甚至會把「比對到的內容之前/之後的全部原文」整段插進去），是另一個獨立、就算陣列邊界抓對了也可能出包的地雷，這次雖然沒有實際踩到（污染主要是邊界抓錯），但既然要重寫就一併避開。

**額外的安全網**：套用前一律先寫到 `.NEWSYNC.tmp` 暫存檔（不直接覆蓋原檔），對暫存檔做完整驗證（大括號/中括號深度是否配平、節點數與 `站存.txt` 是否一致、有無重複 id、`edgeData` 有無指向不存在節點的懸空連線、比對節點 id 順序是否跟 `站存.txt` 完全一致）全部通過才 `mv` 覆蓋正式檔案；`圓點位置編輯器.html` 的同步也套用同一套流程。往後只要是「大範圍改動、不適合逐段手動 `Edit`」的同步，都應該延用這一版腳本（抓取邏輯 + 暫存檔驗證兩步），不要再用假設固定空白格式的正規表示式去抓整段陣列。

**過程中的插曲**：修好腳本、重新套用到主網頁與編輯器並各自驗證通過後，使用者在同一個對話回合裡傳來「有更新了」——`站存.txt` 又被改過一次（這次是節點順序、`edgeData` 連線方向的調整，內容本身沒有再增減節點）。重新用同一套（已經修好的）腳本再跑一輪，兩份檔案都以最新版本為準。

**驗證方式**：暫存檔驗證通過後才覆蓋正式檔案（見上面「額外的安全網」）；Playwright 對主網頁桌機（1440px）與手機（390px）兩種寬度分別確認渲染出的節點數都是 95（78+17），console 無錯誤，並截圖比對桌機圓環版面（新節點的標籤、連線都正常顯示，沒有排版跑掉）。

### 把「圓點位置編輯器.html」已上線的節點調整同步回主網頁（2026-08-17 第三十五次調整）

**背景**：使用者在「圓點位置編輯器.html」裡進一步調整了節點內容跟位置、部署上線後，把上線後的頁面存成 `D:\2026as實習\網頁\站存.txt`，要求「同步一下目前主網頁的內容」——實際意思是把 `站存.txt` 反映出來的最新結果，同步回這個 repo 裡的 `建築事務所首頁.dc.html`（跟編輯器自己的種子資料），方便以後繼續在這裡編輯。一開始誤以為使用者是要跟真實網站 `https://www.as-structure.com/` 的線上內容比對同步，實際去抓了首頁但抓不到用 JS/動畫渲染的節點清單，問過使用者才確認真正的意思是上面這樣。

**比對結果**：拿 `站存.txt` 跟主網頁當時的 `nodeData`（104 節點，第二十八次調整後的版本）逐欄位比對，發現差異不只是位置/順序，還包含實質的內容刪減：

- `站存.txt` 少了 26 個節點：`r10`（跟 `r8` 重複的「工作文化」）、`r12` ArchiCAD、`r14` midas、`r31` 新營服務區公廁報導、`r34` 香港大學木構專案、`r42` 嘉義市立美術館競圖、`r45`～`r57` 一整批「○○競圖獲勝／台灣建築獎／ADA新銳建築獎／德國IF設計獎」（共 13 筆）、`r63`／`r65`／`r69`／`r77`／`r85`／`r90`／`r102`／`r103`。
- `specialIds` 少了 `r11`、`r22`（不再套用特殊節點樣式）。
- `r1`（章 architecture design）、`r2`（原型教學）從 `nodeData` 陣列最前面搬到最後面——**節點順序直接決定 `computeSphereUnits()`/桌機圓環排位邏輯算出來的畫面位置**（見上面「已知的資料相依細節」），這不是無關緊要的格式調整，會實際改變球面/圓環上的排列。
- `edgeData` 的連線方向大幅調整，多數從「子節點 → 分類大標」改成「分類大標 → 子節點」（例如原本 `['r30','r115']` 一筆一筆列，改成統一寫成 `['r115','r30']`／`['r115','r31']`…），語意不變（線本身無方向性，只影響陣列寫法），但既然要同步就一併採用新的寫法，避免同一份資料兩種風格混在一起。

因為刪除 26 個節點是很大、又不好復原的改動（這個環境沒有版本控制），套用前先用 `AskUserQuestion` 明確列出全部 26 個節點跟其他異動，請使用者確認是刻意刪除、不是 `站存.txt` 存的時間點比較舊、少存了這些——使用者確認「是我刻意刪的，照 站存.txt 為準」後才動手套用。

**做法**：沒有整段覆蓋 `nodeData`/`edgeData`，而是逐段用 `Edit` 精確比對兩份檔案內容、只改有差異的節點/連線區塊，原因是主網頁在 `站存.txt` 存檔之後可能又被手動修過其他欄位，整段覆蓋會把這些後續修正也一起洗掉。實際比對後發現唯一一處這種情況：`r109`（原型建築）的 `href`，主網頁當時已經是正式網域 `https://www.as-structure.com/%E5%8E%9F%E5%9E%8B%E5%BB%BA%E7%AF%89`，`站存.txt` 裡還是舊的 `https://as-studio001.github.io/asarch/`——這次同步刻意保留主網頁較新的網址，沒有跟著 `站存.txt` 退回舊連結（`languages` 陣列的引號風格也有純格式差異，同理不覆蓋）。

編輯器（`圓點位置編輯器.html`）的 `NODE_DATA`／`EDGE_DATA`／`SPECIAL_IDS` 也要跟著更新，否則下次打開編輯器又會看到舊的 104 節點版本。這次改用一段 Node.js 腳本做，而不是手動照抄：先從（已同步好的）主網頁 `nodeData` 取出最終的節點 id 順序，再逐一比對編輯器原本 `NODE_DATA` 裡「每個 id 自己的那一行」照這個新順序重新排列/篩選——**保留編輯器既有的、跟主網頁不一樣的欄位**（例如編輯器的節點資料本來就沒有 `mobileName`、`r23` 的 `href` 編輯器裡是本機測試用的 `./原型結構.dc.html` 而非主網頁的正式網址），只動順序跟「有沒有這個節點」，不動編輯器自己既有的欄位內容；`EDGE_DATA`／`SPECIAL_IDS` 因為沒發現編輯器特有的差異，直接整段取用主網頁同步後的內容。

**驗證方式**：改完用腳本檢查 `nodeData`/`NODE_DATA` 無重複 id、`edgeData`/`EDGE_DATA` 沒有指向不存在節點的「懸空」連線；再用 Playwright 對主網頁跑桌機（1440px）跟手機（390px）兩種寬度，確認畫面上實際渲染出來的節點數都是 78（104 − 26），瀏覽器 console 沒有新增錯誤，並截圖確認桌機圓環畫面正常（分類大標圓點依然明顯放大、連線正常）。

### 手機版標題排版：非中文語言改成整段文字視覺置中，不再死釘 ×（2026-08-04 第三十四次調整）

**背景**：`titleLeftX`/`titleRightX` 一直是用「以 × 為準，左右對稱展開固定間距（字級 × 0.6）」算的（第十八次調整，2026-08-02，使用者當時要求「把 X 設定為球體的核心」）。中文「原型」「未來」跟簡體中文「原型」「未来」左右都剛好是 2 個字，本來就對稱，× 釘在正中央、整段文字的視覺中點自然也在正中央，兩者是同一件事，看不出問題。翻譯成其他語言後，使用者發現「原本的設定是 X 字樣固定在中心，但現在調整為除了中文之外（包括簡中），整段文字（原型 X 未來）的翻譯版本的水平方向中點要固定在球的圓心或核心」——也就是繼續把 × 死釘在正中央這件事，對長度不對稱的翻譯（例如英文「Prototype」9 個字母、「Future」6 個字母）來說是錯的：左邊字比右邊字長一截，× 卻還是釘在正中央，肉眼看起來整組文字明顯偏向左邊，不像置中。

**做法**（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) `renderVals()`）：

1. 判斷目前語言是否要走「整段置中」模式：`const centerWholeBlock = !!lang && lang !== 'zh-Hans';`——`lang` 是 `null`（原文中文）或 `'zh-Hans'`（簡體中文）才維持舊行為（× 死釘 `cx`），其餘語言一律切成整段置中。
2. `centerWholeBlock` 為真時，用既有的 `estimateTitleWidth()` 分別估出 `centerTitle.left`／`centerTitle.right` 在目前字級下的寬度，取寬度差的一半當位移量：`titleShiftX = (leftWidth - rightWidth) / 2`；`centerWholeBlock` 為假（中文/簡中）位移量固定是 `0`。
3. 新增 `titleAnchorX = cx + titleShiftX`，取代原本寫死的 `cx`／`450`，變成 × 本身、`titleLeftX`（`titleAnchorX - titleCoreGap`）、`titleRightX`（`titleAnchorX + titleCoreGap`）、標題背光暈（`titleGlowGrad`／`circle` 的 `cx`）共用的錨點——左右文字之間的固定間距（`titleCoreGap`）邏輯完全不變，只是整組（×＋左字＋右字＋背光暈）一起往寬度較短的那一側平移，讓整段文字的左右邊界到 `titleAnchorX` 的視覺距離拉平，視覺中點自然落在球心。副標題「Architecture + Structure」维持釘在固定的 `450`（頁面/球體中心），不受這次調整影響——使用者這次描述的範圍明確是「原型 X 未來」這一行，不含固定不翻譯的英文副標題。
4. 桌機版完全不受影響：桌機標題本來就是單一 HTML 字串（`centerTitleCombined`）用 `text-align:center` 整段置中，天生就是「整段文字視覺中點在中心」，從來沒有「× 死釘」這件事，這次改動的判斷式、`titleAnchorX` 都只在 `isMobile` 分支的排版計算裡起作用。

**驗證方式**：Playwright 切換全部六種語言＋原文，讀出 ×／左字／右字三個 `<text>` 元素的 `x` 屬性——原文跟簡體中文的 × 都精確等於 `450`（球心，未位移）；English/Français/Español（左字「Prototype/Prototipo」比右字長）× 都往右偏移（例如 English 偏移到 `476.97`，手算 `(estimateWidth("Prototype",31) - estimateWidth("Future",31))/2 = (161.82-107.88)/2 = 26.97`，跟程式算出來的一致）；日文/韓文同樣正確偏移。放大截圖比對中文版跟英文版的標題區塊，兩者視覺上都置中對齊，英文版不再明顯偏左；桌機版（104 個節點、標題文字不變）重新截圖確認完全不受影響，無 console 錯誤。

### 撤銷「標題搬最上層」，改回夾在背面/正面節點層中間（2026-08-04 第三十三次調整）

**背景**：上一次（第三十二次）為了解決標題常被節點蓋住看不見的問題，把整組「背光暈＋標題」搬到全部節點畫完之後，變成手機球體 SVG 裡最上層的東西。使用者實際體驗後回報：「但現在看不出來原型未來是在球內 反而會擋住正面移動經過的圓點 希望能跟本來很明顯在球核效果」。

**問題**：標題搬到最上層之後，球心附近一小圈範圍內的正面節點永遠無法蓋過標題（會被標題連同背光暈蓋住）——這正好整個推翻了第十二次調整（2026-07-31）刻意做的「球體節點依景深分兩層 DOM，讓置中標題看起來嵌在球心」設計：那次調整的重點就是「正面節點蓋過標題、標題蓋過背面節點」這個雙向遮蔽關係，讓標題視覺上像是真的嵌在球體內部、會被轉到前面的節點正常擋住。上一次的修法解決了「常常整個看不見」，但代價是徹底犧牲了這個立體感，使用者體驗後明確表示「嵌在球心」比「隨時 100% 可讀」更重要，兩者衝突時要選前者。

**做法**：只把 DOM 順序改回去，標題（含背光暈）這一整塊 `sc-if` 從「全部節點畫完之後」搬回原本「背面節點層／正面節點層中間」的位置——正面節點的 `sc-for` 迴圈重新排在標題*之後*，恢復「正面節點蓋過標題」的遮蔽關係。上一次同時修的另外兩件事維持不動、繼續生效：

1. **SVG `<text>` 包 `<span class="sc-interp">` 導致翻譯文字整個不畫出來的 bug**——`data-center-title-left`／`data-center-title-right` 兩個 `<text>` 元素還是留空、由 `_syncCenterTitleText()` 直接寫 `textContent`，不透過 `{{ }}` 內插，這個修法跟標題擺在哪一層沒有關係，繼續有效。
2. **標題背光暈**（頁面背景色、往外羽化的放射狀漸層圓）——維持不變，還是能把標題背後那層連線/節點的視覺雜訊擦掉，緩解「常被蓋住看不清楚」的問題；只是現在正面節點會照常蓋在光暈跟標題上面，不再是「不管轉到哪個角度都保證看得到」。

**驗證方式**：Playwright 讀 `[data-globe-svg]` 底下子元素的順序，確認「最後一個含 `[data-node-back]` 的元素」的索引（262）小於「含 `[data-center-title-left]` 的元素」的索引（266），且小於「第一個含 `[data-node-front]` 的元素」的索引（269）——DOM 順序確實是背面層 → 標題 → 正面層。截圖比對多個拖曳角度，確認標題文字仍然清楚可讀（背光暈還在），同時能看到節點的圓點/文字標籤局部疊在標題文字上面（例如「型」或「來」字被节點蓋住一角），這正是「嵌在球心」該有的視覺效果；桌機版（104 個節點、不受這次調整影響）重新截圖確認正常。

### 修正手機版球心標題翻譯後消失：SVG text 不接受 span 內插、加背光暈防止被節點蓋住（2026-08-04 第三十二次調整）

**背景**：上一次（第三十一次）加了防溢出縮放之後，使用者實際在手機上檢查，回報「手機版中的原型未來消失了」。

**根本原因一：SVG `<text>` 不接受模板引擎的 `<span>` 內插包裝**。桌機版標題是 HTML（`foreignObject` 裡的 `<div>`），手機版標題是三個獨立的 SVG `<text>` 元素——這個差異在第三十次調整（把 `{{ centerTitleLeft }}`／`{{ centerTitleRight }}` 直接當成 `<text>` 的子內容）埋下問題：DC 模板引擎的 `walkText()`（`support.js`）對「已經解析出字串」的 `{{ }}` 內插，固定會包一層 `h("span", { className: "sc-interp" }, String(v))`（這是給串流載入時顯示 placeholder 用的機制，程式碼裡沒有關掉的選項）。桌機版是 HTML 情境，`<div>` 裡包一個 `<span>` 完全正常；手機版是 SVG 情境，`<text>` 的內容模型只接受文字節點或 `<tspan>`，塞進一個 HTML 命名空間的 `<span>` 元素，瀏覽器直接不畫出來——結果就是「原型」「未來」兩個文字節點完全消失，`×` 和固定不變的英文副標題「Architecture + Structure」因為是樣板裡的**字面文字**（沒有走 `{{ }}` 內插），所以不受影響、還看得到，這也是一開始比對「× 看得到、翻譯字看不到」這條線索、找到問題本質的關鍵。用 Playwright 直接讀 DOM 的 `outerHTML` 才看穿這個問題——`getBoundingClientRect()` 這類量測 API 在這台機器的無頭瀏覽器上量 SVG `<text>` 常常得到可疑的定值或零寬度，不能只看量測數字判斷，要直接看 `outerHTML` 裡有沒有意外多出來的 `<span>`。

**做法一**：樣板裡這兩個 `<text>` 元素改成留空、掛穩定的 `data-center-title-left="1"`／`data-center-title-right="1"`，不再用 `{{ }}` 內插；新增 `_syncCenterTitleText()` 方法直接對這兩個元素寫 `textContent`（跟既有的 `_pinLinkLabel.textContent = ...` 是同一套「SVG/imperative 情境下繞過 React 內插」的做法），在 `componentDidMount()`（初次掛載）跟新增的 `componentDidUpdate()`（涵蓋語言切換、深色模式、手機/桌機切換等任何觸發 re-render 的操作）都呼叫一次；桌機模式下這兩個 `<text>` 不存在於 DOM，`querySelector` 找不到就跳過，不用另外判斷 `isMobile`。

**根本原因二：就算文字畫出來了，也常被節點蓋住**。手機球體把標題夾在「背面節點層」（畫在標題前面）跟「正面節點層」（畫在標題後面）中間，是刻意設計（第十二次調整），讓標題視覺上像嵌在球心。但背面連線／節點的顏色淡但仍會透出來，加上偶爾轉到中央附近的正面節點，多個角度截圖比對下來，中心那一小塊經常被密集的線條/節點文字整個蓋住，肉眼幾乎看不到標題。

**做法二**：先用 AskUserQuestion 跟使用者確認處理方向（三個選項：加背光暈 vs. 標題永遠最上層放棄嵌入感 vs. 維持現狀），使用者選「加一層深色光暈/背景圓」。實作：

1. 新增一個 `radialGradient`（`titleGlowGrad`），中心用頁面背景色 `bgColor`（跟深色模式一起變）、95% 不透明，往外羽化到完全透明，半徑固定為 `titleSafeWidth / 2 + 45`（不隨目前這句翻譯的實際長度變動，切換語言時光暈大小穩定不跳動）。
2. 光只把光暈墊在標題後面（原本「背面層／標題／正面層」的中間位置）只能擦掉背面那層，正面節點蓋過來還是沒解——把整組「光暈＋標題」從中間位置搬到全部節點（背面＋正面兩層）都畫完*之後*，變成手機球體 SVG 裡最上層的東西。代價：球心附近一小圈範圍內（光暈半徑內）的正面節點會被蓋住看不到，範圍外的節點完全不受影響、深淺景深效果照舊——這一小圈範圍本來節點也擠在一起分不清楚單一節點是誰，犧牲這個換標題在任何角度都讀得出來，划算。

**驗證方式**：Playwright 直接讀 DOM `outerHTML` 確認修正後 `<text>` 元素不再包 `<span>`，`textContent` 正確顯示翻譯文字；截圖比對——拖曳球體到 4 個不同角度（含預設角度），中文原文跟切換英文之後，「原型 × 未來」／「Prototype × Future」都清楚顯示在淡化的背景光暈上，不被節點蓋住；深色模式下背光暈正確跟著變深、文字正確變白色（`oklch(0.95 0 0)`）；桌機版（未受影響的 `!isMobile` 分支）重新截圖確認 104 個節點、標題文字都正常，沒有被這次修改波及。

### 球心標題多語系重做：加上防溢出、字級依翻譯長度自動縮小（2026-08-04 第三十一次調整）

**背景**：第三十次調整撤銷了球心標題多語系。使用者接著回報，原本那版（第二十九次）的問題其實是「原型X未來字樣的翻譯版 會因為字串的長度而產生超出圓圈或圓球」，並要求「請自行調整位置 並保持X符號要在中心」——也就是不是不要這個功能，是要連同溢出問題一起修好再上線。

**根本原因**：第二十九次調整把 `centerTitleLeft`/`centerTitleRight` 換成翻譯文字，但字級（`titleFontSize`，桌機固定 62px、手機固定 31px）完全沒有跟著文字長度調整。中文原文「原型」「未來」都是 2 個全形字，短；翻譯後例如英文「Prototype」「Future」是 9／6 個字母，法文「Prototype」「Futur」、日文「プロトタイプ」「未来」、韓文「프로토타입」「미래」長短不一，但字級固定不變，字串一長，畫出來的寬度就跟著等比例變寬，桌機版超出圓環範圍、手機版超出球體輪廓，這就是使用者說的「超出圓圈或圓球」。

**做法**：不是替每種語言手動量一個位置/字級（語言之後還可能再增加，手動維護不划算），改成通用的「依文字長度自動縮小字級」機制，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html)：

1. 新增 `estimateTitleWidth(text, fontSize)` 方法：粗略估算一段文字用某個字級畫出來大概多寬——CJK 表意文字／假名／諺文／全形符號算一個全形寬度（`1 × fontSize`），其餘（拉丁字母、數字、× 符號、空白）平均抓 0.58 個全形寬度（`0.58 × fontSize`）。不是精確量測（渲染前沒有實際 DOM 可以量），只用來抓「這句翻譯明顯比中文原文寬很多」這種情況。
2. `renderVals()` 裡：先算出基準字級 `baseTitleFontSize`（桌機 62、手機 31，跟原本一樣），再算出目前語言的標題文字（`getCenterTitle()`）用基準字級的估算寬度，跟一個「安全寬度」比較——桌機安全寬度是標題框寬度扣 40px 邊界（`titleBoxWidth - 40 = 380`）；手機安全寬度是球體半徑（`_GLOBE_SPHERE_RADIUS_VB = 260`）的 1.5 倍（`390`，比桌機略寬鬆，因為手機字級基準本來就比較小，翻譯後字串通常不會逼近球體邊緣）。估算寬度超出安全寬度，就把字級整組等比例縮小（`titleScale = 安全寬度 / 估算寬度`，夾在 `[0.5, 1]` 之間，最小不縮到原本一半以下，避免超長翻譯把字縮到看不清楚）；沒超出就維持原本的基準字級不變（中文原文屬於這種情況，不會被誤縮）。
3. × 符號能永遠保持在正中央，靠的是既有排版公式本身的性質，不用額外處理：手機版 `titleLeftX = cx - titleCoreGap`、`titleRightX = cx + titleCoreGap`（`titleCoreGap = titleFontSize × 0.6`）本來就是「以 cx 為準，左右對稱展開」，字級縮小、`titleCoreGap` 跟著等比例縮小，但公式的中心點恆定是 `cx`，× 本身固定畫在 `(cx, cy)`，不受字級影響；桌機版更單純，整個標題是置中的單一字串，`text-align:center` 天生就是以容器中心為準左右對稱展開。

**驗證方式**：先用 Playwright 直接讀出元件即時算出的 `titleFontSize`（讀 `<div>`/`<text>` 的 inline `style.fontSize`，不是量渲染後的像素寬度——headless Chromium 在這台機器上量 SVG `<text>` 的 `getBoundingClientRect()` 不穩定，量到的寬度好幾次都是可疑的定值，改看實際套用的字級數值才是可靠的地面真相）：桌機版原文 62px（未縮小，因為估算寬度 355.88 沒有超出安全寬度 380）、English 縮到 36px、Français 縮到 39px，跟公式手算結果一致。接著截圖比對桌機版切換到 English 前後：標題完整置中顯示在圓環中央，沒有跟外圍節點圓點重疊或超出圓環範圍。手機版因為基準字級本來就小（31px），六種語言在目前的安全寬度門檻下都不需要縮小（估算寬度全部小於 390），× 的 SVG `x` 屬性在切換語言前後都維持 `450`（球心 x 座標）不變，確認「× 保持在中心」這個要求成立。

### 撤銷球心標題多語系（2026-08-04 第三十次調整）

**背景**：上一次（第二十九次）剛做完球心「原型 × 未來」標題多語系，使用者馬上要求「改回上一版」。沒有版控可以還原，是逐一手動刪掉上次新增的四處程式碼。

**做法**：完整刪除第二十九次調整加的東西，一個不留——`centerTitleTranslations` 欄位（連同上面的說明註解）、`getCenterTitle()` 方法（連同上面的說明註解）、`renderVals()` 裡的 `centerTitle`/`centerTitleLeft`/`centerTitleRight`/`centerTitleCombined` 這幾行、回傳物件裡的 `centerTitleLeft, centerTitleRight, centerTitleCombined`；樣板三處分別改回寫死的字面文字：桌機 `<div>` 改回 `原型 × 未來`，手機兩個 `<text>` 元素分別改回 `原型`／`未來`。改完之後這幾處程式碼跟第二十九次調整之前逐字一樣，不是留著功能只是關掉。

**驗證方式**：Playwright 確認桌機/手機切換語言（English／日本語／Español 都試過）後，球心標題維持「原型 × 未來」／「×」「原型」「未來」不變、不會跟著語言切換鈕變化；同時確認節點名稱（`getDisplayName()`／`nodeData[].translations`，第二十三次調整那個功能）沒有被這次撤銷波及，切換語言後圓環/球體上的節點文字依然正確翻譯；全程 `page.on('pageerror')` 沒有任何錯誤。

### 球心「原型 × 未來」標題多語系（已撤銷，見上面「撤銷球心標題多語系」——2026-08-04 第二十九次調整）

**背景**：主視覺多語系（第二十三次調整）當時只處理了圓環/球體上的節點名稱，球心那個固定不動的「原型 × 未來」標題（桌機是一個 `<div>`，手機是拆成三個獨立 `<text>` 的「原型」「×」「未來」，見第十八次調整）當時完全沒動，切換語言只有節點文字會變、球心標題永遠維持中文。使用者要求「原型未來字樣也要可以多語言版本」。

**做法**：這不是 `nodeData` 裡的節點，不適合塞進編輯器的節點翻譯欄位，改成直接在 [建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 寫一個小的 `centerTitleTranslations` 對照表（跟 `languages` 用同一組 code：`en`/`zh-Hans`/`fr`/`ja`/`ko`/`es`），不走編輯器同步流程：

```js
centerTitleTranslations = {
  en: { left: 'Prototype', right: 'Future' },
  'zh-Hans': { left: '原型', right: '未来' },
  fr: { left: 'Prototype', right: 'Futur' },
  ja: { left: 'プロトタイプ', right: '未来' },
  ko: { left: '프로토타입', right: '미래' },
  es: { left: 'Prototipo', right: 'Futuro' }
};
```

新增 `getCenterTitle()` 方法，邏輯跟既有的 `getDisplayName()` 對稱：目前語言在對照表裡找得到就回傳對應的 `{ left, right }`，找不到（沒選語言、或選到「中文原文」）就回傳 `{ left: '原型', right: '未來' }`。`renderVals()` 裡算出 `centerTitleLeft`／`centerTitleRight`（給手機版兩個獨立 `<text>` 元素直接用）跟 `centerTitleCombined`（`left + ' × ' + right`，給桌機版單一 `<div>` 用），取代樣板裡原本寫死的「原型 × 未來」／「原型」／「未來」三處字面文字。

手機版「× 精確釘在球體投影中心」的排版（`titleLeftX`/`titleRightX = cx ∓ titleFontSize*0.6`，第十八次調整）完全不用跟著改——這個間距是固定值、不是依文字寬度算的，`text-anchor:end`／`text-anchor:start` 會讓文字往遠離中心的方向長，不管翻譯後的文字多長（例如英文「Prototype」比中文「原型」長很多），都只會讓文字離 × 更遠，不會反過來擠壓或蓋住中間的 ×，所以換語言不需要重新量測排版參數。

**驗證方式**：Playwright 分別在桌機（1440px）跟手機（390px）寬度下，切換語言選單依序點過 English／日本語／Español，確認桌機的 `<div>` 文字正確變成「Prototype × Future」「プロトタイプ × 未来」，手機三個獨立 `<text>` 元素正確變成「×」「Prototipo」「Futuro」（分別對應原本的「×」「原型」「未來」三個節點）；切換前預設維持原文「原型」「未來」「×」不變；全程 `page.on('pageerror')` 沒有任何錯誤。

### 套用編輯器實際操作後的匯出資料：刪 13 個節點、加 1 個節點、修正壞掉的網址（2026-08-03 第二十八次調整）

**背景**：使用者在「圓點位置編輯器.html」裡實際整理過 `languages`／`specialIds`／`nodeData`／`edgeData`（跟上一次「批次貼入翻譯」不是同一份操作——上一次是我用腳本把校對表寫進編輯器，這次是使用者自己在編輯器 UI 上動了手，包括刪節點、改網址、加新節點），把「同步至主網頁」的匯出文字貼給我，要求「把以下資料更新到主網頁的程式碼」，並主動提醒「目前原點編輯器匯出的資料有點亂」。

**比對出來的問題，沒有照單全收，先跟使用者確認過才套用**：

1. **13 個現有節點在匯出資料裡完全消失**（`r4` 美感教育、`r5` 孩童、`r6` 文化、`r13` midjourney、`r44` 2016 桃園市蘆竹國民運動中心興建工程、`r51` 2017 宜蘭中山小巨蛋 台灣建築獎佳作、`r68` 2016師大美術館、`r74` 2018獅甲幼兒園、`r75` 2018威斯汀木構婚禮教堂、`r78` 2019 札哈木原民會館、`r80` 2020 教師會館、`r83` 2021綠美圖帷幕、`r87` 2022新竹慈雲路空橋），`edgeData` 裡這 13 個節點的連線也乾乾淨淨地一起消失，沒有留下指向已刪節點的殘留連線——這種「乾淨刪除」的模式比較像使用者在編輯器裡用了「刪除此節點」功能（會連帶清掉關聯的連線），不像複製貼上漏字這種意外，但畢竟是要從主網頁移除 13 筆真實作品資料、沒有版本控制可以復原，**問了使用者才動手**，確認是刻意刪除。
2. **兩筆節點的網址被複製貼了兩次黏在一起**：`r56`（2018 澎湖ENISHI緣民宿ADA新銳建築獎首獎）跟 `r67`（2016 嘉義美術館），網址變成同一個 URL 重複兩次直接接在一起（例如 `...p_id=502815https://www.mashup.com.tw/...p_id=502815`），明顯是壞掉的資料——這個直接判斷是 bug、不是刻意的，寫了一個「前後兩半字串完全相同就取一半」的小工具函式自動修正，不用使用者逐一確認。
3. **匯出資料完全沒有 `mobileName` 欄位**——這是編輯器工具資料模型本身的既有缺口（`NODE_DATA` 從來沒有這個欄位，不是這次才漏掉的），如果整批覆蓋過去，會把主網頁現有 78 個節點的手機版縮短標題全部洗掉。**做法**：合併時依 `id` 從主網頁目前的 `nodeData` 讀出既有 `mobileName`，套進新資料裡，不受這次匯出內容影響。
4. **節點陣列順序**：`computeSphereUnits()`（手機版 3D 球體排位）跟桌機 2D 圓環都是依 `nodeData` 陣列順序（濾掉/濾入 `specialIds` 後的相對順序）決定節點在圓環/球面上的位置，見「目前狀態：手機版 3D 球體」那節的說明——如果直接照使用者貼的文字順序整批覆蓋，會讓兩種版面的節點排列大改版（使用者這次沒有要求重新排版）。**做法**：不採用貼入文字本身的順序，而是保留主網頁 `nodeData` 目前的相對順序，只在原順序上「移除 13 個 id」＋「在 `r40` 後面插入新節點 `r117`」（新節點在貼入資料跟主題分類上都最靠近 `r40`「2023 X-SITE「途中」獲獎」這群 X-SITE 得獎節點，插在那裡最自然），其餘 103 個既有節點的相對順序完全不動。

**做法**：寫了兩支 Node.js 腳本（scratchpad，不併入專案）分別處理 [建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 跟 [圓點位置編輯器.html](../圓點位置編輯器.html)（兩邊都要更新，编輯器的 `NODE_DATA`/`EDGE_DATA`/`SPECIAL_IDS` 是這個工具重新整理載入時的種子資料，不同步會導致下次打開編輯器又看到舊的 116 筆資料，覆蓋掉使用者剛做的整理）：

1. 解析使用者貼的 `nodeData` 文字，取得每個 id 對應的 `name`/`href`/`translations`，並修正 `r56`/`r67` 的重複網址。
2. 讀出主網頁/編輯器現有 `nodeData`／`NODE_DATA` 的 id 順序（跟主網頁另外讀出的 `mobileName`），依「移除 13 個已確認刪除的 id」＋「`r40` 後插入 `r117`」的規則產生新順序，逐一用貼入資料的 `name`/`href`/`translations` 更新每個節點，主網頁那份額外把原本的 `mobileName` 接回去。
3. `specialIds`／`SPECIAL_IDS` 直接換成使用者貼入的 12 筆（比原本多了 `r15` 原型原點）。
4. `edgeData`／`EDGE_DATA` 直接整批換成使用者貼入的版本（連線本身的陣列順序不影響任何位置計算，只有 `nodeData` 的順序才會）。
5. `languages`／`LANGUAGES` 套用使用者貼入的六語清單（跟上一次批次貼翻譯那次寫入編輯器的內容一致，這次補寫回主網頁）。

**驗證方式**：先寫一支純 Node.js 腳本（不開瀏覽器）逐項檢查合併後的 [建築事務所首頁.dc.html](./建築事務所首頁.dc.html)：`nodeData` 陣列裡 `{`/`}` 數量對稱、`edgeData` 陣列裡 `[`/`]` 數量對稱（合併時字串插入沒有漏括號）；節點數為 104 且沒有重複 id；13 個確認刪除的 id 在 `nodeData`／`edgeData` 都徹底找不到；沒有任何 `edgeData` 連線指向不存在的節點 id（沒有殘留的懸空連線）；`r117` 同時存在於 `nodeData` 跟至少一條 `edgeData` 連線裡。接著用 Playwright 分別開桌機（1440px）跟手機（390px）寬度：桌機量到 104 個 `[data-node]` 元素、手機量到 104 個 `[data-node-back]` 元素（跟純節點數一致，代表沒有多畫或少畫）；點開語言切換選單、切成 English，確認 `r23`（原型結構）節點文字正確變成「Prototype Structure」；確認新節點 `r117` 存在且切換英文後顯示「Analogue Forest」；全程 `page.on('pageerror')` 沒有任何錯誤。編輯器那邊同樣用 Playwright 開節點清單，確認筆數變成 104、新節點「2024 X-SITE 林木林」在清單裡、已刪除的「美感教育」不在清單裡、語言管理清單六種語言仍然完整，也沒有任何 console 錯誤。

### 圓點位置編輯器批次貼入 116 筆節點翻譯、新增六種語言（2026-08-03 第二十七次調整）

**背景**：上次「電腦版圓點標題多語系校對表」（116 筆、六種語言）給使用者校對過後，使用者要求「可以直接幫我把這些翻譯貼進去編輯器嗎」——也就是不要手動一個個貼，直接把校對表的內容寫進 [圓點位置編輯器.html](../圓點位置編輯器.html) 的 `LANGUAGES` 跟每個 `NODE_DATA` 節點的 `translations` 欄位。

**做法**：沒有在編輯器 UI 裡手動操作 116 次（既慢又容易手滑打錯字），改用一支一次性 Node.js 腳本（寫在 scratchpad、不併入專案）直接改寫 `圓點位置編輯器.html` 原始碼：

1. 腳本內嵌一份跟校對表 `ROWS` 完全一致的 116 筆 `[id, 中文, en, zh-Hans, fr, ja, ko, es]` 資料（校對表本身沒有校對意見回饋、視為使用者已核可的版本直接採用）。
2. 把 `const LANGUAGES = [];` 取代成六個內建語言物件（`{code, label}`），對應「新增語言改成下拉選單」那次調整（第二十五次）裡 `PRESET_LANGUAGES` 用的同一組代碼（`en`/`zh-Hans`/`fr`/`ja`/`ko`/`es`）。
3. 用正則 `\{ id: '(r\d+)', name: "...", href: "..." \}` 逐一比對 `NODE_DATA` 裡的 116 個節點物件，比對到就在 `href` 後面插入 `translations: {...}`（`JSON.stringify` 六語物件），插入時用捕捉到的 `id` 去查表，不是靠陣列順序，避免任何節點錯位對應到別的翻譯。

**驗證方式**：腳本執行後回報「injected: 116, missing ids: []」，確認 116 筆全部命中、沒有漏掉或對錯 id。另外起本機伺服器、用 Playwright 開啟編輯器：(1) 打開「語言管理」彈窗，確認清單顯示 English/簡體中文/Français/日本語/한국어/Español 六筆；(2) 打開節點 r23（原型結構）的編輯面板，確認翻譯區塊六個欄位都已經帶入正確的值（英文「Prototype Structure」、簡體中文「原型结构」……），不是空的；(3) 全程 `page.on('pageerror')` 沒有任何錯誤，確認插入的 JSON 語法沒有破壞既有的 `NODE_DATA` 陣列。這次只改了「圓點位置編輯器.html」的資料內容，不是程式邏輯，之後要同步到主網頁一樣走既有的「同步至主網頁」複製貼上流程（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 這次沒有直接改動）。

### 圓點位置編輯器節點編輯面板捲不到最下面（2026-08-03 第二十六次調整）

**症狀**：使用者回報「編輯欄位會因為東西太多需要可以捲動的功能，現在無法編輯到太下面的欄位」——節點編輯面板（`<aside id="nodeConnections">`）原本裝的欄位不多（位置角度、大小、顏色、超連結、連線對象），這次多語系調整又加了一整段「翻譯」欄位（[圓點位置編輯器.html](../圓點位置編輯器.html) 第二十三次調整新增），語言一多，面板總高度就超過視窗可用高度。

**根本原因**：`.node-connections` 這個外層容器是 `position:fixed; top:...; bottom:0; overflow:hidden; display:flex; flex-direction:column`——固定高度、`overflow:hidden`，裡面用 flex column 直向排列每一段（名稱列／欄位／連線對象子標題／連線清單／翻譯子標題／翻譯欄位）。原本只有「連線對象」清單本身有 `flex:1 1 auto; overflow-y:auto`，設計成「其他欄位固定、只有連線清單自己在剩餘空間裡捲動」——這在欄位少的時候沒問題，但翻譯欄位是加在連線清單「後面」的新區塊，不在那個唯一會捲動的區域裡，整個面板的總高度一旦超過視窗，多出來的部分直接被外層的 `overflow:hidden` 裁掉，連捲軸都不會出現，滑鼠滾輪完全沒反應。

**做法**：把「名稱／刪除」那一列（`.edit-panel-head`，使用者需要隨時看到目前在編輯哪個節點，維持固定不動）以外的所有內容——欄位、連線對象、翻譯——整段包進一個新的 `.edit-panel-scroll` 容器，這個容器才是真正的 `flex:1 1 auto; overflow-y:auto`，取代原本只讓連線清單自己捲動的做法，改成整個面板下半部一起捲。額外加了 `min-height:0`——這是 flexbox 常見的坑：flex 子元素預設 `min-height:auto`，內容多的時候會把子元素撐到跟內容一樣高，反而讓 `overflow-y:auto` 失效、捲軸出不來，明確蓋掉這個預設值才會真的捲得動。

**驗證方式**：Playwright 把視窗高度縮到 700px（刻意製造欄位塞不下的情況）、新增全部 6 種內建語言，量測面板可捲動內容高度 2232px vs. 可視高度 576px（確認真的溢出、需要捲動）；把面板捲到底，確認最後一個翻譯欄位（Español）完整落在可視範圍內且能正常輸入文字；截圖比對面板顯示正常、捲動後每一段內容都排列整齊。

### 圓點位置編輯器頁首版面跑掉、新增語言改成下拉選單（2026-08-03 第二十四、二十五次調整）

這兩次都是針對上一版「主視覺多語系」調整裡、`D:\2026as實習\網頁\圓點位置編輯器.html`（跟主網頁分開的獨立工具，不在 `design_handoff_architecture_site` 這個資料夾裡）新增的「語言管理」功能做的後續修正/優化，不影響主網頁本身（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 這次完全沒有再改動）。

**問題一：頁首高度變超高**。根本原因是 CSS 選擇器清單漏掉新按鈕：編輯器頁首每個純 icon 按鈕（匯入、Undo、Redo、重新整理間距、深淺模式、同步）都是靠兩條共用的 CSS 規則統一控制大小——一條管按鈕本身的 padding，另一條 `#btnImportSite svg, #btnResetSpacing svg, #btnThemeToggle svg, #btnSyncSite svg { width: 15px; height: 15px; }` 專門把裡面的 SVG icon 縮到 15×15px。新增「語言管理」（`#btnLanguages`）按鈕時，只顧著讓它跟其他按鈕外觀一致（同樣的 class、同樣的 SVG 寫法），卻忘了把它的 id 加進這兩條選擇器清單——結果它裡面的 SVG 沒有被套用任何明確尺寸，瀏覽器對「沒有指定 width/height 的 `<svg>`」的預設行為是退回一個相對很大的固定尺寸（約 300×150 CSS px），這顆 icon 直接把整個 `<header>`（`display:flex; align-items:center`）撐到跟它一樣高。**修法**：把 `#btnLanguages` 補進那兩條選擇器清單即可，不用改任何邏輯。**驗證方式**：Playwright 量測修正後頁首高度回到 68px（跟其他既有按鈕同一個量級），`#btnLanguages` 內 SVG 的 `getBoundingClientRect()` 確認是 15×15px，截圖比對頁首排版恢復正常。

**問題二：新增語言的介面改成下拉選單**。原本「新增語言」是兩個空白文字框（代碼、顯示名稱）都要手打，使用者覺得應該要有一個下拉選單、內建常用的英文／簡體中文／法文／日文／韓文／西班牙文，另外留一個「自訂」選項給沒列出來的語言用。**做法**（[圓點位置編輯器.html](../圓點位置編輯器.html)，語言管理彈窗那一段）：

1. 兩個文字框換成一個 `<select id="newLangPreset">`，六個內建選項（`en`/English、`zh-Hans`/簡體中文、`fr`/Français、`ja`/日本語、`ko`/한국어、`es`/Español）＋一個 `custom`/「＋ 自訂語言…」，原本那兩個文字框包進一個 `#customLangFields` 容器，預設 `hidden`，只有選到「自訂語言」才會顯示（`select` 的 `change` 事件切換）。`zh-Hans`（簡體中文）這個代碼刻意呼應主網頁前次調整補上的 `<html lang="zh-Hant">`（見「網頁的內容可翻譯」那次）——兩者都是用「書寫系統」代碼（Hant/Hans）而不是「國家/地區」代碼（TW/CN），語意上更準確對應「同一種語言、不同書寫系統」這個情境。
2. `addLanguage()` 改成先看 `newLangPreset` 選了什麼：選到六個內建選項之一，代碼跟顯示名稱直接從一個 `PRESET_LANGUAGES` 對照表取值；選到「自訂」才去讀那兩個文字框的值（原本的必填檢查邏輯不變）。
3. 新增 `updateLanguagePresetOptions()`：已經加進 `LANGUAGES` 的內建語言，會把下拉選單裡對應的那個 `<option>` 設成 `hidden`（避免選到已經加過的、被「這個語言已經存在了」擋下來，體驗上不如直接不給選），每次語言清單變動（新增/刪除/匯入）都會重新呼叫這個方法同步選單狀態；如果目前選到的選項剛好因此被藏起來，自動跳到下一個還沒加過的選項，不會讓選單停在一個看不見的值上。

**驗證方式**：Playwright 開啟語言管理彈窗，確認下拉選單有 7 個選項（6 個內建＋自訂）、自訂欄位預設隱藏；直接按「新增」（預設選到 English）確認語言清單正確出現「English（en）」、選單裡的 English 選項自動變成 `hidden`、選取值自動跳到下一個（簡體中文）；切到「＋ 自訂語言…」確認文字框正確顯示、輸入 `de`/Deutsch 新增成功；刻意用自訂欄位再打一次 `en` 這個已存在的代碼，確認被現有的重複代碼檢查擋下來，語言清單沒有變化。

### 主視覺多語系（2026-08-03 第二十三次調整）

**需求**：使用者要求「這個網頁的內容可翻譯成各種語言」，討論後聚焦到「主要針對主網頁的主視覺」——也就是圓環/球體上的節點文字，不含頁首導覽、頁尾、標題「原型 × 未來」等其他文字。做法分兩半：（1）翻譯內容由「D:\2026as實習\網頁\圓點位置編輯器.html」（節點位置編輯器，跟主網頁分開的獨立工具）手動維護；（2）主網頁（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html)，手機、桌機都要，子網頁不用）頁首新增一個語言切換鈕，可以切到對應語言版本。

**資料模型**：`nodeData` 每個節點可以有一個 `translations: { code: '翻譯文字' }` 欄位——直接掛在既有的節點物件上，不是另外開一個陣列，這樣可以沿用既有的 `specialIds`／`nodeData`／`edgeData` 三段式複製貼上同步流程，不用額外多一套同步機制去對應節點 id。額外新增一個 `languages = [{ code, label }]` 陣列（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html)，`specialIds` 宣告處旁邊），放「頁首下拉選單要列出哪些語言」，一樣靠複製貼上同步。兩者預設都是空的——沒有同步過任何語言、任何翻譯的話，語言鈕點開只會看到「中文（原文）」一個選項，行為跟改動前完全一樣。

**主網頁怎麼決定要顯示什麼文字**：新增 `getDisplayName(d, mobile)`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html)，`computeDegree()` 前面）統一算：目前語言有這個節點的翻譯就用翻譯，否則手機版退回 `mobileName || name`、桌機版退回 `name`（跟改動前的退回規則完全一樣，只是多了翻譯這一層優先權）。**這個方法必須是唯一算「節點該顯示什麼文字」的地方**，`renderVals()`（桌機/手機兩個分支）跟 `paintGlobe()`（手機版每影格重算 labelWrap／foreignObject 寬度，圖表框寬度用的是同一個文字長度）都要呼叫它，不能各自分別判斷——這正是這份手冊前面已經踩過兩次的「imperative（`paintGlobe()`）跟 declarative（`renderVals()`）各自算一套，算出來的文字或寬度對不上」那個既有雷（見更早的「文字只有一開始在正面的節點才會顯示」「球體卡住不會轉」兩節），多語系這個新維度更容易在不注意的情況下再犯一次，所以刻意共用同一個方法從根本避免。搜尋結果清單、地標 icon 的無障礙標籤（`_pinLinkLabel`）也都呼叫同一個方法，語言切換之後這些地方會一起換。

**頁首切換鈕**：`state.currentLang`（`null` 代表原文）+ `state.showLanguageMenu`，`toggleLanguageMenu()`/`setLanguage(code)`。按鈕本身放在 nav pill 裡、**不包在 `isMobile`／`!isMobile` 的 `sc-if` 裡**（跟裝置切換鈕、深淺模式鈕同一層，手機桌機都會出現），下拉選單第一項固定是「中文（原文）」，其餘依 `languages` 陣列 `sc-for` 出來。點擊外部關閉沿用既有的 `_onDocMouseDown` 機制，多加一個 `[data-lang-menu-box]` 判斷（跟 `[data-menu-box]`／`[data-search-box]` 是同一套模式）。

**圓點位置編輯器新增的功能**（`D:\2026as實習\網頁\圓點位置編輯器.html`）：

1. **語言管理**：頁首新增一個地球圖示按鈕，開一個小彈窗可以直接新增/刪除語言（表單即時生效，不用像 nodeData/edgeData 那樣先貼文字再套用）——語言清單通常就幾筆，逐筆增刪比整段貼文字方便。刪除語言時會順便把每個節點底下這個語言的翻譯一起清掉，避免同步出去的內容留著孤兒資料。
2. **每節點翻譯欄位**：編輯面板（點節點清單的 ▸ 展開的那個側欄）新增一個「翻譯」區塊，`languages` 有幾個語言就出現幾個文字輸入框，輸入即時寫回該節點的 `translations`，留空會把那個語言的 key 整個刪掉（不是留空字串）。
3. **同步匯出／匯入擴充**：「同步至主網頁」彈窗多一個 `languages［ ］` 區塊；`nodeData［ ］` 的每個節點物件，有 `mobileName` 的話比照主網頁欄位順序（`id`/`name`/`mobileName`/`href`）帶出來、有翻譯的話在最後補上 `translations: {...}`（`JSON.stringify()` 產生標準 JSON，主網頁那邊可以直接 `JSON.parse()`）。「從主網頁匯入」彈窗同樣多一個 `languages［ ］` 欄位（選填，沒貼內容就保留編輯器裡目前的語言清單）。

**順便修掉的既有 bug（發現於改動這段程式碼的過程中，不是這次新增翻譯功能才產生的）**：編輯器的 `parseImportNodeData()` 原本用一個嚴格的正規表示式，要求貼進來的每個節點物件**剛好**是 `{ id: '...', name: "...", href: "..." }` 這個順序、中間不能有別的欄位——但主網頁很多節點在 `name` 跟 `href` 之間其實還有一個 `mobileName` 欄位（手機版縮寫名稱），只要節點有 `mobileName`，正規表示式就完全比對不到，**整個節點在匯入時被靜默漏掉**，使用者毫無提示。這個 bug 比這次的翻譯功能更早就存在，只是這次要讓匯入／匯出也能處理「順序不固定、有沒有都可以」的 `translations` 欄位，兩個問題根源相同，一起修：改成先用容許一層巢狀大括號的正規表示式抓出每一個 `{ ... }` 區塊，區塊內部再各自找 `id`/`name`/`mobileName`/`href`/`translations`，不管欄位順序、有沒有齊全都不影響比對到其他欄位。連帶把 `snapshotStructure()`／`rebuildFromSnapshot()`（新增/刪除節點、pen mode 拖曳重新排序時的 Undo/Redo 機制）也一併補上 `mobileName`／`translations`，不然這兩個欄位會在做任何「結構性編輯」的 Undo/Redo 時被整批清空。

**驗證方式**：Playwright 測了完整流程——(1) 主網頁：切換語言後，桌機/手機版有翻譯的節點正確顯示翻譯文字、沒有翻譯的節點正確退回原文（含桌機的方框寬度、手機版拖曳觸發 `paintGlobe()` 重算寬度都正確反映翻譯後文字的長度，不會停留在舊的中文長度）；搜尋結果清單也正確顯示翻譯後名稱；下拉選單點外部會關閉；子網頁（原型教學/原型原點/原型獲獎/原型結構）確認完全沒有語言切換鈕；桌機版在 `languages` 為空陣列時只顯示「中文（原文）」一項，跟改動前行為一致，沒有 console error。(2) 編輯器：新增語言、輸入翻譯、同步匯出，確認 `nodeData［ ］`／`languages［ ］` 內容格式正確；貼一段刻意包含 `mobileName` 跟 `translations` 的 `nodeData` 文字進「從主網頁匯入」，確認三個節點全部正確匯入（改之前有 `mobileName` 的那個會直接消失）、翻譯內容也正確還原；新增節點再撤銷（pen mode 的 Undo），確認翻譯資料沒有被結構性 Undo 機制清空。

### 手機版全螢幕播放模式（2026-08-02 第二十二次調整）

**需求**：頁首新增一個全螢幕播放按鈕（手機版限定）。播放期間無法使用任何功能——不能跳轉子網頁、不能搜尋——唯一允許的操作是拖曳旋轉球體。退出方式兩種：按 Esc 鍵，或點畫面上一個用「淡、細線」繪製的退出鈕。

**狀態與方法**：新增 `state.mobileFullscreenPlay`（預設 `false`）。`enterMobileFullscreenPlay()`／`exitMobileFullscreenPlay()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近，`toggleFooterContact()` 後面）負責切換；進入時主動關掉搜尋列／搜尋結果／漢堡選單，並且如果當下正好有搜尋標註在顯示，直接呼叫 `dismissFlash()`——確保「無法使用任何功能」不會被進入當下還開著的面板/標註破例。`renderVals()` 開頭把 `mobileFullscreenPlay` 算成 `isMobile && this.state.mobileFullscreenPlay`（不是單純讀 state），避免「先手動切成桌機版，state 裡卻還留著 `true`」這種邊界情況牽動到桌機分支。

**怎麼擋掉每一種功能**：

1. **頁首整個隱藏**（`headerDisplay: mobileFullscreenPlay ? 'none' : 'flex'`，套在 `<header>` 的 `display`）——搜尋圖示、漢堡選單、深淺模式切換、裝置手動切換鈕全部在頁首裡，藏起頁首本身最簡單也最保險，不用逐一確認每個按鈕都被擋掉。用 `display:none`（不是 `sc-if` 卸載），元素還在但不吃任何互動、也不佔版面，做法跟既有的其他 display 切換一致，不會有 DOM 卸載/重新掛載的風險（不用擔心 `_globeElCache` 之類的快取失效問題）。
2. **頁尾隱藏**：新增 `showMobileFooter: isMobile && !mobileFullscreenPlay`，手機版頁尾（Contact 按鈕）原本包在 `<sc-if value="{{ isMobile }}">` 裡，改成 `<sc-if value="{{ showMobileFooter }}">`。
3. **節點全部不可點擊**：節點的 `pointerEvents`／`cursorStyle`（`renderVals()` 手機分支的 `nodes = ordered.map(...)`附近）原本是 `(g.clickable || d.id === this.state.flashId) ? 'auto' : 'none'`——「分類大標可點」跟「目前被搜尋標註的節點也恢復可點」這兩條規則，改成都再加一個 `!mobileFullscreenPlay` 條件，全螢幕播放時不管節點原本可不可點、有沒有被標註，一律 `pointer-events:none`。
4. **球體本身撐滿整個螢幕**：`data-globe-surface` 新增四個依 `mobileFullscreenPlay`切換的樣式——`position:fixed; inset:0; z-index:500; background-color:{{ bgColor }}`（非播放狀態時分別是 `static`/`auto`/`auto`/`transparent`，等於完全沒動原本的樣子）。z-index 給 500，蓋過 `#works`（原本 z-index:2）就夠，不需要蓋過頁首（1000）——頁首這時候整個隱藏，根本不會出現在畫面上。**不需要另外調整球體大小的公式**：`diagramContainerWidth` 本來就是用 `vw`/`vh` 算的（`min(92vw, 88vh)`），容器變成真正的滿版 viewport 之後，同一條公式自然會撐出更大的球體，省了一組另外的全螢幕專用數值。
5. **拖曳旋轉／雙指縮放維持正常**：這兩個操作是直接掛在 `data-globe-surface` 上的 Pointer Events 監聽器（`_onGlobePointerDown`/`_onGlobePointerMove`/`_onGlobePointerUp`），跟頁首/頁尾/節點點擊是完全獨立的機制，隱藏頁首、擋掉節點點擊都不會影響到這組監聽器，全螢幕播放時球體一樣可以正常拖曳旋轉、雙指縮放（使用者只要求擋掉「跳轉子網頁、搜尋等等」這些會中斷播放的功能，縮放屬於單純觀看球體的一部分，跟旋轉是同一類，沒有特別擋）。

**退出方式**：

1. **Esc 鍵**：`componentDidMount()` 新增 `document.addEventListener('keydown', ...)`，判斷 `e.key === 'Escape' && this.state.mobileFullscreenPlay` 才觸發 `exitMobileFullscreenPlay()`，`componentWillUnmount()` 對應移除。
2. **退出鈕**：新增一個獨立的 `<sc-if value="{{ mobileFullscreenPlay }}">` 區塊（緊接在 `</header>` 後面），`position:fixed; top:16px; right:16px; z-index:1500`，一個簡單的 ✕ icon，`stroke-width:1.2`（其他頁首 icon 都是 `2`，這裡刻意減半做出「細線」的區隔）、`opacity:0.45`（「淡」）——播放期間唯一保留的互動元素，不用太搶眼，Esc 鍵是更順手的退出方式，這顆按鈕只是備援，樣式上刻意低調。

**進入按鈕**：頁首 nav pill 裡 `<sc-if value="{{ isMobile }}">`（原本只有搜尋圖示那個區塊）新增第二顆按鈕，圖示是標準的「四角展開」全螢幕符號，跟既有頁首 icon 同樣 `stroke-width:2`（進入按鈕本身不需要「淡、細線」，那是退出鈕的要求）。

**驗證方式**：Playwright 測了完整流程——(1) 點進入鈕後量測 `header` 的 `display:none`、`footer` 元素整個從 DOM 消失（`showMobileFooter` 讓它卸載，不是只隱藏）、`data-globe-surface` 的 `position:fixed`、實際渲染尺寸等於整個 viewport（390×844）、退出鈕存在且 `opacity:0.45`；(2) 點一個原本可點擊的分類大標節點（`r23`「原型結構」），確認畫面上沒有出現真正的轉場覆蓋層（用 `position:fixed; z-index:99999` 這個特徵去找，不是隨便抓 `document.body.lastElementChild`——那樣會誤抓到頁面最後的 `<script>` 標籤），也確認節點 `<a>` 的 `pointer-events` 計算值確實是 `none`；(3) 對 `data-globe-surface` 做拖曳，確認節點座標有跟著改變（旋轉仍正常運作）；(4) 按 `Escape` 鍵，確認 `header` 的 `display` 撥回 `flex`、退出鈕消失；(5) 重新進入後改點退出鈕，確認同樣正確恢復；(6) 進入前先搜尋一個節點、讓標註成立，再進入全螢幕播放，確認標註的 pin 圖示 `opacity` 變成 `0`、`display:none`（`dismissFlash()` 有被正確呼叫），手機搜尋列（用 `position:fixed` + 內含 `[data-search-input]` 的特徵去找，避免跟桌機版那個常駐在 DOM 裡、只是 `display:none` 的搜尋框搞混）也確認整個卸載。桌機版（1440px）重新測過，確認進入鈕在桌機版的頁首完全不存在、`header`/`footer` 顯示正常、`data-globe-surface` 的 `position` 仍是 `static`，沒有 console/page error。

### 連續搜尋兩次 icon 消失、強化點擊漣漪動畫的觸發機制（2026-08-02 第二十一次調整）

**問題一：連續搜尋兩次之後，地標 icon 就不會再出現**。根本原因是 `display` 屬性的一個既有邊界情況（追溯起來比「手動切換模式」、甚至比「地標 icon」這個功能本身都早——只是一直沒有被「連續搜尋兩次」這個操作路徑踩到）：`dismissFlash()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）淡出計時器結束時會把 `this._pinLink.style.display = 'none';`，但**沒有任何地方會把它撥回來**——`ensureLandmark()` 只有在 `<a>` 元素第一次被建立時才會透過 `cssText` 設一次 `display:flex`，之後每次呼叫都因為元素已經存在（`document.body.contains()` 判斷為真）直接 `return`，不會重新套用任何樣式。結果就是：第一次搜尋，icon 正常出現（第一次呼叫 `ensureLandmark()` 時設的 `display:flex` 還在）；使用者滑開／點掉標註，`dismissFlash()` 把 `display` 改成 `none`；第二次搜尋，`goToNode()` 呼叫 `ensureLandmark()` 卻因為元素已存在直接 return，`display:none` 沒人清掉，icon 從此再也不會出現，即使 `opacity`／`transform`／`_pinLineSvg` 的虛線都還在正常更新。

**修法**：`goToNode()` 手機分支裡，每次設定 `href`／`label` 的同時，主動把 `this._pinLink.style.display = 'flex';` 撥回來——跟 `startFlash()` 每次都重設 `f.style.display = 'block'`（黃色亮點那顆）是同一個道理，`ensureLandmark()`/`ensureFlash()` 只負責「元素存不存在」，「每次要不要顯示」要在每次真正觸發的地方自己保證。

**驗證方式**：Playwright 連續搜尋兩次不同節點（先「台南咾咕石芳宅」，中途點掉標註，再搜「原型結構」），修正前第二次搜尋後量到 `display:none`、icon 的 bounding box 全部是 0；修正後兩次都正確是 `display:flex` 且有正常的座標。

**問題二：使用者回報「漣漪動畫不是點擊時觸發，而是地標一出現就不斷播放」**。這個問題比較特殊，記錄一下排查過程：在本機用（1）`page.mouse.click()`、（2）真實觸控 `page.touchscreen.tap()`、（3）在 `<a>` 上monkey-patch `addEventListener` 加計數器監看 `click` 監聽器實際被呼叫的次數，三種方式反覆測試，從搜尋定位成立那一刻起持續監看 3~4 秒**完全沒有點擊**，`.pin-ripple` 的 `animationName`／`opacity` 全程都是 `none`／`0`，click 監聽器的呼叫次數全程是 `0`——**沒有在本機重現出「不點擊就自動播放」的情況**。進一步跟使用者確認過，看到的確實是 icon 本身在閃爍/擴散，不是虛線流動效果（虛線持續流動是很早以前就有、故意設計的「飛碟降落」效果，跟這次新增的點擊漣漪是兩回事）。

**排查結論**：懷疑是真實手機瀏覽器上某種本機環境重現不出來的重複觸發路徑（例如觸控裝置常見的「ghost click」相容性補觸發，或其他無法在無頭瀏覽器裡模擬的行為），沒辦法從程式碼邏輯本身找到「自動播放」的路徑（`_pinGrowStart` 只在 `goToNode()` 裡設一次、不會被任何迴圈重複重設；`click` 監聽器只在 `ensureLandmark()` 第一次執行時綁定一次，不會重複綁定）。既然抓不到確切成因，做法是讓觸發機制本身對「不管什麼原因造成的重複觸發」免疫，不再去猜成因：

1. **500ms 防重複觸發**：`playPinClickAnim()`（`ensureLandmark()` 裡，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）用 `this._pinRippleLastTrigger` 記錄上次真正播放的時間，500ms 內的重複呼叫直接忽略——動畫本身只有 0.5s，同一次使用者互動不可能真的需要在這麼短時間內重播第二次，任何「補觸發」的重複呼叫都會被這個窗口吃掉，不會疊加成看起來像連續播放。
2. **`animationend` 明確收尾**：不再完全依賴 `animation-fill-mode:forwards` 讓瀏覽器自己把動畫定格在結束畫面——改成監聽 `.pin-ripple` 的 `animationend` 事件，動畫真正播完的當下明確把 `animation` 設回 `'none'`、`opacity` 設回 `'0'`、`transform` 設回初始的 `scale(0.25)`。這樣就算某些行動裝置瀏覽器對「用 `none`→強制 reflow→重新指定動畫名稱」這種手動重播技巧的 `fill-mode` 支援不夠一致（沒辦法排除這個可能性，但也沒辦法在本機驗證），至少動畫播完之後一定會被明確收回乾淨的靜止狀態，不會有殘留的中間畫面被誤判成「還在播放」。

**驗證方式**：Playwright 在同一個 `<a>` 上用 `dispatchEvent(new MouseEvent('click'))` 連續觸發 10 次（模擬任何形式的重複觸發），確認畫面上只播出一次 `pinClickRipple`；等動畫時長過後（700ms）確認 `.pin-ripple` 確實回到 `animation:none; opacity:0` 的乾淨靜止狀態，沒有卡在動畫中間；再等一段冷卻時間後補一次全新的點擊，確認漣漪仍然正常觸發（不是被永久防呆鎖死）。這些改動沒有解決「本機重現不出來的真正成因」，如果使用者的裝置之後還是看得到同樣的現象，需要請使用者提供螢幕錄影或更精確的裝置/瀏覽器資訊才能繼續往下查。

### 修正兩組獨立分佈公式的極點重疊 bug、icon 再下移 1cm、搜尋時球體整體下移空出 2cm（2026-08-02 第二十次調整）

**問題一：「台南咾咕石芳宅」（r114）跟「原型獲獎」（r115）重疊**。根本原因在 `computeSphereUnits()`（見第一次修正球面分布的詳細說明，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）：11 個 `specialIds` 分類大標跟其餘 105 個一般節點各自獨立跑一次 Fibonacci sphere 公式，`y` 座標原本用 `1 - (i/(n-1)) * 2` 計算——這個寫法會讓**任何一組**的 `i=0`（第一個）精確等於 `y=+1`（北極點）、`i=n-1`（最後一個）精確等於 `y=-1`（南極點），**不管 n 是多少都一樣**，因為公式本身就是「把 i 從 0 到 n-1 線性映射到 y 從 +1 到 -1」，兩端點必定剛好是 ±1。兩個分組各自獨立套用同一條公式，代表**兩組的「第一個成員」永遠疊在同一個北極點、「最後一個成員」永遠疊在同一個南極點**——這跟兩組實際成員數量、`theta`（經度）怎麼分佈完全無關：極點上 `rAtY = sqrt(1-y²) = 0`，`x = cos(theta)*rAtY`、`z = sin(theta)*rAtY` 不管 theta 是多少都會算出 0，也就是說在極點，經度這個維度直接被壓扁成一個點，theta 再怎麼散開也救不了。用 Playwright 實際算過兩組的座標，改之前 r114（一般組最後一個，`i=104, n=105`）跟 r115（特殊組最後一個，`i=10, n=11`）的球面角距離精確是 `0.00°`——不是「碰巧很近」，是數學上保證重合。（連帶查出另一個潛在的同類重疊：特殊組第一個 `r1` 跟一般組第一個 `r3` 也精確疊在北極點，這次順便一起修掉，即使使用者這次沒有回報看到。）

**修法**：`y` 座標改用「每格中心點取樣」，`y = 1 - (2i + 1) / n`——這是更標準的 Fibonacci sphere 取樣寫法，`i` 從 0 到 `n-1` 時 `y` 只會逼近 ±1、**永遠不會精確等於** ±1（`n=105` 時最接近的值是 `1 - 1/105 ≈ 0.9905`，不是 `1`）。兩個獨立分組不管各自 n 是多少，理論上都不可能再剛好落在同一個精確極點。改完重新驗證：r114/r115 角距離變成 `18.59°`，r1/r3 變成 `16.71°`；順便掃過全部 11×105 個「特殊組 vs 一般組」組合，找最近的一對——改之前最近的就是這兩對精確 `0°` 重疊，改之後最近的一對角距離是 `2.00°`（其餘同組內部的節點本來就各自跑同一條連續公式，理論上不會互相重疊，這次公式調整也不影響同組內部的既有分佈品質）。

**做法**（`computeSphereUnits()` 裡的 `placeGroup()`，只動一行）：`const y = n > 1 ? 1 - (i / (n - 1)) * 2 : 0;` 改成 `const y = 1 - (2 * i + 1) / n;`（`n=1` 時算出 `y=0`，跟原本 `n<=1` 的特例分支結果一致，不用再額外判斷）。`theta = goldenAngle * i` 完全沒動，黃金角讓連續索引均勻散開球面的性質不受影響。

**驗證方式**：Playwright 直接讀取手機版（390×844）初始畫面（不搜尋、不操作，剛載入的狀態——這正是使用者截圖回報重疊時的狀態）裡 r114/r115 兩個圓點的螢幕座標，改之前兩點幾乎完全疊在同一個像素（螢幕距離 0px），改之後相距 17.3px，肉眼可辨。桌機版完全不受影響（`renderVals()` 的桌機分支不呼叫 `computeSphereUnits()`，走自己的 2D arc-walk 排版，見既有註解）。

**問題二／三是同一次調整一起處理的**：使用者接著要求 icon 再往下移 1cm，並且「搜尋觸發標註出現時，整顆球體要往下移動，讓 icon 完整出現在畫面中（最上緣至少離頁首下緣 2cm）」。

**icon 再下移 1cm**：延續第十九次調整的做法，`lineLen`（圓點到 icon 的距離）係數再減 `1.0`，從 `CM_PX * 2.3` 改成 `CM_PX * 1.3`（累計比最初的 `2.5` 短了 `1.2cm`）。

**搜尋標註出現時整顆球體下移**：舊的做法是只夾 `endY`（icon 錨點本身的下限，`headerBottomY + pinIconHeight + 8` 那組 8px 極小緩衝），這只能保證 icon「沒有真的疊進頁首那塊」，離「完整、舒服地露出畫面」還差很多，而且這種夾法只夾 icon 自己，圓點沒有跟著移動，夾到底的時候虛線會變得很短、貼死在頁首下面，視覺上不像「線從圓點長出來」，比較像卡住。這次改成不夾 `endY`，而是把整顆球（`<svg data-globe-svg>`，節點、連線、標題全部在同一個 `<svg>` 裡）整組往下平移一段距離——圓點、虛線長度、icon 相對圓點的位置完全不變，只是整組一起往下挪出頁首要求的 2cm 空間。

實作在 `tick()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近，跟原本算 `endY`、`dasharray` 的同一段迴圈裡）：

1. `<svg>` 標籤加上 `data-globe-svg="1"`（方便查詢）跟 `transition: transform 0.4s ease;`（讓後面用 JS 改 `transform` 時會自動補間出平滑的動畫，不用自己手刻 lerp）。
2. 每一影格算出「這次需要的 icon 最上緣位置」（`headerBottomY + pinIconHeight + CM_PX * 2`）跟「icon 目前實際的最上緣位置」（`endY - pinIconHeight`）的差距，把差距轉換成球體需要往下平移的量，寫進 `<svg>` 的 `transform: translateY(...)`。
3. **這裡有一個容易踩的陷阱，記錄下來**：不能直接拿「這一影格量到的 `endY`」跟需求值比較、把差值疊加到已經套用的位移量上（ratchet 疊加），因為 `<svg>` 上的 `transform` 有 0.4s 的 CSS transition，這一影格量到的 dot／icon 位置其實是「補間過程中」的即時值，不是最終值——如果每影格都把「這一刻還沒補完的差距」疊加上去，會跟轉場動畫自己的補間互相打架，越疊越多，位移量失控暴衝。正確做法：用 `getComputedStyle(svg).transform` 配合 `DOMMatrix` 讀出「這一影格當下真正生效的即時位移量」（矩陣的 `.f` 分量），從量到的 `endY` 扣掉這個即時位移，還原出「完全沒有位移時」icon 應該在哪裡，再拿這個還原值跟需求值比較、算出正確的**絕對**目標位移量（不是疊加值），每影格都是獨立算對，不會受轉場進度影響、不會暴衝。
4. 標註消失時（`dismissFlash()`，以及 `tick()` 裡 `showPin` 為 false 的分支）把 `<svg>` 的 `transform` 重設回空字串，靠同一個 CSS transition 平滑地把球體移回原位。

**驗證方式**：Playwright 搜尋「台南咾咕石芳宅」並等待生長動畫＋飛到極點動畫＋位移轉場全部穩定後，量到 icon 最上緣離頁首下緣 94.6px（需求是 2cm ≈ 75.6px，達標且留有餘裕），`<svg>` 的 `transform` 確實是 `matrix(1,0,0,1,0,15.3)`（往下平移約 15.3px）；點擊畫面空白處讓標註消失後，`transform` 平滑地變回 `none`，球體位置跟平移前逐像素一致（比對截圖）。桌機版全程 `transform` 維持 `none`（桌機沒有 `ensureLandmark()`／`_pinLineSvg` 這組元素，`tick()` 裡這整段邏輯自然是 no-op）。點擊地標 icon 的按壓／漣漪動畫（第十九次調整）重新驗證仍正常觸發，沒有被這次改動影響。

### 地標 icon 下移 0.2cm、新增點擊回饋動畫（2026-08-02 第十九次調整）

**需求**：使用者要求（1）地標 icon 的位置再往下移 0.2cm；（2）新增一個滑鼠/觸控點擊地標 icon 時的動畫，示意「點下去會進入這個節點的子頁」。

**下移 0.2cm**：地標 icon 的錨點位置是 `dot 位置 - lineLen * eased`（見 `tick()`，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近），`lineLen` 是虛線＋icon 離圓點的總距離，原本是 `dotR + 60 + CM_PX * 2.5`（第九次調整訂的往上拉 2cm、第九次再加 0.5cm）。這次把係數從 `2.5` 改成 `2.3`，等於整條線縮短 `0.2 * CM_PX ≈ 7.56px`，icon 跟著往下（往圓點方向）靠近 0.2cm。**不影響**夾住頁首下緣的 `minY` 防呆邏輯（第十五次調整加回的那段）——這次量測到的 icon 位置仍然遠低於 `minY` 門檻，沒有被夾住，純粹是 `lineLen` 本身變短生效。

**點擊回饋動畫**：地標 icon（`ensureLandmark()` 建立的 `<a>` 元素，[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）點擊後，實際的頁面跳轉是靠檔案最下面一段全域 `document.addEventListener('click', ...)` 攔截 `a[href$=".dc.html"]` 的點擊、`preventDefault()` 後播放「閃字」轉場動畫（`WORD_POOL`）才真正導航——這段既有機制沒有動。這次新增的是**在轉場動畫接手之前，icon 自己先給一個立即的視覺回饋**，讓使用者清楚感覺到「有點到」：

1. `ensureLandmark()` 建立 icon 時，額外插入一個 `<span class="pin-ripple">`（`position:absolute`，疊在 icon 正中央，用跟黃色亮點同一組顏色的放射狀漸層），預設 `opacity:0` 不可見。
2. `<style>` 區塊（跟既有的 `flashBlink`/`arrowPulse`/`dashFlow` 放在一起）新增兩個 keyframes：`pinClickPress`（icon 本身縮小到 0.75 倍再彈回，模擬按壓感）、`pinClickRipple`（`pin-ripple` 從 `scale(0.25)` 放大到 `scale(2.6)` 同時淡出，像水波往外擴散）。
3. `a.addEventListener('click', ...)`（`ensureLandmark()` 裡新增）：每次點擊時把 `animation` 先設成 `'none'`、強制觸發一次 reflow（`void el.offsetWidth`），再重新指定動畫名稱——這是讓同一個 CSS animation 可以「重播」的標準做法（不這樣做的話，animation-name 沒變，瀏覽器不會重新播放）。這個監聽器掛在 icon 自己身上，會在全域轉場攔截器（掛在 `document` 上）之前先觸發（同一個 `click` 事件，target 自身的監聽器一定先於祖先元素的監聽器執行），所以按壓/漣漪動畫一定會先起頭，轉場閃字動畫再疊上來銜接。

**驗證方式**：Playwright 測了完整流程——手機搜尋「原型結構」、點搜尋結果讓地標出現並等待 380ms 生長動畫結束、量測 icon 位置確認在頁首下方且未被夾住；接著點擊 icon 本身，於點擊後 ~80ms 量測 `getComputedStyle` 的 `animationName`，確認 `pin-ripple` 是 `pinClickRipple`、icon 的 `<svg>` 是 `pinClickPress`（動畫確實有被觸發，不是死值）；最後確認點擊 icon 之後轉場閃字覆蓋層仍正常出現（`opacity` 接近 1、文字是目的頁面名稱），代表新加的回饋動畫沒有干擾到既有的導航機制。

### 手機版標題縮小、「×」精確釘在球體核心（2026-08-02 第十八次調整）

**需求**：使用者要求手機版「原型 × 未來」標題字級再縮小一半，並且「把 X 設定為球體的核心」——不是含糊地讓標題整體看起來「大概在中間」，是要「×」這個字元精確對齊球體投影中心。

**桌機版原本的做法**：`titleFontSize`/`subtitleFontSize`（62px／22px）套用在一個 `foreignObject` 包住的 `<div style="text-align:center;">` 上，裡面是一整串文字「原型 × 未來」，靠 `text-align:center` 讓整個字串在容器裡置中。這個做法能讓 × 看起來「差不多在中間」，純粹是因為「原型」「未來」剛好都是 2 個字、字寬相近，×兩側的空白對稱——**不是結構上保證的，換字型、加減字都可能讓 × 偏移**，使用者這次要的是真正結構上釘死的置中，不是巧合。

**做法**（`renderVals()`／樣板的 `<svg>` 區塊，只動手機分支，桌機逐字不變）：

1. `titleFontSize`／`subtitleFontSize` 改成 `isMobile ? 31 : 62`／`isMobile ? 11 : 22`（其餘邏輯不變，只是把原本寫死的 `62`/`22` 拆成依 `isMobile` 選值）。
2. 手機版標題不再用「一整串文字置中」的做法，改成三個獨立的 SVG `<text>` 元素：
   - `×` 用 `x="450" y="450" text-anchor="middle" dominant-baseline="central"`——`(450,450)` 正是 `buildGlobeGeometry()`/`renderVals()` 全程共用的球體投影中心座標（`cx`/`cy`），`text-anchor:middle`＋`dominant-baseline:central` 讓這個字元的視覺中心精確對齊到這個點，不是「大概對齊」。
   - `原型` 用 `text-anchor="end"`，錨點 `x` 是 `titleLeftX = cx - titleCoreGap`（往左偏移一段距離，靠右對齊，讓文字尾端貼齊偏移點）。
   - `未來` 用 `text-anchor="start"`，錨點 `x` 是 `titleRightX = cx + titleCoreGap`（往右偏移，靠左對齊）。
   - 副標題「Architecture + Structure」維持水平置中（`text-anchor="middle"`，`x="450"`），垂直位置用 `titleSubtitleY = cy + titleFontSize * 0.9` 往下偏移，字級變動時位置還是跟著合理縮放。
   - `titleCoreGap = titleFontSize * 0.6`——原型/未來兩側的偏移量依目前字級等比例算，不是寫死的 px，之後如果又要調字級，排版不會跟著跑掉。
3. 桌機版的 `foreignObject`+`div` 標題整段包進 `<sc-if value="{{ !isMobile }}">`，手機版三個 `<text>` 包進 `<sc-if value="{{ isMobile }}">`，跟其他手機/桌機分流的地方（節點、頁尾）用同一種模式。

**驗證方式**：Playwright 量測——(1) 手機版「×」文字元素的 `getBoundingClientRect()` 中心點，換算成螢幕座標後跟球體投影中心（SVG viewBox `(450,450)` 換算成螢幕座標）的誤差是 `0.00px`（完全精確對齊，不是「差不多」）；(2) 「原型」「未來」文字內容跟位置正確；(3) 手機版標題字級量到 `31px`、副標題 `11px`（桌機分支對照組維持 `62px`/未變動）；(4) 桌機版（1440px）重新截圖比對，確認 `<svg>` 裡沒有 `×` 這個獨立文字元素（桌機走的是 `foreignObject` 分支，不是 `<text>` 分支），標題文字、字級跟改動前逐字一致。

### 手機版搜尋「點了沒反應、黃色亮點沒出現」（2026-07-31 修正，兩個 2D 版本之間發生，3D 球體沿用同一個修法）

桌機版搜尋框外層 `<div>` 有 `data-search-box="1"`（[建築事務所首頁.dc.html:143](./建築事務所首頁.dc.html)），`componentDidMount()` 裡的全域 `mousedown` 監聽器（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) `_onDocMouseDown`）靠這個屬性判斷「點擊發生在搜尋框內還是外」——點在框外才會清空 `searchResults`。手機版搜尋列（`showMobileSearchBar` 區塊，[建築事務所首頁.dc.html:100-121](./建築事務所首頁.dc.html)）當初新增時，外層 `<div>` **忘了加上同一個 `data-search-box="1"` 屬性**。問題在於：真實手機點擊（touch tap）在瀏覽器裡會依序觸發 `mousedown` 再 `click`（不是只有 `click`）；`mousedown` 先發生時，因為手機搜尋列沒有 `data-search-box`，全域監聽器判定「點在框外」，立刻把 `searchResults` 清空、對應的結果列 DOM 直接被移除——等 `click` 事件真正要觸發時，那一列已經不在畫面上了，`goToNode()` 根本沒被呼叫，黃色亮點自然不會出現。**這個 bug 只有用真實的點擊事件序列才測得出來**：先前用 `element.click()`（DOM API 直接觸發，只會發出單一個 `click`，不會先發 `mousedown`）驗證「搜尋能定位」時看似正常，就是因為繞過了這個真實使用者互動才會踩到的路徑——之後如果要再驗證任何跟 `mousedown`/`click` 交互相關的行為，要用 Playwright 的 `locator.click()`（會走完整事件序列）而不是 `page.evaluate(() => el.click())`。修法：手機搜尋列外層 `<div>` 補上 `data-search-box="1"`，跟桌機版一致。

### 其他仍然有效的手機版細節

1. **頁首**：手機版隱藏 Team／Film／Contact，只留漢堡選單、搜尋圖示（點了才展開輸入框）、深色模式切換——跟圓環圖是 2D 還是 3D 無關，兩次重大調整都沒有動這部分。
2. **手機版連線加粗**：`stroke-width` 改成 `{{ edgeStrokeWidth }}`，`renderVals()` 算成 `isMobile ? 2 : 1`——3D 球體同一時間畫出的連線比早期 19 節點版密得多，細線容易糊成一片，這個加粗延續下來。
3. **手機版黃色亮點定位的邊界判斷**：`tick()`（[建築事務所首頁.dc.html](./建築事務所首頁.dc.html) 附近）手機版用 `margin=16`（桌機維持 `52`）判斷目標點是否「太靠邊該顯示三角箭頭」。3D 球體容器縮到 60vw 後所有節點的螢幕座標都遠離邊界，這個數字沿用不用再調。
4. **手機版搜尋結果不再依節點是否「畫得出來」篩選**：3D 球體的每個節點永遠存在（只是文字標籤依 `depthT` 決定顯不顯示），`handleSearchInput()` 讀完整 `this.nodeData`（116 筆）即可，不需要額外篩選。
5. **開場影片 sessionStorage 判斷、中央標題 `white-space:nowrap`**：這兩件事跟圓環是 2D 還是 3D 無關，維持 2026-07-15 修好的邏輯不變（見下面「仍然有效、沒有被回退影響的修正」）。

### 歷史紀錄（2026-07-15 ～ 2026-07-31，「精簡手機版」跟「2D 全節點縮圖」兩個階段，已不再是目前行為）

保留這段是因為之後如果又有人建議「手機版節點太密看不清楚，要不要砍掉一些低連線的節點」，這裡記錄了曾經這樣做過、後來使用者明確要求改回完整版——不要重蹈覆轍再繞一次。

1. ~~圓環節點圖曾經拉高最小字級（15→20 viewBox 單位）、放大點徑（`rMin/rMax` 調過 4-24 → 8-34 → 24-70 → 33-92 幾輪）~~——2026-07-31 已全部改回跟桌機相同的 4/24、15。
2. ~~手機版曾經先篩掉連線數 ≤3（且非 `specialIds`）的節點，只畫 19 個~~（`Component.getMobileVisibleIds()`，門檻調過兩次，一開始 `>1` 後改 `>3`）——2026-07-31 這個方法跟篩選邏輯已整個移除，116 個節點全部畫出。當初這樣做是為了让剩下的節點能放大到「相鄰圓點間距約 12px」的可讀性目標，但使用者後來認為這犧牲了「完整度」，要求改回等比例縮小而不是砍節點。
3. ~~手機版曾經關掉「原型結構」朝中央標題的光暈裝飾（`hasSpotlight: isMobile ? false : !!spotlight`）~~——當初是因為節點少、圓點放大後，這個裝飾色塊在稀疏的圖上顯得特別醒目，容易被誤認成「沒有終點的連線」。2026-07-31 節點數改回 116 個、密度跟桌機一致後，這個問題的前提已經不存在，光暈效果跟桌機一樣照常顯示。
4. ~~圓環容器一度放大到 `min(94vw,82vh)`，後來因為兩側文字被切到螢幕外，改縮小到 `min(65vw,59vh)`，同時 `gap`（20→10）、`fontSize`（15→20，這是上面第 1 點「拉高字級」之後又再調小）一起微調~~——這三個數字都是配合「先放大字級/點徑、再篩掉節點」這條路線量身調的，2026-07-31（第一次）改回桌機數字之後全部作廢，改成單一的容器寬度公式（`min(53vw,48vh)`）解決同樣的溢出問題。
5. ~~2026-07-31 第一次調整後的「2D 圓環、但畫出完整 116 個節點、容器縮到 min(53vw,48vh)」這整個版本~~——這版本本身撐不到一週：同一天稍晚使用者接著要求把 2D 圓環改成可拖曳旋轉的 3D 球體，`diagramContainerWidth` 的 53vw、`rMin/rMax`/`fontSize`/`gap` 跟桌機共用同一份數字這些設計，全部被上面「目前狀態：手機版 3D 球體」那套新邏輯取代。文字框超出螢幕的量測方法也跟著換了一套（3D 版要測拖到很多角度後的最差情況，2D 版量一次就夠），兩者的「安全容器寬度」數字不能互相套用。

### 仍然有效、沒有被這次回退影響的修正

1. **開場影片改用 sessionStorage 判斷「只在真正新進站時播一次」**（2026-07-15 修正）：原本只靠 `document.referrer` 判斷「是不是從站內子頁過來」，但這個做法在「按瀏覽器上一頁」時不保證生效（要看瀏覽器有沒有用 bfcache 還原畫面，見上面第 121-124 行的說明）。使用者回報「任何從子頁按上一頁回首頁都不該播」之後，改成更直接可靠的做法：`sessionStorage` 記一個 `sc_introPlayed` 旗標，**只要這個分頁這次工作階段已經播過一次，不管是點連結、按上一頁（不論有沒有吃到 bfcache）、還是重新整理，一律不再播**；只有分頁關閉重開、或真的開一個新分頁進站，`sessionStorage` 才會是空的、才會再播一次。`document.referrer` 判斷降級為 `sessionStorage` 被瀏覽器封鎖時（例如嚴格隱私模式）的備援，不是主要機制了。程式碼在 [建築事務所首頁.dc.html:16-60](./建築事務所首頁.dc.html) 附近。
2. **中央「原型 × 未來」／「Architecture + Structure」在部分手機瀏覽器會斷成兩行、位置跑掉（2026-07-15 修正）**：這兩行文字原本沒有設 `white-space:nowrap`（節點自己的文字標籤有設，但中央標題忘了設），在某些手機瀏覽器的 SVG `foreignObject` 縮放實作下，文字有機率換行，兩行之間的相對位置就會跟著跑掉，看起來像「兩行文字分開了」。修法：兩行都加上 `white-space:nowrap`，防止被換行。2026-07-15 當時還一併把標題／副標題字級做成手機版加大（62→90、22→32），但 2026-07-31 已經跟著「圓環改回桌機完整度」一起復原成桌機同樣的 62／22／420，`white-space:nowrap` 本身則繼續保留（跟字級大小無關，任何字級下都需要防止換行）。
3. **（附帶發現，尚未處理）桌機/平板在 768-820px 寬度區間，節點文字一樣有超出螢幕的情況**：容器 `min(90vw,86vh)`、字級 15、`gap` 20 這條桌機路徑完全沒被手機版的任何一次改動碰過，但在 768px 跟 820px 寬度下，116 個節點裡有 53 個文字標籤超出螢幕範圍——這是既有問題，不是任何一次手機版調整造成的，本次沒有動它（範圍外、且這種較寬螢幕的個別調整風險評估需要另外討論），先記錄起來，之後如果要處理平板寬度的顯示，這是要修的地方。
