# 子網頁維護手冊：從 Excel 同步到子頁面

這份手冊講的是「子網頁範本」本身的規則，不是針對「原型教學」這一頁寫的——`原型教學.dc.html` 只是目前唯一一個已經做出來的範例，其他子頁（原型原點、原型結構、原型建築、原型獲獎…）之後套用同一套版型時，也照這份手冊處理。跟首頁的 [維護手冊_Excel資料同步.md](./維護手冊_Excel資料同步.md) 是同一組文件，建議兩份一起讀。子頁內文段落裡如果要放「案例資訊卡」手風琴元件（`原型原點.dc.html` 的「海之博物館」卡是範例），做法是獨立一份手冊：[維護手冊_案例資訊卡元件.md](./維護手冊_案例資訊卡元件.md)。

## 背景：這個範本被哪些頁面共用

活頁簿 `活頁簿1.xlsx` 裡，除了首頁用的「工作表1」，還有好幾個「跟子頁同名」的工作表，每個都準備了同一種欄位結構（編號／大標／小標），代表這些都是套用同一個子頁範本的候選頁面：

| 工作表名稱 | 對應網頁 | 目前狀態 |
| --- | --- | --- |
| 原型教學 | `原型教學.dc.html` | ✅ 已建置 |
| 原型原點 | `原型原點.dc.html` | ✅ 已建置（**大事記時間軸從「原型教學.dc.html」搬移過來放在這頁**，緊接在「原型三時期」下方，兩者用接續動畫串接——見「垂直時間軸」那節；滑塊 02/03/04 已匯入照片，其餘尚未匯入，暫用佔位圖示；滑塊 01-04 已指定 `target`，05-07 尚未指定，點擊無動作） |
| 原型結構 | `原型結構.dc.html` | ✅ 已建置（無時間軸模組；工作表僅 4 筆資料 01-04＝木構造/竹構造/RC構造/鋼構造，小標欄目前是空的；4 個滑塊已依大標名稱對應 `media/原型結構/` 資料夾照片，複製進 `uploads/原型結構/`，檔名維持原始，皆用白字 `textOverride`（該來源資料夾裡另有一張「螢幕擷取畫面 2025-08-04 144233.png」跟大標名稱對不上，不是照片素材，未複製）；`target` 皆未指定，點擊無動作；中英文內文為維護者提供的手寫文案，非 Excel 驅動；`<h1>` 標題與內文段落之間額外插入四張案例資訊卡手風琴，對應木構造/竹構造/RC構造/鋼構造，規格清單內容尚未提供，先用【...】佔位，且左右邊框拿掉、只留上下框線當分隔線，跟元件手冊預設的四邊框樣式不同；展開時原本 `.sc-accordion.open` 的 `box-shadow` 也拿掉了會在四周形成一圈 1px 實線的 `0 0 0 1px rgba(180,162,73,0.3)` 那段，只留下方的模糊光暈陰影，確保展開狀態左右也不會再冒出邊框；標題與內文左右各留 `1cm` 內距（`.sc-accordion` 的 `padding:4px 1cm`，取代元件手冊預設的 `padding:4px 24px`），內文夠長換行時右側一樣會保留這 1cm，不會頂到邊框——之後同頁若要再加卡要沿用這個拿掉左右框、留 1cm 內距的版本；四張卡（木構造/竹構造/RC構造/鋼構造）都因為要放很多案子的資料，統一改成「案例輪播」呈現，取代單一固定規格清單；下方規格清單（中英文兩段）自動換成置中案例的資料；資料分別存在 `woodProjects`／`bambooProjects`／`rcProjects`／`steelProjects` 四個陣列裡（欄位格式完全一樣），目前每個都是 6 筆【...】佔位，之後要收錄案子直接在對應陣列 push 新物件即可，不用改 HTML／CSS／JS；四張卡的 `.sc-project-carousel` 各自帶一個唯一 id（`sc-project-carousel-wood`／`-bamboo`／`-rc`／`-steel`），`componentDidMount()` 裡對每個 id 各呼叫一次共用的 `_initProjectCarousel(rootId, projects)`，四份 cleanup 都推進同一個 `_projectCarouselCleanups` 陣列，卸載時一次清掉；`_initProjectCarousel`／`_applyProjectSlidePhotoContrast`／輪播的 CSS 只有一份（寫在木構造卡的 `<style>`），四張卡共用，不用重複貼；輪播縮圖比例橫向 16:9，案名文字不套漸層底色塊，改成 JS 讀照片本身平均亮度（canvas 取樣，見 `_applyProjectSlidePhotoContrast`）決定黑字或白字，跟網頁的深/淺色模式無關，沒有照片的佔位縮圖維持 CSS 預設的黑字；輪播的拖曳滑動效果參考 skiper-ui.com/v1/skiper51「Creative Carousel 002」，原元件是 Swiper.js 的 creative 特效模組，這裡改用原生 Pointer Events 手刻拖曳（`_initProjectCarousel`），拖曳中直接跟隨游標（不套 CSS transition），放開後依拖曳進度（超過一輪位移基準的 35%，`COMMIT_THRESHOLD`）決定補完到下一/上一張還是彈回原位，這時才重新套用 transition 做補間；索引用取模循環，捲到底／捲到最前會自動接回去。**縮圖尺寸這點跟原元件不同，是維護者指定的調整**：縮圖寬度不寫死，改成 `sizeSlides()` 在 mount／resize 時即時量測 `.sc-project-carousel-viewport` 的可用寬度，扣掉左右箭頭保留區 `ARROW_RESERVE`（40px，對應箭頭現有的 32px 寬＋8px 間距）之後再除以 active 的放大倍率，讓照片盡量貼近資訊卡寬度、同時箭頭原本的位置/寬度不變；`STEP_PX`（拖曳位移基準，也是 `REST.next.x` 的位移量）跟著這個量測結果同步，不是固定常數。`next` 的靜止位置維持原元件設定（`REST.next.x = STEP_PX`），但因為縮圖已經放大到接近資訊卡寬度，`.sc-project-carousel-viewport` 改成 `overflow:hidden`（原本是 `visible`）——靜止時「下一張」完全裁在畫面外看不到，只有在拖曳／放開切換到下一張的過程中才會從右側裁切邊界滑進來，對應維護者要求的「案例一過渡到案例二時，案例一向後方/下層退出（縮小＋變暗，原地不平移，套 `REST.prev` 的樣子）、案例二從右方滑入（套 `REST.next` 往 `x:0` 滑動）」。**`STEP_PX` 一度直接沿用縮圖寬度 `boxWidth`，結果縮圖幾乎跟 viewport 一樣寬時，位移一個卡寬並不足以讓整張卡片的左邊界超出 viewport 右邊界，`overflow:hidden` 裁不乾淨、靜止時右側會露出一小截「下一張」的邊緣（維護者回報過這個問題）；改成 `STEP_PX = totalWidth`（viewport 本身的寬度）才能保證卡片中心位移之後、卡片左邊界必定超出 viewport 右邊界，靜止時完全裁乾淨，這點如果之後又要調整縮圖寬度的算法，要記得一起檢查這個邊界條件。**往回（顯示上一張）方向對稱處理：active 退到 `next` 的靜止位置往右滑出，`prev` 原地長大成 active。輕點露出一角的「下一張」縮圖等同前進一格，但因為 overflow:hidden 之後靜止時已經看不到縮圖，這個點擊入口實際上只在拖曳到一半、縮圖局部滑進畫面時才碰得到，主要互動路徑還是拖曳／箭頭／小圓點；下方案子文字資訊（規格清單）的左側要跟照片左側齊平，因為照片在 viewport 裡是置中的、左邊界會落在 `ARROW_RESERVE` 那個位置（不是貼齊 viewport 本身的 0），所以 `.sc-project-detail` 加了 `padding-left:40px`，這個數字要跟 JS 的 `ARROW_RESERVE` 保持一致，之後如果調整箭頭保留區寬度，這兩處要一起改，不然文字會跟照片邊界對不齊。木構造 `woodProjects` 10 筆案子的案名、照片、`基地位置`／`Location` 欄位已經填入真實資料：案名與照片對應維護者提供的檔案（`uploads/原型結構/木構造/`），基地位置文字（中文城市名 / 英文地名）用 `<a href="https://www.google.com/maps/search/?api=1&query=完整地址" target="_blank" rel="noopener">` 包起來變成可點擊的 Google Maps 連結，地址是 Claude 用 WebSearch 查證後手動組出查詢用網址（不是猜的座標／place id），`renderDetail()` 用 `innerHTML` 組字串所以連結能正常渲染；年份欄位 `year`（顯示在縮圖角落）目前先借用地名英文當佔位（例如 `Chiayi`／`Tainan`），之後有正式年份要記得換掉。竹構造／RC構造／鋼構造三張卡的案例資料仍是佔位文字，還沒套用這個做法。木構造 9 個案子（除了華德福實驗分校第二期校舍，維護者沒提供資料）已經填入真實的建築類型／項目狀態（或設計時間＋施工時間）／構造系統／攝影／合作團隊，`renderDetail()` 也跟著改寫成 `buildDetailLines()` 通用版本：欄位改成陣列 `[label, value]` pairs，`value` 是空字串／undefined 就直接跳過那一行，不會印出「XX / 」的空行；資料欄位也從原本手風琴元件手冊預設的「建築設計／結構設計」兩個獨立欄位，簡化成單一 `team`（合作團隊）欄位＋可選的 `designTime`／`constructionTime`（設計時間／施工時間，跟 `status` 二選一），因為維護者提供的實際案例資料是「一段完工年份或工期」＋「一串合作單位」，不是手冊預設的單一建築師＋單一結構技師格式；`archDesign`／`structDesign` 這兩個欄位已經從全部 4 張卡（含佔位資料）移除。英文（`en`）欄位：木構造 9 個真實案子（華德福還是佔位，同上）的 `type`／`status`（或 `designTime`／`constructionTime`）／`system`／`photography` 是 Claude 直接翻譯中文內容產生的（不是維護者提供的官方英文稿，用字/斷句是 Claude 判斷，之後有正式英文文案要記得整段換掉），但 `team`（合作團隊）欄位維護者要求「英文版直接寫中文」不翻譯，所以 `en.team` 跟 `zh.team` 是同一句中文字串，不是英文；攝影欄位裡的工作室／事務所專有名詞（例如「原間攝影工作室」→ Yuanjian Photography Studio）沒有官方英文名稱可查，是 Claude 用拼音音譯的，不是查證到的正式譯名，"原型結構" 例外用了官方英文名 "AS Studio"（這個有查證到）。竹構造 `bambooProjects` 維護者說「只有4個案子」，所以陣列從 6 筆佔位改成剛好 4 筆真實案子（構竹林鐵／繁花之頂／長榮堂_長榮大學竹構集會堂／雲林農博生態園區餐廳），對應的 `hint-placeholder-count` 也從 6 改成 4；中文（`zh`）欄位已經填入真實資料，構竹林鐵這筆維護者沒給基地位置／攝影，就照 `buildDetailLines()` 的規則直接不放這兩個 key（不是空字串），詳情面板會自動跳過那一行。竹構造的英文（`en`）欄位後來也補上了：`type`/`status`/`system` 是 Claude 翻譯的草稿（不是查證過的正式文案），但 `photography`（攝影）跟 `team`（合作團隊）維護者這次要求兩個都直接沿用中文不翻譯——這點跟木構造不一樣（木構造只有 `team` 沿用中文，`photography` 有音譯成英文），如果之後其他資訊卡也要比照処理，要先跟維護者確認這次的規則還適不適用，不要照搬木構造那份 |
| 原型建築 | （尚未建立） | Excel 已有 8 筆資料，網頁未做 |
| 原型獲獎 | `原型獲獎.dc.html` | ✅ 已建置（無時間軸模組；6 個滑塊已依大標名稱對應 `media/原型獲獎/` 資料夾照片，複製進 `uploads/` 並依名稱重新命名，皆用白字 `textOverride`；`target` 皆未指定，點擊無動作；C 欄工作表標題是「小小標」，內容為年份，仍對應到 `en` 欄位） |
| 原型方法 | `原型方法.dc.html` | ✅ 已建置（無時間軸模組，架設人確認不需要）；**這頁不在活頁簿既有候選工作表清單裡，內容不是 Excel 驅動**——`galleryItems` 4 筆（構/法/章/成）與內文 4 段是維護者直接在對話裡提供的手寫文案「構・法・章・成」，`en` 欄位放的是文案裡的中文副標（例如「組構・幾何」），不是英文翻譯；`<h1>` 下方多加了一行置中的「構・法・章・成」字樣副標題，是範本原本沒有的元素；每段內文最後多一句粗體結語（例如「構，不僅是組合的關係理解而是精準幾何的建構。」），也是範本原本沒有的元素，樣式是維護者指定調過的：字級比內文本身還大（結語 32px、內文 26px）、`weight:700`、`opacity:0.5` 呈現灰階，跟內文明顯區隔；`target` 已指定為 `sc-section-gou`／`sc-section-fa`／`sc-section-zhang`／`sc-section-cheng`；首頁 `nodeData` 尚未新增/修改任何節點連到這頁，架設人說之後再處理；四段（構/法/章/成）彼此之間的 flex `gap` 拉大到 112px（範本其他頁面沒有這個間距需求，是這頁專屬調整）。**曾經加過又整段拿掉的東西（記錄一下避免之後重複做白工）**：維護者一度提供簡報《10x10》（`D:\2026as實習\網頁\構法章成.pptx`，20 頁多數是整頁滿版圖，讀法是 unzip 拆 `ppt/media/` 直接看圖，`markitdown` 對這份檔案抽不出文字）要求把摘要加進頁面，Claude 先加了跨頁首圖＋各段配圖＋「成」段的 9 筆已實踐案例清單和 6 張案例縮圖（圖片存在 `uploads/原型方法/`），維護者接著要求把圖拿掉、文字融合進內文段落（`sc-section-gou`／`sc-section-fa`／`sc-section-zhang` 各自多了 1-2 個延伸 `<p>`），再要求連這些融合進來的簡報摘錄文字段落也整段刪除，內文段落當時完全回到最初的手寫文案。**後來又指定要用簡報裡特定 4 頁的照片當畫廊滑塊的圖**（第 4 頁＝水平模型照對應「構」、第 6 頁＝扭轉模型照對應「法」、第 12 頁＝拱之結構原型隧道照對應「章」、第 19 頁＝新營服務區公共廁所照對應「成」），要求去掉圖片裡的文字、裁切成正方形；做法是用 PIL 從 pptx 解出的原圖（`ppt/media/imageN.jpg`）手動框選不含文字的裁切區域（新營那張第一次框選有算錯，裁出來下緣還留了一行標題文字，用 numpy 掃描亮度找出文字實際起始列後重裁才乾淨）、正方形裁切後 resize 成 900x900 存進 `uploads/原型方法/`，檔名直接對應 zh（`構.jpg`／`法.jpg`／`章.jpg`／`成.jpg`），`galleryItems` 加回 `photo`／`textOverride:'#ffffff'` 兩個欄位；**內文的四段文字目前仍是最初的手寫文案，簡報摘錄的文字段落沒有回來，只有滑塊圖片是取自簡報**，如果之後要核對照片來源直接看該 pptx 檔案第幾頁 |

**名稱通常相同，但不要當成必然規則**：工作表名稱目前都剛好等於子頁的中文標題，但**每個子頁實際要抓哪個工作表，請在架設當下直接問架設人**，不要自己假設「頁面叫什麼名字就去讀同名工作表」——萬一之後工作表改名、或某頁刻意要接不同工作表的資料，照名字硬猜會抓錯。

## 版面固定的部分 vs 可選模組

以 `原型教學.dc.html` 為例，結構由上到下：

**每個子頁都要有（固定，不要因為換頁面就重新設計）：**

1. **頁首／頁尾**：跟首頁 `建築事務所首頁.dc.html` 完全一樣的設計，唯一差異是拿掉搜尋框（子頁不需要跨節點搜尋）。**頁首 logo 的連結固定指向 `./index.html`（回到主網頁）**，不要連到外部網址——子頁通常是從主網頁點進來的，logo 是使用者返回主網頁的固定入口，`原型教學.dc.html` 已經是這樣設定，新子頁複製範本時原樣保留即可。**logo 圖檔本身要用 `uploads/favicon.png`（2026-08-17 修正，見下面「頁首 logo 圖檔統一」一節）**：五個既有子頁（原型教學／原型原點／原型結構／原型獲獎／原型方法）原本各自沿用一個舊檔案 `uploads/logo_20210310101340.png`，跟首頁不是同一個 logo；新子頁複製範本時要確認抄的是修正後的版本（`src="uploads/favicon.png"`、`filter:{{ logoFilter }}`），不要抄到舊檔名。
2. **Hover-Expand 橫向滑塊畫廊**（`sc-gallery` / `galleryItems`）——**這是 Excel 驅動的部分**，見下一節。
3. **麵包屑**：「首頁 / {頁面標題}」。
4. **`<h1>` 標題**：頁面中文標題。
5. **一段或多段「中英文對照文字區塊」**：純手寫文案，**不是** Excel 驅動（目前找不到對應工作表）。
6. **檔案最上方還有一段「進場過場動畫」（2026-08-18 改版，見下面「前導動畫」一節）**：從首頁點節點過來時，子頁自己會播放跟首頁第一次進站同一支開場影片（`uploads/網頁前導片.mp4`），不是文字。這段是 5 個子頁完全一致的固定樣板（`<div id="sc-arrival-overlay">` + 對應 `<script>`，約第 9-28 行），**新子頁複製範本時整段照抄即可，不用像舊版 `ARRIVAL_WORD` 那樣改任何頁面專屬文字**。

**可選模組（不是每個子頁都要有）：**

- **垂直時間軸**（`sc-timeline` / `timelineItems`）：**目前放在 `原型原點.dc.html`**（大事記，已從 `原型教學.dc.html` 搬移過來，`原型教學.dc.html` 現在沒有這個模組），是**可選的**，不是子頁範本的必要組成。架設新子頁時要先問架設人這一頁要不要放時間軸，不要預設每頁都要。這頁的時間軸目前是佔位文字，**尚未接上 Excel**（見「尚未接上 Excel 的部分」）。
  - **`原型原點.dc.html` 這裡是特殊情況，不是標準用法**：大事記緊接在「原型三時期」（`sc-h-timeline` / `threePeriodItems`）下方，兩段用接續動畫串接——三時期必須先靠 wheel 事件全部展開完，才會觸發大事記第一筆展開；大事記後續每一筆改回監聽標準 `scroll` 事件（因為大事記展開會撐高頁面、重新出現可捲動空間，不像三時期本身不會長高）。串接邏輯在 `componentDidMount()` 的 `_onThreePeriodWheel`（尾端呼叫 `_revealNextChronologyItem()`）與 `_onChronologyScroll`（開頭判斷 `_threePeriodTargetProgress` 是否已滿）。**如果之後其他子頁也要放垂直時間軸，但那頁沒有「原型三時期」這種頁尾 wheel 模組，就不要照抄這段串接邏輯**——直接沿用時間軸原本最單純的版本（初始預先顯示 2-3 筆＋捲到底部用 `scroll`＋`nearBottom` 逐筆顯示，同「尚未接上 Excel 的部分」下面描述的邏輯），不用管三時期銜接的部分。

## 頁首 logo 圖檔統一（2026-08-17）

**背景**：使用者要求「將所有子網頁頁首有用到logo的都改為新版的logo(跟主網頁一樣的logo)」。比對後發現五個既有子頁（`原型教學.dc.html`／`原型原點.dc.html`／`原型結構.dc.html`／`原型獲獎.dc.html`／`原型方法.dc.html`）的頁首 logo 都是同一個舊檔案 `uploads/logo_20210310101340.png`（深色線稿），跟首頁 `建築事務所首頁.dc.html` 用的 `uploads/favicon.png`（白色線稿）不是同一份圖，兩者原本就沒有同步過。

**發現的連帶問題**：五個子頁的 logo 濾鏡原本直接寫在樣板裡：`filter:{{ isDarkMode ? 'invert(1)' : 'none' }}`——DC 樣板引擎的 `{{ }}` 不支援三元運算子，會靜默失效（見 [維護手冊_Excel資料同步.md](./維護手冊_Excel資料同步.md) 的「已知的資料相依細節」）。實測（Playwright 讀 `getComputedStyle(img).filter` 並切換深色模式）確認這五頁的 `filter` 屬性確實整個消失、深淺色模式切換完全沒有套用濾鏡，是既有的小 bug，只是換成白色線稿的 `favicon.png` 之後才會真正顯現出來（深色線稿不管有沒有濾鏡在淺色底都還看得見；白色線稿如果濾鏡沒生效，淺色模式下logo 會整個「隱形」在白色背景上）。

**做法**：

1. 樣板裡的 `<img>` 從 `src="uploads/logo_20210310101340.png"` 改成 `src="uploads/favicon.png"`，`filter:{{ isDarkMode ? 'invert(1)' : 'none' }}` 改成 `filter:{{ logoFilter }}`（改用變數，不再直接寫三元運算式）。
2. 各子頁的 `renderVals()` 回傳物件新增 `logoFilter: isDarkMode ? 'none' : 'invert(1)'`——**注意這跟原本子頁那個（沒生效的）濾鏡公式方向剛好相反**，因為 `favicon.png` 本身預設是白色線稿（深色模式下不用轉、淺色模式下要反轉成黑色才看得見），跟舊的深色線稿 `logo_20210310101340.png` 的需求方向相反，這跟首頁 `logoFilter` 的公式完全一致。
3. logo 的連結目標（`href="./index.html"`）沒有變動，只換圖檔跟濾鏡。

**驗證方式**：Playwright 對五個子頁分別讀取 logo 的 `src` 與 `getComputedStyle` 算出的 `filter`，切換深色模式前後各測一次，確認：淺色模式 `filter: invert(1)`、深色模式 `filter: none`，五頁結果一致且都跟首頁的行為相符；並截圖比對淺色/深色模式下 logo 的實際顯示（跟首頁的「as」logo 外觀一致，兩種模式都看得見、不會隱形），console 沒有新增錯誤。

## Excel 資料結構：畫廊滑塊（galleryItems）

每個子頁工作表的欄位是（**注意：這些工作表第一列是標題列**，跟首頁「工作表1」沒有標題列不同）：

| 欄位 | 內容 | 對應網頁 `galleryItems` 欄位 |
| --- | --- | --- |
| A（編號） | 二位數編號，如 `01`、`02`... | `num` |
| B（大標） | 中文標題 | `zh` |
| C（小標） | 英文副標，**部分子頁工作表這欄是空的**（例如原型原點、原型結構、原型建築、原型獲獎目前都沒填） | `en` |

以「原型教學」工作表為例，實際資料：

```
編號  大標    小標
01   實習    Internship
02   美感教育  Aesthetic Education
03   孩童    Children
04   文化    Culture
05   設計方法  Design Methodology
06   工作文化  Work Culture
07   大事記   Chronology
```

對應網頁 `galleryItems` 陣列（[原型教學.dc.html:312-320](./原型教學.dc.html)）逐筆一致。

### `galleryItems` 裡 Excel 沒有的欄位——同步時不要動

- **`target`**：點這個滑塊要捲動到頁面裡哪個 section 的 id（例如 `'sc-section-school'`）。純手動維護，Excel 沒有對應欄位。新增/搬動滑塊時要由維護者指定，不確定的話先留空（`undefined`），點擊就不會有動作，不會壞掉，之後再補。
- **`photo`**：只有少數滑塊有背景照片（目前「實習」「工作文化」兩個），路徑對到 `uploads/` 資料夾裡的圖檔。
- **`textOverride`**：搭配 `photo` 用，讓有照片的滑塊文字顏色固定不隨深色模式變動。

同步 Excel 資料時，這三個欄位**維持原樣**，除非維護者明確要求新增照片或改連結目標。

## 同步流程

1. **確認路徑**：跟首頁手冊一樣，Excel 活頁簿路徑、子頁 `.dc.html` 路徑都要維護者明確給，Claude 不會自己找。
2. **確認要抓哪個工作表**：直接問架設人／維護者這一頁要抓活頁簿裡的哪個工作表，不要用「工作表名稱＝頁面標題」自動猜。確認後，A/B/C 欄＝編號/大標/小標，**第一列是標題，從第二列才是資料**。
3. **讀網頁**：解析子頁檔案裡 `galleryItems = [...]` 的原始碼。
4. **比對**：用「編號」對編號，比對大標／小標是否一致；如果 Excel 增刪或搬動了列，代表滑塊數量或順序要跟著改。
5. **套用規則**：
   - `num`／`zh`／`en` 照 Excel 內容寫回。
   - 新增的滑塊，`target` 由維護者指定（沒有明確指示就先留空）。
   - 不要清掉既有的 `photo`／`textOverride`。
   - 不要動這個檔案的 CSS、動畫邏輯（游標拖尾效果、滑塊展開後自動算成正方形、時間軸捲動漸進顯示等），這些都是版型本身，跟資料無關。
6. **套用前列出差異給維護者確認**，不要靜默覆蓋。

## 尚未接上 Excel 的部分（現況如此，不是漏做）

- **時間軸 `timelineItems`（現在放在 `原型原點.dc.html`，記得時間軸本身是可選模組，不是每頁都要）**：**已經是維護者直接在對話裡提供的真實年表內容**（2005-2026，共 22 筆），不再是佔位文字，欄位是 `year`／`title`／`desc` 三個（`title` 是短標題、粗體；`desc` 是較長的說明文字、細體），對應畫面上年份／粗體小標／描述三行。**這批資料目前是用聊天訊息直接貼給 Claude 填入的，還沒有反向同步回 Excel**——Excel 裡有一個「原型大事記」工作表，表頭是「年分／事件／說明／圖片」，但**目前沒有任何資料列**（空的）。如果之後這個工作表補上資料、且維護者要求改成從 Excel 同步：
  - 要先跟維護者確認 Excel 表頭的「事件」「說明」怎麼對應到程式碼現有的 `title`／`desc`（目前這兩個中文詞的意思跟表頭字面不完全一樣，不要照字面硬套），以及要不要一併把「圖片」這欄也做出來（目前畫面上沒有圖片欄位，`.sc-timeline-item-inner` 的版面要跟著擴充才放得下）。
  - 套用前先列出目前 22 筆聊天貼入的資料跟 Excel 版本的差異給維護者確認，不要靜默覆蓋掉聊天訊息裡提供的內容。
- **中英文本文段落**（例如「學校是一切感知的起點」那幾段）：目前找不到任何對應的 Excel 工作表，是直接寫在 `.dc.html` 裡的文案。如果之後要讓維護者透過 Excel 改這些段落，需要另外規劃一個工作表格式，目前沒有，不要自己假設欄位硬套。
- **前導動畫的閃字序列（`WORD_POOL`）**：嚴格來說這是**首頁** `建築事務所首頁.dc.html` 的內容，不是子頁自己的。**2026-08-18 起，這個閃字動畫已經不再用於一般本機子頁**（見下面「前導動畫」一節），只剩「原型建築」（`r109`，外部連結、檔案較大）這個特例還在用。Excel 有一個「前導動畫字串」工作表（單欄、無標題列），對應首頁程式碼裡的 `WORD_POOL` 陣列，同步規則見 [維護手冊_Excel資料同步.md](./維護手冊_Excel資料同步.md)「Excel 資料結構：前導動畫字串（WORD_POOL）」那一節，但現在只影響 `r109` 這一個節點的動畫內容，不算在這份子頁手冊裡。

### 前導動畫（2026-08-18 改版：從字卡輪播換成開場影片）

**現況**：使用者要求「把所有進入子網頁的字卡輪播動畫都換成進入主網頁的logo動畫」——原本首頁點節點會先播一段 `WORD_POOL` 閃字卡片、停留在目的地文字後才跳頁，子頁到達後自己又播一段 `ARRIVAL_WORD` 文字滑開動畫，這整套「字卡輪播」已經拿掉，改成：**子頁到達時播放跟首頁自己第一次進站同一支開場影片**（`uploads/網頁前導片.mp4`，`#sc-arrival-overlay` / `#sc-arrival-video`，見各子頁檔案開頭第 9-28 行左右）。

- **首頁這邊**：點擊本機 `.dc.html` 節點**不再攔截、不播任何動畫**，直接讓瀏覽器原生導頁；點擊當下只用 `sessionStorage.setItem('scTransitionIn', '1')` 設一個旗標告訴子頁「這次是從首頁點進來的」。`playAndNavigate()` 函式已經整個移除。
- **子頁這邊**：讀到 `scTransitionIn` 旗標存在，才播放開場影片（播完/發生錯誤/15 秒逾時保險任一觸發就淡出移除遮罩），並立刻清掉旗標；直接重新整理、直接輸入網址、瀏覽器上一頁都不會有這個旗標，不會播。
- **5 個子頁的這段前導動畫程式碼完全一致**，沒有任何頁面專屬變數要改（不像舊版 `ARRIVAL_WORD` 每頁不一樣），新子頁複製範本時整段照抄即可。
- **「原型建築」（`r109`）這個特例不受影響**：它連到外部網址，走另一條 `playAndPreloadThenNavigate()` 路徑（跳字無限循環＋隱藏 iframe 預載＋20 秒逾時保險），使用者明確要求這條路徑維持原樣不動。

套用這個範本做新子頁時，驗收項目要包含：**從首頁點擊該子頁節點，確認子頁到達時有播放開場影片**（不是文字閃卡），影片播完或逾時後正常顯露子頁內容。

另外驗收時也要測試**瀏覽器的「上一頁」按鈕**：從子頁按上一頁回首頁，首頁應該正常顯示。首頁本身現在點本機子頁節點不會攔截、不會留下未清除的遮罩，所以理論上不會再卡住；`pageshow`／`event.persisted` 的 bfcache 修正目前只保留給「原型建築」那條外部預載路徑用（那條路徑跳字是無限循環、逾時長達 20 秒，被 bfcache 凍結再還原的風險比較高），修改首頁過場動畫程式碼時要注意別把那段 `pageshow` 監聽拿掉。

## 如果要用這個範本做「下一個」子頁（例如原型結構.dc.html）

Excel 裡已經有現成的候選工作表（例如「原型結構」），但網頁還沒做。如果維護者要求新增子頁，**架設之初一定要先問架設人兩件事**，不要自己假設：

0. **問清楚兩件事**：
   a. 滑塊畫廊要抓活頁簿裡**哪一個工作表**（不要用「頁面標題＝工作表名稱」自動猜，萬一要接的工作表跟頁面標題不同名就會抓錯）。
   b. 這一頁**要不要放時間軸**模組——時間軸是可選的，不是每個子頁都要。
1. 複製 `原型教學.dc.html` 當範本另存新檔；如果這頁不需要時間軸，把 `sc-timeline` 相關的 HTML／CSS／`timelineItems`／捲動顯示邏輯整段拿掉，不要留著空模組。
2. 全域取代頁面專屬的字串：`<h1>` 標題、麵包屑文字、`<title>`（如果日後有加的話）。**前導動畫區塊（`#sc-arrival-overlay`）不用改**，5 個子頁都是同一段，沒有頁面專屬變數（2026-08-18 起已經不是舊版每頁要改的 `ARRIVAL_WORD` 了）。
3. `galleryItems` 換成步驟 0(a) 確認過的工作表資料，`target`／`photo`／`textOverride` 需要重新指定（範本沒有自動帶入機制）。
4. 中英文本文段落（若這頁要放時間軸，時間軸內容也是）需要維護者提供新文案，範本不會自動生成。
5. **別漏掉**：回到首頁 `建築事務所首頁.dc.html` 的 `nodeData`，把該節點（例如「原型結構」對應的 `r23`）的 `href` 從外部網址改成 `./原型結構.dc.html`，並在首頁手冊的「固定排除」規則下確認它會被涵蓋（href 已經是站內相對路徑的節點會自動排除，做法比照「原型教學」節點目前的處理方式），否則下次同步 href 時會被 Excel 的舊網址蓋回去。
